# Wander — план

Только несделанное. Закрытое и отклонённое удаляется при финализации, не
помечается: как работает — DONE, почему так — ARCHITECTURE, не делаем —
REJECTED, спека — в гите (FINALIZING.md, п. 4). Буквы секций стабильны — на
них ссылаются другие доки.

| | Приоритет | Когда |
|---|---|---|
| **P0** | Багфиксы, незавершённое из готового, блокеры | Сейчас, до фич |
| **P1** | Обязательное для нормального файл-менеджера | Следующая итерация |
| **P2** | Заметное улучшение | После P1 |
| **P3** | Опциональное / нишевое | Когда дойдём |

Пометки: **[иссл.]** — начинается с исследования или стенда; **[решить]** —
ждёт решения человека, кода не получает; без пометки — спека есть или
пишется перед запуском.

## Версия 0.5.0 — блоки

Решение человека 2026-09-22: блоков 5–7, каждый по одному флоу — Фабле
пишет спеку сразу с местами, именами и связями (большая — отдельным файлом
в `docs/`, как OPERATIONS.md), Опус реализует блок целиком, Фабле ревьюит,
дочищает, финализирует (WORKFLOW, «Фича: четыре этапа»).

| # | Блок | Состав | Спека | Объём |
|---|---|---|---|---|
| 2 | **Панели, папки, список — переработка** | AN модель состояния и правил; AG чей вид — папки или общий; Z1 база параметров папки; H1; долги 0.4.x по списку и сторожу; AD3-хвост; U1 / U2; AB; J4 | закрыт 2026-09-23 (спека — REDESIGN.md в гите); остаток D6-хвост ниже | крупный |
| 8 | **Баги новых функций 0.5** | полный экран: оценка вслепую и без отката, `Alt+Space` / `F10`; первый кадр полного экрана и сплита; сбой IFilter гасит `.doc`; `.jfif` не открывается; гард: «Извлечь рядом» в корень диска, системные папки профиля; провал извлечения — «пароль»; память под картинки; кэш кадров вхолостую; B7-хвост подпись большого exe | ниже | средний |
| 5 | **Панель просмотра: скорость и форматы** | сделано — DONE; осталось: AK повтор замера после блока 8; B10 heic системным кодеком; AL миниатюры и автогалерея в архиве; B8 SVG в панели; B3 футер; B6-хвост | ниже | средний, по пунктам |
| 6 | **Мелочи и сайт** | X вставка из буфера файлом; AD1 WebView2; AD2 корзина — холодный заход; G6 поле фильтра над списком; архивы папками мимо чужой ассоциации [иссл.]; F2 сайт на Pages | ниже | средний |
| 7 | **Доки и релиз** | сжатие ARCHITECTURE, DONE, QA, GUIDE; устаревшие ссылки; анализаторы точечно; Win32-имена [решить]; PRERELEASE | ниже | малый |
| — | **Обсуждения** | Y3 сеть и FTP; B9 системные обработчики; AA синхронизация; M хоткеи; N чек-лист; G5; тёмная тема | | |

Решения человека 2026-09-24: [в 0.5 — блоки 2, 8, 5, 6, 7; «Надёжное
копирование» (был блок 4) — в 0.6: версия и так плотная]; [новые пакеты в
0.5 не вводим — решать в 0.6]; [блок 8 — параллельно с финализацией 2, где
файлы не пересекаются; пересекающиеся — отдельно, после]. Что уже решено
на 0.6 — раздел «Версия 0.6» ниже.

Порядок: 8 → 5–7. 8 — баги того, что 0.5 показывает впервые; 5–7 — по
темам. Повтор замера AK (блок 5) — после правок кэша в блоке 8. Между
блоками — финализация и `check.bat run`. Закрыты: блок 1 — 2026-09-22
(DONE, «Хелперы отсмотра снимков»); блок 2 — 2026-09-24 (DONE, «Навигация,
дерево, закладки», «Клавиатура и области окна», «Список файлов»); блок 3 —
2026-09-24 (DONE, «Диагностика»; AD11 — BACKLOG, «Сборка и поставка»).

## Блок 2. Панели, папки, список — переработка

Закрыт 2026-09-23 (DONE «Навигация, дерево, закладки», «Клавиатура и области
окна», «Список файлов»; ARCHITECTURE «Модель окна», «Вид — у папки»,
«`folders.json`»). Спека блока — `docs/REDESIGN.md` в коммитах 4845b8f и
622a674, удалена при финализации. Остаток — ниже.

### D6-хвост — [P3]

Перетаскивание закладок мышью внутри панели; порядок уже меняется меню и
`Ctrl+↑/↓`. По первому запросу.

## Блок 8. Баги новых функций 0.5

Решение человека 2026-09-24: [баги поверхностей, которые 0.5 показывает
впервые, — в план, до релиза]; [параллельно с финализацией блока 2, где
файлы не пересекаются]. Собраны из TECHDEBT (ревью 2026-09-23 и -24),
блоков 4–5 и живого прогона. Спеки нет: правка — в пункте; где риск тихой
смены поведения (IFilter, гард), первым смотрит Фабле. С блоком 2 делит
файл только «Провал извлечения» (`MainViewModel`) — он после финализации 2.

### Полный экран: оценка вслепую и без отката — [P1]

Три дефекта одной связки, ревью 2026-09-24:

- полоса снимка видна только у `Kind` Image / Gif, а футер при
  `ShowPictureBar` прячет звёзды всегда: у половины пары — видео со
  спутником или RAW, который не разобрался (Unsupported), — звёзд не видно
  нигде, и на полном экране цифра ставит оценку молча;
- `Ctrl+Z` на полном экране не работает — привязка только у главного
  окна. Правка: жест в `UndoCommand` из `OnPreviewKeyDown`;
- отказ записи оценки не виден: ошибка сайдкара — в `CompanionStatus`
  футера, итог — в строку состояния главного окна, на полном экране нет ни
  того, ни другого. Место — строка на полосе снимка или короткая плашка.

Решения человека 2026-09-24: [полоса снимка — у всего, чему предлагается
оценка (`OfferRating`), целиком: звёзды и кнопки хелперов]; [полосу
прятать совсем, пока курсор не у нижнего края (как в FastStone); клавиша
оценки показывает её на время — видно, что поставлено]. Сейчас полоса
приглушена, пока мышь не внизу.

### Полный экран: `Alt+Space` и `F10` — [P2]

По коду, вживую не проверено: одиночный `Alt` погашен, эти два доходят до
системного меню, а «Восстановить» у окна без рамки ломает полный экран.
Сначала глазами; подтвердилось — гасить там же, где `Alt`.

### Первый кадр полного экрана и второй половины сплита — [P1]

Столп 4. `SetPrimary` зовётся, пока половина скрыта; `UpdatePreviewAsync`
ставит `Kind = None`, и после показа видна «выберите файл», пока не придёт
картинка. Поле ещё 0 (`PictureFit.DecodeWidth` → null) — первая пара за
сеанс декодирует второй снимок целиком. Так же первый кадр полного экрана и
первый правый: `SetVisible(true)` раньше, чем панель сообщила размер; у JPEG
24–45 Мп — сотни миллисекунд и больше 100 МБ. Правка: поле по монитору
владельца до `SetVisible` либо включать просмотрщик в `Loaded` панели.

### Один сбой IFilter — все `.doc` «не прочитаны» — [P1]

Ревью 2026-09-24, стендом не проверено: `FilterTextExtractor.Extract`
помечает расширение «фильтра нет» по любому HRESULT — вероятно, и у
запертого или битого `.doc`, и у служебного `~$….doc` Word'а; дальше до
конца сеанса все `.doc` — «не прочитано» в поиске и «нет просмотра» в
панели (показ `.doc` текстом, B5, делает это видным). Правка: помечать
только «фильтр не зарегистрирован»; какие это коды — стендом.

### «Извлечь рядом» в корень диска — [P1]

Найдено 2026-09-23. `ExtractionService.ExtractAsync:68` проверяет саму
папку-цель `SystemPathGuard.IsProtected` — гардом «нельзя двигать и
удалять»: извлечение и вставка из архива в корень диска (`E:\` флешки —
главный случай «рядом») и в корень профиля отказывают, в статусе —
английская строка гарда мимо `Strings.resx`. Обычное копирование цель не
проверяет (`BatchExecutor` — только источник переноса и заменяемое). Так же
— папка выходов действий (`ExternalActionRunner:160`). Правка: проверять
пути назначения, как `BatchExecutor` при Replace, либо отдельное правило
«писать внутрь можно» (дерево Windows — нет, корень диска — да); текст
причины — через `ITextSource`. Правка гарда — полный набор автотестов на
подготовке (PRERELEASE).

### Гард: системные папки профиля — [P1]

Решение человека 2026-09-24: [системные папки пользователя не трогаем — не
наша специфика]. Сейчас `SystemPathGuard` защищает Windows, Program Files,
ProgramData, `C:\Users` и сам профиль, а «Рабочий стол», «Документы»,
«Загрузки», «Изображения», «Музыка», «Видео», AppData удаляются и
переносятся (тест `UserProfileRoot_IsProtected_ButDocumentsAreNot`
закрепляет это — меняется по существу этим пунктом). Правка: известные
папки (Known Folders) — в защищённые корни, сама папка не удаляется, не
переносится и не переименовывается, внутри — как обычно. Вместе с пунктом
выше: тот же гард, тот же полный набор автотестов на подготовке.

### `.jfif` и `.jpe` не открываются в панели — [P1]

Живой прогон 2026-09-24: `.jfif` — «нет просмотра». `ImageFormats`
(Core, `:30`) их не знает, хотя `PictureLoader:50` декодирует оба; список
миниатюр `SystemIconProvider:1122` — тоже без них. Правка — строка в оба
списка и тест в `PreviewRouterTests`.

### Провал извлечения — «архив защищён паролем» — [P1]

`MainViewModel.ExtractAsync` (2026-09-23): все элементы `Failed` и ничего
не пришло — так выглядят и носитель только для чтения, и нехватка места, а
пользователь читает «защищён паролем» (по PRERELEASE — «врёт
пользователю»). Различать по исключению элемента.

### Память под картинки: потолок от памяти машины — [P1]

Два кэша расшифрованных картинок без предела в байтах (из AK и AM,
2026-09-24):

- миниатюры: `IconImageCache` держит 256 штук, бюджет считан на 256 px
  (~0,25 МБ штука); с AM сторона — 384 px при 150–175 % и 512 px при 200 %
  (`ThumbnailCacheOptions.SideFor`): до ~144 и ~256 МБ вместо ~64;
- кадры просмотра: `PictureCache` — три кадра; не-JPEG (PNG, TIFF, WebP,
  TGA) декодируются целиком, соседи тоже: 16-битный TIFF 50 Мп — около
  300 МБ на кадр, до 0,9 ГБ на кэш; скрытая панель кэш не отпускает
  (`SetVisible(false)`).

Живой лог человека 2026-09-24 (сборка из дерева 0.5, `v0.4.1-beta.86
R`): полный экран на CR3 с хелперами — рабочий набор 0,7 → 2,0 ГБ, private
2,3 ГБ, LOH 418 МБ, +4,1 ГБ аллокаций за 5 с, CPU 34 %; через 15 с — 1,1
ГБ. Буферы хелперов по полному кадру (`bg.helpers`) — третий потребитель,
мерить вместе.

Решения человека 2026-09-24: [полгигабайта — нормально]; [потолок по
умолчанию — 1/16 памяти машины: 16 ГБ → 1 ГБ, 32 ГБ → 2 ГБ]; [настройка —
потолок конкретным числом в МБ; заменяет «Кэш в памяти (миниатюр)» в
штуках, `ThumbnailMemoryEntries`]. Как: правило в Core — общий бюджет
картинок от объёма памяти (`GC.GetGCMemoryInfo().TotalAvailableMemoryBytes`,
а не свободной в моменте — она скачет) либо из настройки, доли на
миниатюры и кадры; кэши считают байты (W×H×4); сосед, который в остаток
не влезает, не греется; `Clear` при скрытии панели. [решить] ужимать ли
кэши, когда системе не хватает памяти (`MemoryLoadBytes` ≥
`HighMemoryLoadThresholdBytes`).

### Кэш кадров: работа вхолостую — [P2]

Ревью 2026-09-24 (был AK, п. 2): декод соседа, законченный после смены
выделения, выбрасывается — и тогда, когда выделение пришло на него же;
после паузы серии (`BurstDelayMs`) кэш не переспрашивается. Правка: готовый
декод — в кэш и при отмене; `TryGet` после паузы.

### B7-хвост. Подпись большого файла — [P1]

Ревью 2026-09-24: `WindowsExecutableInfo.Read` проверяет подпись
`WinVerifyTrust`, а тот хэширует файл целиком — на каждом выборе `.exe` /
`.msi` полное чтение на пуле, без паузы, очереди и отмены. Стрелками по
папке установщиков на 1–3 ГБ чтения идут параллельно, карточка последнего
ждёт от секунд до десятков секунд. Правка: версия, PE и значок — сразу;
подпись — вторым шагом, после паузы (как `FullSizeDwellMs`), по одной
(`SemaphoreSlim(1)`). Решение человека 2026-09-24: [крупный файл — с
кнопки]: выше 200 МБ — «подпись не проверялась (3,1 ГБ)» и кнопка
«Проверить»; начатую проверку не прервать, поэтому сама она не
запускается.

## Блок 5. Панель просмотра: скорость и форматы

Область кода одна — `PreviewController`, `PreviewPane`, `ImageDecoder`,
`PreviewRouter`, миниатюры. Сделанное — меры AK, AM, TGA, B7, B5, B6, Q5
— в DONE, «Панель просмотра» и «Галерея и оценки»; баги этих мер — блок 8.

### AK. Изображение открывается мгновенно — [P1, иссл.]

Столп 4 (CLAUDE.md): смена снимка выглядит мгновенной, без мельканий и
заглушек; ориентир — FastStone. Меры сделаны (DONE): прежний кадр стоит до
нового, JPEG — под размер панели, соседи — в кэше, серия нажатий не
декодирует то, что проскочила.

1. **Замер «до»** сделан 2026-09-24 (PERFORMANCE, «Замер 2026-09-24»): из
   335 смен снимка в лог попали 50 (≥ 33 мс), худшие 87–165 мс, JPEG 24 Мп
   и CR3 — до 145 мс; соседи греются; глазом — без замечаний. Осталось:
   повтор после правок кэша (блок 8) — стрелки человек на релизной сборке
   (`publish.ps1 -Run`: Claude ввод не эмулирует и её не запускает), лог —
   Фабле; `preview.shown` копит время, пока панель закрыта (1640 мс до
   `Ctrl+Q`, 32 с на полном экране), — считать от показа панели.
2. Подвисания галереи (BACKLOG «Производительность») — отдельный заход по
   описанному там порядку.

### Форматы и миниатюры

- **[P2] B10. heic системным кодеком.** В миниатюрах есть
  (`SystemIconProvider:1106`), в панели нет; WIC открывает при HEIF Image
  Extensions + HEVC Video Extensions (HEVC платный либо от производителя).
  Решение человека 2026-09-24: [в 0.5 — системный кодек и подсказка, какого
  расширения из Microsoft Store не хватает (столп 3); библиотека, webm и
  DjVu — в 0.6, «Поддержка отложенных типов»]. Фикстуры — BACKLOG.
- **[P2] AL. Миниатюры в архиве и автогалерея внутри.** Сейчас выключены
  решением; панель уже распаковывает запись во временную копию (одна за
  раз). Решение человека 2026-09-24: [в 0.5 — миниатюры записей и
  автогалерея внутри архива: zip обычно читается быстро; замеры потом].
  Источник — стенд Фабле: миниатюра shell-элемента записи (дёшево, если
  шелл её даёт) или распаковка + обычный путь (solid 7z / rar: одна запись
  стоит распаковки блока); бюджет: предел размера записи, только видимое,
  отмена по уходу; ключ `thumbs` — архив + запись + mtime архива.
- **[P2] B8-хвост. SVG в панели** (TGA сделан): панель знает `.svg` как
  код. Решение человека 2026-09-24: [рисовать — WebView2 с переключателем
  «картинка / исходник», без новых пакетов]; [миниатюра — как проще]:
  системная, если в Windows есть обработчик SVG (например, PowerToys),
  иначе значок; снимок из превью (`CoreWebView2.CapturePreviewAsync` при
  показе в панели → в кэш миниатюр) — если окажется просто. Растеризатор
  пакетом — 0.6.
- **[P1] B3.** Сверить футер для картинок и текста после переработок панели.
  Харнесс 2026-09-24 (`preview-formats`, скриншот `02-image-cr3`, панель
  ~280 px): кнопки хелперов и RAW забирают правую часть футера, подпись
  сжата в столбец ~70 px — имя рвётся посреди («IMG_0002.C» / «R3»), 10
  строк вместо 4, снимок ниже; на 0.4.1 (прогон 21.09) — 4 строки.
  Кандидат в блок 8.

### B6-хвост. Поиск в панели — [P1]

Поле поиска сделано (DONE). Решения человека 2026-09-24: [`Ctrl+3` — войти
в панель с клавиатуры, по ряду с `Ctrl+1` (панели папок) и `Ctrl+2`
(список); `Esc` — обратно в список; `Tab` в панель по-прежнему не входит];
[`F3` — следующее совпадение, после последнего — первое совпадение в
следующем файле выдачи поиска по содержимому]. [решить]: (1) подхват
запроса из поиска по содержимому — сейчас только в главной панели: нужен
ли во второй половине сплита и в «Сравнить»; (2) совпадения за первым
мегабайтом текста (200 000 знаков документа) не находятся: искать по всему
файлу (дольше, память) или оставить с пометкой «искали в первом
мегабайте». Проверить глазами: у Markdown, FB2, HTML, PDF свой поиск
WebView2 — перехватывает ли его `Ctrl+F` окна (пункт в QA, «Просмотр и
галерея»).

## Блок 6. Мелочи и сайт

### X. Вставка текста и картинки из буфера файлом — [P2]

`Ctrl+V` при тексте в буфере — `.txt`, при картинке (`CF_DIB` / `PNG`) —
PNG без настройки; имя «Текст.txt» / «Изображение.png» с `(1)`
(`UniqueNames`) и сразу в переименование (`RenameAdorner`); UTF-8 без BOM;
`SystemPathGuard`, лог, undo (файл в корзину), без подтверждения; в архиве
и корзине погашено; `ISystemClipboard` — пара «есть текст / есть картинка»,
байты читать в момент вставки; `ClipboardController` сегодня читает только
`CF_HDROP` и `Preferred DropEffect`. Решения человека 2026-09-24: [в буфере
несколько видов — вставляется один: файлы → текст → картинка; Excel кладёт и
текст, и картинку ячеек]; [невставленное — строкой в журнал и лог: «Не
вставлено: картинка — в буфере был и текст»; ничего подходящего — то же с
перечнем форматов буфера].

### AD1. WebView2: место и чистка — [P2]

`<DataRoot>\WebView2` — 42 МБ за два месяца: компоненты, которые процесс
браузера качает сам. (1) `AdditionalBrowserArguments` =
`--disable-component-update --disable-background-networking`,
`EnableTrackingPrevention = false` — стендом: свежий профиль, состав через
сутки; (2) место — за `AppSettings.UseSystemTemp` → `%TEMP%\Wander\WebView2`
(профиль одноразовый), действует после перезапуска, старая папка удаляется
на старте, как `TempFiles.Sweep`; (3) «очищать при выходе» — нет (1–2 с
холодного старта).

### AD2. Корзина: холодный заход — [P1]

Тёплая — ~0,4 с. **Не проверено:** первый заход после перезагрузки — на
`Shell.Application` стоил 6–7 с. Строка лога `Recycle bin: opened … first
row after … rows in …` скажет, где секунды: размазаны — отдавать порциями
(`ReconcileEntries`); все до первой строки — пересмотреть отказ от `$I`
(REJECTED). Быстро и холодным — закрыть.

### G6. Поле фильтра над списком — [P2; решено 2026-09-03: пробуем]

Поле из верхней панели окна (`SearchBox` в `MainWindow`) — в полосу над
списком (`GalleryStrip`), к звёздам и хелперам: оно про область файлов;
полоса тогда видна всегда. После блока 2 (полоса над списком — его
область). Решения человека 2026-09-24: [одно поле — одна строка ввода над
списком; окно поиска `Ctrl+Shift+F` — как есть]; [пока открыто окно
поиска, поле не прятать]. [решить] ширина — постоянная или тянется до
звёзд и хелперов. Оценка глазами. Рядом — идея «тип через `/`» (BACKLOG,
«Поиск: что доделать»).

### Архивы папками мимо чужой ассоциации — [P2, иссл.]

Решение человека 2026-09-24: [открывать папками все архивы, которые умеет
Windows, даже когда ассоциацию забрал WinRAR или 7-Zip]. Сейчас правило —
«папкой только то, что открывает оболочка» (`ShellArchiveFolder.
ReadExtensions`: `UserChoice` / класс расширения против списка ProgId
папок), и при WinRAR `.rar`, `.gz`, `.tgz`, `.tar` — просто файлы. Стенд
Фабле: создать обработчик архивов Windows по CLSID (`CompressedFolder` для
zip, обработчик libarchive для остальных) и открыть им файл мимо
ассоциации — `IPersistFolder` / `IInitializeWithItem`; отказ по формату —
как сейчас. Вход в архив — папкой, как у zip; пункты меню архиватора —
как были. Харнесс: шаги `archives` с `ifOpensAsFolder` перестанут
пропускаться сами.

### F2. Сайт: лендинг и гайд — [P2]

Решения человека 2026-09-22: [сайт русский]; [`GUIDE.md` остаётся
источником — общая техническая информация о возможностях; сайт формируется
из него]. 2026-09-24: [сайт — только по последнему релизу; руководства
прошлых версий — ссылками на GUIDE в гит-истории, `blob/vX.Y.Z/docs/
GUIDE.md`]. Отсюда: сайт собирается из тега релиза (`release.yml`), а не
пушем в master — GUIDE на master описывает дерево, а не выпуск (RELEASING,
«Руководство релиза»). С версией приложения сайт связан только ссылкой
«Справка» — может выйти и после 0.5.

Цель: лендинг на GitHub Pages — вводная, читаемая вёрстка, быстрая
загрузка, отдельные страницы гайда с оглавлением, «дальше / назад»,
якорями для ссылок из приложения; страница «Прошлые версии» со ссылками на
теги; скриншоты повторяемо (харнесс рендерит окно в PNG). Адрес —
`github.io`.

[решить] — напомнить человеку при старте блока (решение 2026-09-24: пока
не разбирал):

1. Генератор. (а, рекомендация) свой `tools/site/` — консоль на Markdig
   (уже зависимость App): режет GUIDE на страницы, оглавление и навигация
   из заголовков, лендинг — `docs/site/index.md` руками + скриншоты, шаблон
   HTML один, без JS; ноль внешних инструментов, вёрстка своя. (б) MkDocs
   Material — навигация и поиск из коробки, Python в CI, структура из
   `mkdocs.yml`, GUIDE всё равно резать на файлы. (в) голый `docs/` на Pages
   с Jekyll — дёшево, оглавление руками, вид по умолчанию бедный.
2. Нарезка. В GUIDE пять разделов `## `, «Что умеет» — ~740 строк из 936:
   по `## ` выйдет одна огромная страница. Резать по `### ` — или
   перестроить разделы GUIDE при его сжатии (блок 7), и сайт — после.
3. «Справка» из приложения (`CrashReporter.GuideUrl`: master для Debug,
   `blob/vX.Y.Z/` для релиза). На сайт с якорем раздела — удобнее, но
   пользователь старой версии прочтёт про новую; или GUIDE своей версии в
   гите, как сейчас, — точно, но без вёрстки сайта.

## Блок 7. Доки и релиз

- **Сжать под конец версии** (решение человека 2026-09-22, по одному
  файлу за заход, начиная с пары ARCHITECTURE ↔ DONE): ARCHITECTURE (185
  КБ), DONE (127), QA (118), GUIDE (78). Правило — факты, решения, имена;
  история — в CHANGELOG и гите.
- **Устаревшие ссылки**: QA.md → PLAN W1; ARCHITECTURE → PLAN AC, R2 / R3,
  поле `_manualViewModeFolders` (в коде `_manualViewModes`); проверить
  скриптом `PLAN [A-Z]{1,2}\d?` по докам перед релизом.
- **Анализаторы точечно** — решение человека 2026-09-24: [точечно, какие
  Claude считает весомыми]. Выбор — правила о корректности; пробная сборка
  2026-09-24 (`AnalysisLevel=latest-recommended`): `CA1310` — сравнение
  строк по культуре (16 мест, Core и Platform), `CA1806` — отброшенный
  результат (~7), `CA2016` — токен отмены не передан дальше (~6), `CA1001` —
  поле `IDisposable` у типа без `IDisposable` (~12), `CA2101` — маршалинг
  строк P/Invoke (1). Включить предупреждением в `.editorconfig` и поправить
  за один заход: правило с автоисправлением иначе покраснит `dotnet format
  --verify-no-changes`. Не берём: `CA1305` (~200 мест, в основном текст
  интерфейса, где культура пользователя верна; логи с запятой — TECHDEBT
  «SYS и PERF»), `CA1707`, `CA1861`, `CA1859`, `CA1863` — шум; `VSTHRD100` /
  `110` — нужен пакет анализаторов, пакеты — 0.6.
- **Win32-имена в Rider** [решить]: 118 предупреждений «Name does not match
  rule» — имена в объявлениях Windows API (`CopyFileExW`, `dwFlags`,
  `COPY_FILE_FAIL_IF_EXISTS`) не по правилам именования C#. Переименовывать
  нельзя: имена должны совпадать с документацией Windows. (а) Не трогать —
  PRERELEASE их и так пропускает, но они засоряют список инспекций; (б)
  `// ReSharper disable InconsistentNaming` в файлах с P/Invoke — список
  чистый, поведение не меняется. Предложение — (б).
- **Рабочий набор под харнессом** +10–15 % против 0.3.1: прогон 2026-09-24
  дал пик вдвое ниже 0.4.1 (200–430 МБ против 490–680) — закрыть после
  сверки с релизным прогоном на подготовке.
- PRERELEASE.md целиком; `check.bat qa` и полный набор харнесса по слову
  «готовимся к релизу». Старт single-file на чистой машине (QA, «Сборка»)
  в 0.5 не проверяется: Windows Sandbox — 0.6 (решение 2026-09-24).

## Версия 0.6 — что уже решено туда

Решения человека 2026-09-24. Блоки 0.6 и их порядок складываются после
релиза 0.5 (WORKFLOW, «После релиза»); здесь — только то, что уже туда
отправлено.

### Надёжное копирование (был блок 4 версии 0.5)

Спека — [OPERATIONS.md](OPERATIONS.md): готова, сверена с кодом
2026-09-24 (после финализации блока 2 сверить шаги 1, 5, 7 ещё раз).
Решения человека 2026-09-22: [цель — съёмный том с буквой диска,
приоритет надёжность]; [MTP / PTP — вторая очередь]; [завершённые операции
живут в строке состояния до «убрать», проблемные ждут решения]; [журнал —
сеанса]. 2026-09-24: [перенесено из 0.5 — версия и так плотная].

#### K. Надёжное копирование — [P1]

Запись операции с итогом по элементам (`OperationRecord`,
`IOperationHandle.Finish`), завершённые в строке состояния с «Повторить» /
«Убрать» (K2), повтор при временных ошибках ввода-вывода (`TransientIo`,
`IoRetry`, 3 попытки), понятные сообщения по кодам (место, FAT32 4 ГБ,
устройство пропало), временное имя `.wander-tmp` при копировании с
переименованием в конце, компьютер не засыпает (`IPowerGuard`,
`PowerCreateRequest`), съёмные диски по `WM_DEVICECHANGE` (Y2), `Ctrl+Z`
ждёт занятых, страховка корзины, имя занятого файла в папке (AF), хвосты
TECHDEBT. Стенд — «Отладка» → «Операция» (AI1, DONE).

#### AD5. Журнал — [P1]

Структурная запись (`JournalEntry`, `JournalKind`): заголовок «Скопировано
в E:\Video — 12 файлов, 12,3 ГБ, 5 мин 27 с» и строка на элемент,
переименование «было → стало», открытие и запуск файлов; строка итога в
технический лог (элементы, байты, секунды, МБ/с); строки отката без
английского `Description`. Журнал сеанса, окно — как сейчас.

#### Y2. Съёмные диски — [P1]

`IVolumeChanges` (Core) ← `VolumeChangeHub` (Platform, разбор
`DEV_BROADCAST_VOLUME`) ← хук `HwndSource` в `MainWindow`; панель
«Компьютер» перечитывает верхний уровень (модель блока 2), закладка на
ушедший том сереет, открытая папка на нём уходит по
`NavigationFallback.AfterVolumeRemoved`. «Извлечь» — стенд: даёт ли шелл
глагол для `E:\`.

#### Вторая очередь

BACKLOG, «Копирование — вторая очередь»: MTP / PTP, свой движок с хэшем и
проверкой, продолжение после вылета, пауза при пропаже устройства,
robocopy / небуферизованный ввод-вывод, таймауты UNC, `RestoreMany`.

### Поддержка отложенных типов

Решение человека 2026-09-24: [крупный блок 0.6 — всё, что не открывается
просто: FBX, документы, картинки]; [решение о новых пакетах — здесь, в 0.5
их не вводим]. По каждому кандидату сначала — нужен ли пакет (вес, чужой
нативный код, лицензия), потом формат:

- картинки: heic библиотекой (`LibHeifSharp` + `LibHeif.Native.win-x64`:
  нативный пакет 5 МБ в сжатом виде — около +7 % к exe, последняя его
  сборка — март 2023; libheif и libde265 под LGPL, HEVC — под патентным
  пулом; в 0.5 — системный кодек, B10); DjVu (обвязка над `djvulibre`, порт
  `DjvuNet`, разбор первой страницы — или системный обработчик просмотра,
  B9; миниатюры приходят сами, если в Windows стоит обработчик DjVu);
  миниатюра SVG растеризатором (`Svg.Skia` / `SharpVectors`);
- видео: webm — стенд: играет ли `MediaPlayer` с Web Media Extensions
  (VP9); нет — плеер WebView2 или «не поддерживается» с подсказкой;
- документы: `.doc` / `.xls` / `.ppt`, CHM, `.msg` — системные обработчики
  (B9, «Обсуждения»); PDF без `IFilter` — PdfPig (G4, BACKLOG);
- 3D: FBX (свой парсер или AssimpNet — BACKLOG «Панель просмотра»), PLY,
  3MF, COLLADA; текстуры моделей.

### T. Проверки вне рабочей машины

Решения человека 2026-09-24: [нужно, в 0.6: Windows Sandbox; как включать
и настраивать — разбирается человек; в 0.5 не трогаем]; [отдельное окно
допустимо, если не перехватывает ввод и фокус].

Что туда: стенд корзины на границах (длинные пути внутри `$Recycle.Bin` —
системный вопрос с «Да» по умолчанию, то есть удаление безвозвратно; папка
больше лимита корзины; порог длинного пути — TECHDEBT); старт single-file
на чистой Windows без .NET (QA, «Сборка»); позже — харнесс без риска
мелькнуть окном у человека, UIA (FlaUI) на релизном exe (настоящий ввод и
drag & drop с Проводником), `ConflictWindow` в харнессе.

Как держится изоляция: гипервизор и файл `.wsb`. Внутри — своя
одноразовая ФС; с хостом общее только подключённые папки (источники —
`<ReadOnly>true</ReadOnly>`, результаты — одна папка `artifacts\sandbox`);
сеть и общий буфер выключаются там же. Ввод внутри — свой сеанс: мышь и
клавиатура хоста не трогаются; окно при запуске выходит вперёд, дальше
живёт как обычное — проверить, идёт ли автоматизация в свёрнутом окне.
Запускает человек, Claude готовит `.wsb` и стенд. Другие варианты:
Hyper-V со снапшотами (обслуживать ВМ), CI `windows-latest` (окно WPF и
шелл в неинтерактивной сессии, корзина не та), отдельная машина.

### Прочее

- **Q6. Внешнее сравнение** — решит человек в 0.6. Шаблон команды в
  настройках с автоопределением (VS Code → WinMerge → `git difftool
  --no-index`), «открыть оба» — когда ничего не нашлось; картинки — кнопка
  ведёт в «Сравнить». Открыто: «Оставить оба» подставляет `(1)` без
  диалога имени (TECHDEBT); записи слитой папки внутри архива —
  спрашивать по одной или разом.
- **Ключевые слова из XMP при конвертации RAW → JPEG** — вшитый XMP / IPTC
  через `XmpDirectory` или сайдкар; нужен RAW со словами в фикстурах.
- **Анализаторы `VSTHRD100` / `110`** — пакет
  `Microsoft.VisualStudio.Threading.Analyzers`, вместе с решением о пакетах.

## Обсуждения

Кода не получают, пока нет решения. Кандидаты для сравнения (объём: малый
— дни, средний — неделя, крупный — больше):

| Кандидат | Что даёт | Объём | Риск |
|---|---|---|---|
| **Тёмная тема** (Roadmap) | витрина релиза | средний: второй набор ключей `Palette.xaml`, `GalleryPalette`, тема AvalonEdit, CSS Markdown / HTML, `AppsUseLightTheme` + `UserPreferenceChanged` | каждый XAML; скриншоты харнесса меняются |
| **AA синхронизация, фаза 1** | архив «источник → копия» одним окном | крупный | удаления в копии; деструктивная фаза — после релиза |
| **G5 результаты поиска узлами панели** | выдача переживает навигацию; закрывает пять хвостов (BACKLOG) | крупный | средний |
| **B9 системные обработчики** | doc / xls / ppt / chm / djvu | крупный | чужой COM, `HwndHost`, фокус |
| **M переназначение хоткеев** | для живущих на клавиатуре | крупный: три места разбора | средний |

### Y3. «Сеть» и FTP

Узел «Сеть» через `IShellNamespace` (ветка корзины и архивов);
«подключить сетевой диск» — `WNetConnectionDialog`; закладка на
`\\server\share` работает сегодня. FTP: тот же namespace на `ftp://` без TLS
либо свой клиент (`FluentFTP`, второй `IFileSystem`). Решить: сценарий и
стоит ли он больше, чем закладка на UNC и WinSCP рядом.

### B9. Плагины / системные обработчики для тяжёлых форматов

Системные `IPreviewHandler` / `IThumbnailProvider` — плагины, которые пишут
другие (SumatraPDF, LibreOffice, 7-Zip); столп интеграции за, цена —
`HwndHost`, чужой COM в процессе. Свой формат плагинов — Roadmap.
Предложение: сначала системные обработчики.

### AA. Синхронизация папок

Фаза 1 — односторонний апдейт (источник → копия) окном конфликтов в
режиме «план синхронизации»: новые, изменённые, удалить в копии (в
корзину); фаза 2 — двусторонний синк со снимком прошлого синка в data-dir.
Рядом есть `MergeScanner`, `ConflictVerdict`, `FileContentComparer`,
`ConflictWindow`, `BatchExecutor`. Решить: перенос и переименование без
индекса; «новее» на FAT и при переводе часов; точка входа. Сосед — BACKLOG
«оставить обе папки».

### M. Переназначение горячих клавиш — [P2]

Жесты в трёх местах — `MainWindow.xaml` `InputBindings`, `MainWindow.xaml.cs`,
`FileListView.xaml.cs`; нужен один реестр: идентификаторы команд, жест ↔
текст с раскладко-независимостью, контекст, конфликты, в `AppSettings`
только отличия. Пока — `HotkeyCatalog` менять вместе с местом обработки.

### N. Чек-лист в приложении

Живёт в приложении, отмечает сам там, где действие видно; список — файл,
достижимый Claude и видимый приложению, не в `docs/`. Открыто: источник
событий, место файла, только отладочная сборка или любая, как не
задублировать FINALIZING / RELEASING. Сосед — меню отладки (AI).

### Поиск и фильтры — P3

- **G3-хвост.** Фильтры по дате, размеру и regex — форма диапазонов, общая с
  J3 (ISO и выдержка; EXIF — кэш путь + mtime рядом с `thumbs`).
- **G4.** PDF без системного `IFilter` — честно «не прочитан»; вариант с
  зависимостью — BACKLOG.
- **I5.** Спутники: какие форматы ещё — BACKLOG «Файлы-спутники».

## Чистки

Code health pass между вехами, без фич; повод — фича, изменившая публичный
API Core. Правила и роли — WORKFLOW, «Техчистка»; пройденные #1–#4 — DONE.
Остатки #4: файловые операции в `MainViewModel` (O4.6; «Надёжное копирование», 0.6, даёт
теме своё состояние — запись операции); `SystemIconProvider` 1419 (PInvoke без
тестов — худший размен); тесты на `NavigationController` — с
`IDispatcher`; `Wander.Core/FileSystem/` (27 файлов) — не резать. После
блока 2 (2026-09-23): `MainViewModel.cs` 5683 строки и `MainWindow.xaml.cs`
1642 — выросли на разводку модели окна (проекции, причины фокуса,
исполнение эффектов), кандидаты на вынос в `Controllers/`;
`FileListView.xaml.cs` 1705, `FolderTreesView.xaml.cs` 1156 — жесты мыши и
редактор имени.

## Направления (P3)

- Hex view в панели просмотра.
- Symlinks / junctions / hard links (сейчас только `.lnk`) — Roadmap.
- Тёмная тема — Roadmap; сравнение — таблица выше.
