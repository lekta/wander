# Wander — план

Список **обязательных целей** проекта и их детализация — что делаем и
в каком порядке.

Здесь только **несделанное**. Всё, что уже работает, живёт в
[DONE.md](DONE.md) и описано там как «как оно устроено сейчас», а не как его
делали; закрытый пункт уезжает туда целиком, а не остаётся тут зачёркнутым.

В отличие от соседей:

- [`DONE.md`](DONE.md) — снапшот того, что уже работает.
- [`REJECTED.md`](REJECTED.md) — что обсудили и решили не брать, с причиной.
- [`TECHDEBT.md`](TECHDEBT.md) — мелкая шероховатость (дальние чистки).
- [`BACKLOG.md`](BACKLOG.md) — конкретные второстепенные задачи (полезное, но не сейчас).
- [`CLAUDE.md`](../CLAUDE.md) Roadmap — стратегические направления продукта.
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — как устроен код и механизмы.

---

## Столпы проекта

1. **UX** — убрать лишнее из Win11 Explorer, починить его баги, добавить удобств.
2. **Надёжность** — деструктивные операции всегда требуют подтверждения с Cancel
   по-умолчанию; явные ошибки вместо тихих сбоев; обратимость через `Ctrl+Z`.
3. **Интеграция с системой** — по-умолчанию выбираем вариант, при котором обмен
   с системой и другими приложениями работает (буфер обмена, drag & drop,
   ассоциации, хоткеи). Своя изолированная реализация — осознанная уступка,
   которая выносится на обсуждение и записывается в
   [BACKLOG.md](BACKLOG.md), а не принимается молча.

---

## Приоритеты

| | Что значит | Когда брать |
|---|---|---|
| **P0** | Багфиксы, незавершённое из готового, блокеры | Прямо сейчас, до новых фич |
| **P1** | Обязательное для proper file-manager UX | Следующая итерация |
| **P2** | Желательное, заметное улучшение | После P1 |
| **P3** | Опциональное / нишевое | Когда дойдём |

---

## Пройденные спринты

Что именно в них сделано — в [DONE.md](DONE.md), что менялось от версии
к версии — в [CHANGELOG.md](CHANGELOG.md).

| | Спринт | Итог |
|---|---|---|
| 1 | Багфиксы + связать готовое | `Ctrl+Z`, `Shift+Delete`, файловый лог, баги drag & drop |
| 2 | Async-операции | `OperationTracker`, batch-операции, разрешение конфликтов с UI-потока |
| 3 | Техническая чистка #1 + добить P1 | `BatchExecutor`, контроллеры вместо толстой VM, прогресс, сортировка, поиск, закладки, корзина |
| 4 | Добить P2 | Переименование в строке, папка в панели просмотра, кэш миниатюр, восстановление из корзины, русский интерфейс |
| 6 | Ворох визуальных проблем | Левая панель, виртуализация плиточных режимов, замеры производительности, чистка #3 (частично) |
| 5 | Буфер, миниатюры, размеры видов | Системный буфер обмена, миниатюры в плитках, размеры у каждого вида + `Ctrl` + колесо, авто-обновление списка, сортировка кликом по заголовку, набор имени, сводка по выделению |

Спринт 6 взят вне очереди — накопился список того, что видно глазом каждый
день; спринт 5 закрыт после него.

---

## Ближайший фокус

### Спринт 7 (текущий)

Ничего не выбрано — состав собирается из списка «Обязательные цели» ниже
и из [BACKLOG.md](BACKLOG.md). Кандидаты по убыванию готовности:

1. **B3** — сверить футер панели просмотра для картинок и текста. Мелочь,
   скорее всего один прогон глазами.
2. **J. Режим галереи** — самый крупный незакрытый P2, и всё, на что он
   опирался (companion-файлы, кэш миниатюр, виртуализация), уже есть.
3. **Чистка #4** — условие наступит, когда появится следующий заход
   в публичный API Core.
4. **I5** — какие ещё форматы спутников поддерживать; нужен разговор,
   не реализация.

Из хвостов спринта 5, которые стоит подобрать заодно: вырезанные файлы не
выглядят вырезанными (в Проводнике они полупрозрачные до вставки) — запись
в [TECHDEBT.md](TECHDEBT.md), данные для этого уже есть.

---

## Обязательные цели

### A. Вёрстка / UI

Закрыто целиком: A1 (размеры у каждого вида + `Ctrl` + колесо), A2, A3
(переименование в строке), A4, A5 (меню сортировки), A6, A7 / A8
(миниатюры в плиточных видах), A9, A10 (рамка выделения), A11. Всё — в
[DONE.md](DONE.md).

### B. Панель просмотра

- **[P1] B3. Сверить общую информацию под содержимым.** Футер собирается
  для всех типов, но для картинок и текста его не перепроверяли после
  переработок панели. Скорее всего обернётся в один прогон глазами.

Остальное закрыто: B1 (маленькие картинки не растягиваются), B2 (перепись
папки вместо заглушки).

### C. Выделение — закрыто (см. A9, A10).

### D. Закладки

- **[P3] D6. Перетаскивание закладок между собой.** Сейчас порядок = порядок
  добавления, изменить можно только правкой `state.json`. Делать при первом
  запросе.

Остальное закрыто: панель закладок, спец-папки, корзина внутри Wander с
восстановлением, source-aware разворот дерева.

### E. Async-операции

- **[P2] E2-доработка. Спрятать прогресс в статус-бар.** Сейчас
  `ProgressDialog` модальный: пока идёт копирование, окно занято. Аггрегатный
  индикатор в статус-баре уже есть — не хватает кнопки «свернуть» в диалоге и
  возврата из статус-бара обратно.

Остальное закрыто: batch-операции с прогрессом и отменой, `ProgressDialog`,
двухуровневый кэш миниатюр, guard на undo во время операции.

### F. Настройки

Закрыто: всё в `state.json` с разделением на `Session` / `Favorites` /
`Window` / `Settings`, диалог с категориями, сброс к значениям
по-умолчанию. Новые категории заводятся под реальные настройки, а не
заранее: пустая страница «Тема» до самой тёмной темы — это обещание,
которое доку и интерфейсу приходится нести годами.

### G. Поиск

- **[P3] G3. Расширенный поиск.** Значок ⋮ в поле поиска → панель: рекурсивно
  по подпапкам, фильтр по типу / расширению, по дате и размеру, regex.
  Скоуп уточняем отдельно.

Базовый фильтр по имени (`Ctrl+F`) закрыт.

### H. Спец-возможности — [P3]

- **H1. desktop.ini.** При смене вида в папке — опционально писать в
  `desktop.ini`. Сначала решить: учитывать чужой, писать свой, или оба.

H2 (Unity `.meta`) поглощён секцией **I**.

---

### I. Companion-файлы

Общий механизм «файл + его спутник(и) = одна сущность». Как это работает
сейчас — в [DONE.md](DONE.md) и [ARCHITECTURE.md](ARCHITECTURE.md); правила
живут в `Wander.Core/Companions/`, поддержаны `.meta`, `.pp3` и `.xmp`.

Открыто:

- **I2-хвост.** Если спутник упал уже после того, как основной файл переехал,
  группа помечается Failed, а успевшие члены остаются на месте. `Ctrl+Z`
  чинит, но автоматического отката середины группы (как в `RenameMany`) тут
  нет.
- **I4-хвост. Создание сайдкара по явному согласию.** Сейчас, если `.pp3` /
  `.xmp` рядом нет, ряд оценок просто не показывается — Wander не создаёт
  файлы, о которых пользователь не просил.
- **I5. Какие ещё форматы поддержать** — [обсудить]. Механизм расширяется
  одной строкой в наборе правил, но ни один кандидат пока не выглядит
  нужным. Список кандидатов и причина откладывания — в
  [BACKLOG.md](BACKLOG.md).

---

### J. Режим галереи — [P2]

Самый крупный незакрытый P2. Всё, на что он опирается, уже есть.

#### J1. Автовключение

Если большая часть файлов в папке — графика, режим галереи включается сам.
Нужно определить: порог (доля? абсолютное число?), что считать графикой
(включая RAW), и — обязательно — **не перебивать явный выбор пользователя**.
Если он вручную переключил вид в этой папке, автоматика молчит.

#### J2. Переключатель день/ночь

Значок в тулбаре, меняет фон области просмотра на тёмный. Для разглядывания
фотографий светлый фон мешает.

Прицел на будущее: это фактически первый кусок тёмной темы (Roadmap).
Стоит сразу заводить как переключение палитры области контента, а не как
разовый хак с одним цветом — иначе потом переделывать.

#### J3. Фильтр по свойствам снимка

Фильтрация по **ISO, выдержке, рейтингу**. Вероятно, разворачивается со
стороны панели быстрого просмотра — там уже читается EXIF.

Технические зависимости, которые надо учесть заранее:

- Сейчас EXIF читается **лениво, для выбранного файла**. Фильтр по ISO
  требует прочитать метаданные **всей папки** — это I/O по сотням файлов.
  Нужен фоновый проход с прогрессом и кэшем, иначе UI встанет.
- Рейтинг живёт либо в EXIF/XMP, либо в сайдкаре (`.pp3` `Rank`) — то есть
  **J3 опирается на секцию I**. Порядок: сначала I, потом J3.

---

### K. Надёжное копирование больших файлов — [P2/P3, исследование]

Заменить/дополнить текущий `System.IO`-путь внешним движком для тяжёлых
случаев: копирование десятков гигабайт, нестабильная сеть, съёмные диски.

**Кандидаты:**

- **robocopy** — встроен в Windows, ставить нечего. Даёт retry
  (`/R` / `/W`), restartable mode (`/Z` — продолжает с места обрыва),
  многопоточность (`/MT`), длинные пути, внятные коды возврата.
- **FastCopy** (открытые исходники) — интересен не как зависимость, а как
  референс: свои буферы и обход системного кэша, чтобы копирование не
  вытесняло всё остальное из памяти. Посмотреть подход, не тащить код
  (лицензию проверить отдельно).
- **certutil** — копировать не умеет, это хешер (`certutil -hashfile`).
  Его место — **вторая половина задачи: verify-after-copy**. Скопировали →
  посчитали хеш источника и приёмника → сверили. Для «надёжно» это как раз
  недостающий кусок; для больших файлов считать хеш дорого, так что нужен
  флаг, а не всегда.

**Что придётся решить — это и есть основная работа, а не выбор утилиты:**

- **Слои.** Внешний процесс — платформенная деталь, значит реализация в
  `Wander.Platform.Windows` за существующей абстракцией. В Core не должно
  протечь ни слова про robocopy.
- **Прогресс.** `OperationTracker` ждёт per-item шаги, robocopy печатает
  свой прогресс в stdout. Либо парсить вывод, либо смириться на неопределённый
  прогресс для внешнего пути.
- **Отмена.** Сейчас `CancellationToken` проверяется между элементами. С
  внешним процессом отмена = убить процесс, и надо решить, что делать с
  недокопированным хвостом (robocopy `/Z` позволяет продолжить).
- **Undo.** Копирование само по себе откатывается удалением приёмника — но
  надо убедиться, что undo-запись формируется и для внешнего пути тоже.
  Операция без undo в UI не выпускается.
- **Когда включать.** Всегда? По порогу размера? Флагом в настройках?
  Гонять отдельный процесс ради копирования мелочи — лишние накладные
  расходы.
- **Ошибки.** Коды возврата robocopy нетривиальны: 0–7 это успех разной
  степени, 8+ — реальные сбои. Замапить в человеческие сообщения.

---

### L. Контекстное меню — сделано

Меню как в Windows 10: весь список сразу, без «Показать дополнительные
параметры». Что именно получилось — в [DONE.md](DONE.md), механизм и его
цена — в [ARCHITECTURE.md](ARCHITECTURE.md).

Осталось — [P3]:

- Асинхронный запрос чужого меню для первого клика — см. TECHDEBT.
- Owner-drawn пункты расширений не читаются — см. TECHDEBT.
- Поднятие отдельных пунктов расширения на верхний уровень — в BACKLOG.

---

## Технические чистки

Между крупными вехами проекта останавливаемся и спрашиваем: «а структуру кода
надо ли освежить?». Это не TECHDEBT (там мелочёвка по ходу) и не BACKLOG (там
конкретные фичи). Это **code health pass** — ревизия структуры, тестов и
документации без новых пользовательских фич.

**Что смотрим на каждом проходе:**

- Не пора ли **разделить класс**? Признаки: длинный файл (>500 строк), несколько
  смешанных ответственностей, тесты на разные сценарии живут в одном файле.
- Не пора ли **слить два класса**? Признаки: один всегда таскает второй, второй
  без первого не используется, проектные зависимости циклические.
- **Public API**: какие методы / типы перестали быть нужны снаружи и могут уйти
  во `internal`.
- **Тесты**: что покрыто, что нет, нет ли мёртвых fakes.
- **Doc-комментарии**: появились ли публичные типы без XML-doc.
- **`TECHDEBT.md`**: пора ли выбрать пакет пунктов на разовое закрытие.

**Когда проводить:**

Не по календарю, а по большим вехам. Грубый ориентир: после фичи, которая
**меняет публичный API Core** (новые async-overload-ы, новый интерфейс,
изменение сигнатур service-классов).

### Пройденные проходы

- **Чистка #1 — после E (async-операций).** `BatchExecutor` забрал batch-логику,
  `FileOperationService` стал тонким фасадом; VM разгрузили в `PreviewController`
  и `SelectionController`; геометрия окна собралась в подrecord. Покрытие
  выросло до 98 тестов.
- **Чистка #2 — после D + G + Settings.** `ClipboardController`,
  `SearchController`, `NavigationController`, подrecord `SessionState`.
  MainViewModel ужался с ~1380 до ~1230 строк, покрытие 104 → 126.
- **Чистка #3 — после спринтов 4–5.** `MainWindow.xaml.cs` 2129 → ~1050 строк:
  список файлов и панель просмотра стали отдельными контролами, жесты —
  отдельными классами, а приём drop'а — `Util/DropTargetController`.
  Одиночные операции `FileOperationService` ушли в `internal`.

### Чистка #3 — что осталось

- **Жесты дерева всё ещё в окне** (~200 строк: раскрытие по клику,
  drag узла, правый клик как цель операции, `Shift` + колесо). Выносить
  вместе с превращением деревьев в отдельный контрол — сейчас они
  описаны прямо в `MainWindow.xaml`.
- **Пересмотреть `TreeNodeViewModel`** — он накопил bookmark-флаги,
  drives-флаги, lazy-load semantics. Возможно, `BookmarkNodeViewModel`
  отдельно.
- **Тесты на `NavigationController`** — сейчас покрытие идёт через
  `NavigationService` напрямую; обёрточный контроллер с командами и
  `AddressText` стоит покрыть, когда добавим `IDispatcher`-абстракцию.

### Чистка #4 — когда и что

Условие то же: следующий заход, который меняет публичный API Core. Ближайший
кандидат — секция **J** (галерея потребует фонового чтения EXIF по всей папке,
то есть новой абстракции и нового кэша).

Первый пункт повестки известен уже сейчас: **`MainViewModel` — 2433 строки**.
Чистка #2 ужала его до ~1230, после чего он снова вырос вдвое, и по
собственному признаку из списка выше (>500 строк, несколько смешанных
ответственностей) это самый крупный кандидат на разделение в проекте.
Что в нём лежит отдельными темами и просится наружу:

- закладки (построение, добавление, удаление, персист) — ~200 строк;
- корзина (навигация в shell-namespace, восстановление, гейты команд);
- диалоги подтверждений и отчёты о результатах batch-операций;
- слежение за папкой и троттл авто-обновления.

Разделять не раньше, чем появится вторая причина трогать файл: сам по себе
размер — повод записать, а не повод переписывать.

---

## Активные направления (P3 / обсуждается)

- Расширение панели просмотра: zip-архивы, hex view.
- Symlinks / junctions / hard links — отдельная фича (сейчас только `.lnk`).
- Темы (светлая / тёмная) — см. Roadmap в [CLAUDE.md](../CLAUDE.md).

---

## Связанные файлы

- [CLAUDE.md](../CLAUDE.md) — точка входа, навигация по всем докам, правила.
- [ARCHITECTURE.md](ARCHITECTURE.md) — как устроен код и механизмы.
- [DONE.md](DONE.md) — что уже реализовано.
- [REJECTED.md](REJECTED.md) — что решили не делать и почему.
- [TECHDEBT.md](TECHDEBT.md) — мелкие чистки.
- [BACKLOG.md](BACKLOG.md) — отложенные задачи.
- [README.md](../README.md) — описание для пользователя, сборка, лицензия.
- [.editorconfig](../.editorconfig) — codestyle.
