# Wander — план

Только несделанное. Закрытое и отклонённое удаляется при финализации, не
помечается: как работает — DONE, почему так — ARCHITECTURE, не делаем —
REJECTED, спека — в гите (FINALIZING.md, п. 4). Буквы секций стабильны — на
них ссылаются другие доки.

| | Приоритет | Когда |
|---|---|---|
| **P0** | Багфиксы, незавершённое из готового, блокеры | Сейчас, до фич |
| **P1** | Обязательное для нормального файл-менеджера | Следующая итерация |
| **P2** | Заметное улучшение | После P1 |
| **P3** | Опциональное / нишевое | Когда дойдём |

Пометки: **[иссл.]** — начинается с исследования или стенда; **[решить]** —
ждёт решения человека, кода не получает; без пометки — спека есть или
пишется перед запуском.

## Версия 0.5.0 — блоки

Решение человека 2026-09-22: блоков 5–7, каждый по одному флоу — Фабле
пишет спеку сразу с местами, именами и связями (большая — отдельным файлом
в `docs/`, как REDESIGN.md), Опус реализует блок целиком, Фабле ревьюит,
дочищает, финализирует (WORKFLOW, «Фича: четыре этапа»). Исключение —
блок 2: ведёт Фабле от начала до конца, человек вовлечён по каждому шагу.

| # | Блок | Состав | Спека | Объём |
|---|---|---|---|---|
| 2 | **Панели, папки, список — переработка** | AN модель состояния и правил; AG чей вид — папки или общий; Z1 база параметров папки; H1; долги 0.4.x по списку и сторожу; AD3-хвост; U1 / U2; AB; J4; TECHDEBT «Список файлов и навигация», «Дерево и закладки» | [REDESIGN.md](REDESIGN.md) — сбор; спека по шагам, Фабле ведёт | крупный |
| 3 | **Сборка и отладка** | AH номер сборки; AI1 отладочная операция; AI2 отладочная занятость действием; AD11 — отложен, из него сделана форма `state.json` | ниже, готова | малый |
| 4 | **Операции: надёжное копирование** | K запись операции, повтор, временные ошибки, временное имя, «не спать»; K2 завершённые в строке состояния; AD5 журнал; Y2 съёмные диски; `Ctrl+Z` ждёт занятых; корзина — страховка; AF; I2; хвосты TECHDEBT | [OPERATIONS.md](OPERATIONS.md) — готова | крупный |
| 5 | **Панель просмотра: скорость и форматы** | AK «как FastStone»; AM резкость и масштаб; B10 webm, heic; AL миниатюры в архиве; B8 SVG, TGA; B7 exe; B5, B4; B3; B6 `Ctrl+F` в панели; Q5 «Сравнить» и полный экран | ниже; AK [иссл.] | крупный, по пунктам |
| 6 | **Мелочи и сайт** | X вставка из буфера файлом; AD1 WebView2; AD2 корзина — холодный заход; G6 поле фильтра над списком; Q6 [решить]; F2 сайт на Pages | ниже | средний |
| 7 | **Доки и релиз** | сжатие ARCHITECTURE, DONE, QA, GUIDE; устаревшие ссылки; вопросы, отложенные с 0.4.0; PRERELEASE | ниже | малый, без кода |
| — | **Обсуждения** | Y3 сеть и FTP; B9 системные обработчики; AA синхронизация; M хоткеи; N чек-лист; T автотесты вне машины; G5; тёмная тема | | |

Порядок — по номерам: 2 — самое больное; 3 — стенд для 4; 5–7 — по темам.
Между блоками — финализация и `check.bat run`. Блок 1 закрыт 2026-09-22
(DONE, «Хелперы отсмотра снимков»).

## Блок 2. Панели, папки, список — переработка

Ведёт Фабле; человек — по каждому шагу (решение 2026-09-22: «простые баги
по часу искали — код должен стать поддерживаемым, поведение — под тесты
Core, харнесс только для того, что кодом не проверить»). Сбор, диагноз и
целевая схема — [REDESIGN.md](REDESIGN.md); спека пишется по шагам её
§ 7, каждый шаг — свой коммит с зелёным `check.bat`.

### AN. Поведение панелей и выделения — в логику, за тесты — [P1, иссл.]

Всё, что решает, какая строка подсвечена в панелях, что выделено в списке,
о чём следующая операция и куда идёт клавиатура после операции, живёт в
Core как состояние и правила — событие с причиной → новое состояние +
эффекты — и тестируется без окна; вью только переводят ввод в события и
отражают состояние. Порядок (REDESIGN § 7): трасса и воспроизведение
«слетания выделения» → явный `Target` в VM → `WorkspaceState` +
`WorkspaceReducer` с тестами по § 4.7 рядом со старым кодом → перевод
`FolderTreesView` на модель, адаптер причуд WPF → меню с `MenuContext` →
список (`ReconcileEntries` / `SettleDeparture` / `ApplyArrival` →
`ListingArrival` + `KeyboardRules`) → харнесс `post` / `assert-state`,
снять контрольные строки, ARCHITECTURE, DONE, удалить REDESIGN.md.
Открытые вопросы § 6 закрываются стендом на одном `TreeView` до шага 4.

### AG. Чей это вид — папки или общий — [P1, решить при спеке]

Наблюдение человека (2026-09-21): сменил вид в одной папке — сменился и в
других; непонятно, к чему относится меню «Вид».

Как сейчас (`MainViewModel.SetViewMode:3278`, `AutoSelectViewMode:3328`,
`RememberManualViewMode:3292`): ручной выбор пишется в два места сразу —
общий «последний выбор» (`_userViewMode`, `SessionState.ViewMode`) и
закрепление за папкой (`_manualViewModes`, 128 записей, вытеснение по
порядку вставки). На входе в папку: закрепление → оно; иначе автогалерея
(`ImageFolderProbe`, порог `AutoGalleryPercent`) → «Галерея», нигде не
запоминается; иначе — общий последний выбор. Два подхода смешаны.

Наклон человека (2026-09-22): **вид выбирается для каждой папки
автоматически; изменил руками — только для этой папки; поведение и вид по
умолчанию — в настройках; меню «Вид» показывает, к чему относится** (так у
Проводника). Спека шага:

- `ViewChoice` в Core (`Listing/`): факты — закрепление папки, признак
  «папка со снимками», умолчание из настроек → решение; тесты. Заменяет
  `AutoSelectViewMode`.
- `AppSettings.DefaultViewMode` (умолчание для папок без закрепления и без
  автогалереи; сегодня это «последний ручной»), `AutoGallery` /
  `AutoGalleryPercent` — как есть; `SessionState.ViewMode` уходит.
- Меню «Вид» (`MainWindow.xaml:210-240`): заголовок группы «Эта папка»,
  четыре вида с галочкой, ниже «Автоматически» (снять закрепление) и
  «Сделать видом по умолчанию» (пишет в настройки); подпись в подсказке,
  какой вид сейчас и почему («закреплён» / «авто: снимки» / «по
  умолчанию»). Хоткеи `Ctrl+Shift+1/2/6/7` — закрепление за папкой.
- Наследование подпапками — нет [решить]; Проводник шаблоны наследует,
  Directory Opus — по пути; проще без наследования, пока не попросят.
- Хранение закрепления — Z1 (потолок 128 → тысячи, ключ с датой создания).

### Z1. Параметры конкретной папки — [P2; решено: своя база в data-dir]

Сегодня единственный параметр папки — вид (`ManualViewModes` в
`state.json`, потолок 128). Решено (в): своя база в data-dir — ничего в
чужих папках, работает на read-only и в архивах. Детали [до кода]: ключ
«путь + дата создания папки» (переименование внутри тома ловится по
`CreationTime`), LRU по дате обращения с потолком в тысячи, чистка при
старте в фоне, запись с версией; `ManualViewModes` переезжает первым;
следом кандидаты — сортировка, фон галереи, размер плиток. Остальные
варианты: (а) `desktop.ini` — запись в чужую папку, `+S`, read-only,
Проводник перепишет; (б) свой файл в папке — тот же мусор; (г) гибрид —
читать чужой `desktop.ini`, писать в базу (H1, P3, после AG и Z1).

### Долги 0.4.x по списку и сторожу — [P0]

Найдено ревью патча 0.4.1 (2026-09-21); ложится на `ListingArrival` /
`KeyboardRules` из AN — чинить в модели, не заплатками:

- **Сторож с выделением прокручивает список к выделенному.** `Refresh`
  ставит приземление «оставить выделенное», `RestoreListSelection` делает
  `ScrollIntoView`: список прокручен колесом в сторону, снаружи появился
  файл — список едет обратно. Приземление без прокрутки для тика сторожа;
  риск — возврат клавиатуры после замены строки с фокусом держится на
  `RestoreListSelection`.
- **«Нет в листинге» ≠ «ушёл из папки».** Решение человека 2026-09-22:
  [любое исчезновение выделенной строки — как удаление]: сосед выделен,
  клавиатура на нём, без прокрутки — и когда строку лишь спрятал фильтр
  (сменили оценку под фильтром по звёздам); сделано в `SettleDeparture`
  (REDESIGN §6.5). Цель панели из правила исключена 2026-09-22. Осталось —
  REDESIGN В7: переименование снаружи ведёт выделение за новым именем,
  нужна пара «было → стало» от сторожа.
- **AD3-хвост.** За переименованием и переносом папки не идут `RecentPaths`
  (MRU адресной строки) и `ManualViewModes` — запись протухает. Тот же
  `PathRewrite.Under`; после Z1 — перекладка ключей базы.

### U. Перетаскивание вглубь — [P1]

Оба жеста — про **приём** (`DropTargetController` + список и панели);
решение «пора» — правило в Core под тест (как `DecideWatchTick`).

- **U1. Папка под курсором раскрывается при задержке.** Подержал над
  папкой — открылась (в панели — ветка, в списке — вошли внутрь), отпустил
  уже там. Решить: задержка (`SystemParameters` или константа), «тот же
  таргет» при дрожи курсора, откат раскрытого, если drop не случился; в
  списке это навигация при живом перетаскивании.
- **U2. Автопрокрутка у края.** Зона 20–30 px, скорость от глубины захода,
  та же механика в панелях папок; шаг — арифметика рядом с `TileLayout`.
  Сосед — рамка выделения не тянется за экран (TECHDEBT).

Проверять руками: ввод не эмулируется.

### AB. Массовое выделение: `SetSelectedItems` — [P2, решить]

Стенд `SelectionProbe` (2026-09-03): перенос выделения 5000 строк между
видами — 10,8 с, квадрат внутри `ListBox.SelectedItems.Add`; снимает только
`ListBox.SetSelectedItems` / `MultiSelector.SetSelectedItems` (0,36 с), оба
`protected`: свои потомки `ListBox` и `DataGrid`, четыре правки в
`FileListView.xaml`. Вместе с TECHDEBT «`List_SelectionChanged`
пересобирает список выделенного».

### J4. Оценка на крупных значках — [P2]

Бейдж в углу есть только в «Галерее» (`FileListView.xaml:1006`,
`GalleryRatingBadge`), столбец — в «Таблице»; оценки читаются вторым
проходом в любом виде. Тот же бейдж в «Крупных значках»; «Плитка» —
решить.

### D6-хвост — [P3]

Перетаскивание закладок мышью внутри панели; порядок уже меняется меню и
`Ctrl+↑/↓`. По первому запросу.

## Блок 3. Сборка и отладка

### AH. Номер сборки четвёртым числом — [P2]

Решения человека 2026-09-22: [инкремент на любой сборке — Debug, `check`,
`-Install`]; [сбрасывает только смена версии]; [в гите и у релиза — 0, то
есть три числа]; [где счётчик — решает Фабле, оценка по факту]; [кэш
миниатюр сравнивает три первых числа].

Сейчас: `Directory.Build.props` — `Version 0.4.1`, `InformationalVersion
0.4.1-beta`, `FileVersion 0.4.1.0`, `AssemblyVersion 0.0.0.0` (закреплён);
`BuildInfo` (Core) читает `InformationalVersion` (+ sha от неявного
SourceLink), `BuildDate` из `AssemblyMetadata` (`Wander.App.csproj:51-56`),
`IsDebug` по `DebuggableAttribute`; `Line` = `v0.4.1 D, 96e5e, 22.09.26`.
`LastRunVersion` сравнивается с полной строкой
(`DropThumbnailCacheOnUpgrade`, `MainViewModel:1897`). Ни одной цели
MSBuild в репозитории нет; `check.bat` и харнесс собирают решение в
Debug; релиз — `publish.ps1` и `release.yml` одной командой.

Спека:

1. **Счётчик** — `src\Wander.App\build-number.txt`, одно число, в
   `.gitignore`. Вне гита: в репозитории номера нет, значит у релиза и у
   CI — 0. Свой на машину; снос `obj` / `bin` его не трогает.
2. **Цель MSBuild** в `Wander.App.csproj` рядом с `BuildDate`:
   `StampBuildNumber`, `BeforeTargets="GetAssemblyAttributes"`,
   `Condition="'$(WanderRelease)' != 'true' and '$(DesignTimeBuild)' !=
   'true'"` (дизайн-сборки Rider счётчик не крутят): `ReadLinesFromFile`
   (нет файла — 0) → `$([MSBuild]::Add(n, 1))` → `WriteLinesToFile
   Overwrite` → `<FileVersion>$(Version).$(BuildNumber)</FileVersion>`.
   Проверить в `obj\…\Wander.AssemblyInfo.cs`, что атрибут
   `AssemblyFileVersion` взял значение из цели. Только `Wander.App`: Core,
   Platform, тесты и харнесс версию не носят. Цена: атрибуты меняются
   каждую сборку → `Wander.App` перекомпилируется всегда, даже без правок
   (секунды); `check.bat` (build + test) двигает счётчик на 2 — монотонность
   важнее.
3. **Релиз**: `release.yml` и `publish.ps1` без `-Install` передают
   `-p:WanderRelease=true` → `FileVersion X.Y.Z.0`; `publish.ps1 -Install`
   — с номером (решение человека). `version.ps1` пишет в файл `0`.
4. **`BuildInfo`**: `BuildNumber` из `AssemblyFileVersionAttribute`
   (четвёртая часть); `Line` → `v0.5.0.137 D, 96e5e, 22.09.26`, при 0 —
   без четвёртого числа. `Version` (три числа + суффикс) не меняется;
   `LastRunVersion` хранит и сравнивает `BuildInfo.Version`
   (`CrashReporter.AppVersion` остаётся полным для отчётов).
5. Доки: RELEASING «версия дублируется» — без изменений (файла в гите нет);
   строка в ARCHITECTURE «Состояние и логи» про номер сборки.

Глазами: сборка из Rider → номер в меню вырос; `check.bat` → +2; после
`version.ps1` → 1. Ответ на вопрос человека про перекомпиляцию: нюанс не в
том, что «код менялся — пересоберётся», а в обратном — `Wander.App`
пересобирается и когда не менялось ничего, потому что номер сам по себе
изменение; это секунды.

### AI. Отладочные операции и занятость — [P2]

Меню отладки (`Settings.ShowDebugMenu`, `MainWindow.xaml:343-348`, сейчас
только «Журнал»). Решение человека 2026-09-22: [занятость — не
переключатель, а обычное групповое действие на выделенных файлах, как
остальные в «Действиях»].

**AI1. Отладочная операция** — пункты подменю «Отладка → Операция:
ровно / ошибка на 4-м / отмена посреди / три сразу». App
`Debug/DebugOperation.cs`: `RunAsync(scenario)` через
`RunWithProgressDialogAsync(Strings.ProgressDebug, ct => …)`
(`MainViewModel:5222`): `_tracker.Begin(OperationVerbs.Debug, 8, token:
ct)` → `SetWeighing(true)` 300 мс → `SetTotalBytes(сумма)` → на каждое из
восьми имён (`Снимок-01.cr3` … , 20–80 МБ) `SetCurrentPath`, `AdvanceBytes`
каждые 100 мс 2–5 с, `Advance`; «ошибка на 4-м» — элемент `Failed` с
`IOException`, «отмена» — `ct` срабатывает сам на 5-м, «три сразу» — три
`Task` в параллель. Итог — `op.Finish(items)` из блока 4 (до него —
`Dispose`). `OperationVerbs.Debug = "ProgressDebug"` («Отладочная
операция»). Пункты — команды `DebugOperationCommand` с параметром. Ни
одного обращения к диску.

**AI2. Отладочная занятость действием.** `IBuiltinAction`
(`Core/Actions/IProcessRunner.cs:52`, `RunAsync(input, output, arguments,
ct)`) — новый `HoldFileAction` (Core, `Actions/`): `arguments` = секунды
(по умолчанию 5), открывает `input` с `FileShare.None`, ждёт, закрывает.
Пресеты в `ActionPresets.All`: «Отладка: занять на 5 с», «… на 30 с»
(`Kind = Builtin`, `Category = Actions`, `Types = All`, `Output = null`,
`IsPreset`); регистрация экземпляра в `PlatformBootstrapper:109` рядом с
`ImageConvertAction`. Правки: `ExternalActionRunner.RunBuiltinAsync`
принимает `Output = null` (сейчас бросает); `CustomAction.DebugOnly`
(новое поле) → `ContextMenuBuilder` фильтрует по
`ContextMenuTarget.ShowDebug` (из `Settings.ShowDebugMenu`),
`SettingsViewModel.RebuildActionRows` прячет. Раннер уже заявляет пути
(`_claims.Claim`) и ведёт прогресс по файлам — часы на значке, «Идёт: …» в
панели, отказ другой операции с объяснением и настоящая занятость для
`IFileBusyProbe` и Restart Manager (держатель — Wander) приходят даром.
Тест Core: `HoldFileAction` держит файл на время (`FakeFileSystem` не
умеет — тест через `System.IO` во временной папке тестов, как у
`TransientFilesTests`).

### AD11. Debug и `state.json` при установленной копии — [P2, отложено]

`--yield` не пишет только при **запущенном** владельце (`InstanceLock`).
Сверх того: не писать и при просто **установленной** копии — сборка старее
перебьёт файл более новой.

Предложение «файл писала версия новее моей — не пишу» (`LastRunVersion`
плюс номер сборки из AH) **отклонено как есть** (2026-09-22): счётчик AH
общий на машину и растёт от любой сборки в Rider, поэтому после первой же
отладочной сборки новее оказывается Debug, а установленная копия замолкает
навсегда — ровно наоборот задуманному.

Решение человека 2026-09-22: [пункт отложен, разобрать с Фабле]. Хочется,
чтобы **писать могла любая версия**, но новые блоки не затирались —
вероятно, частичное версионирование состояния (по блокам), а не «всё или
ничего».

Сделано пока (блок 3): `--yield` работает как работал, плюс форма файла —
`AppState.Version` / `AppState.CurrentVersion` (сейчас 1). Правило:
`JsonAppStateStore.Save` не пишет, если на диске форма новее той, что знает
сборка; читает как обычно. Номер поднимается, когда изменение потерялось бы
или было бы прочитано неверно старой сборкой.

## Блок 4. Операции: надёжное копирование

Спека — [OPERATIONS.md](OPERATIONS.md). Решения человека 2026-09-22:
[цель — съёмный том с буквой диска, приоритет надёжность]; [MTP / PTP —
0.6]; [самое полезное в 0.5, сложное — 0.6]; [завершённые операции живут в
строке состояния до «убрать», проблемные ждут решения]; [журнал — сеанса].

### K. Надёжное копирование — [P1]

В 0.5: запись операции с итогом по элементам (`OperationRecord`,
`IOperationHandle.Finish`), завершённые в строке состояния с «Повторить» /
«Убрать» (K2), повтор при временных ошибках ввода-вывода (`TransientIo`,
`IoRetry`, 3 попытки), понятные сообщения по кодам (место, FAT32 4 ГБ,
устройство пропало), временное имя `.wander-tmp` при копировании с
переименованием в конце, компьютер не засыпает (`IPowerGuard`,
`PowerCreateRequest`), съёмные диски в дереве по `WM_DEVICECHANGE` (Y2),
`Ctrl+Z` ждёт занятых, страховка корзины, имя занятого файла в папке (AF),
хвосты TECHDEBT. Стенд — AI1.

### AD5. Журнал — [P1]

Структурная запись (`JournalEntry`, `JournalKind`): заголовок «Скопировано
в E:\Video — 12 файлов, 12,3 ГБ, 5 мин 27 с» и строка на элемент,
переименование «было → стало», открытие и запуск файлов; строка итога в
технический лог (элементы, байты, секунды, МБ/с); строки отката без
английского `Description`. Журнал сеанса, окно — как сейчас.

### Y2. Съёмные диски — [P1]

`IVolumeChanges` (Core) ← `VolumeChangeHub` (Platform, разбор
`DEV_BROADCAST_VOLUME`) ← хук `HwndSource` в `MainWindow`; дерево правит
`Roots` без пересборки, открытая папка на ушедшем томе уходит по
`NavigationFallback.AfterVolumeRemoved`, закладки переспрашивают
`IsMissing`. «Извлечь» — стенд: даёт ли шелл глагол для `E:\`.

### Отложено на 0.6

BACKLOG, «Копирование — вторая очередь»: MTP / PTP, свой движок с хэшем и
проверкой, продолжение после вылета, пауза при пропаже устройства,
robocopy / небуферизованный ввод-вывод, таймауты UNC, `RestoreMany`.

## Блок 5. Панель просмотра: скорость и форматы

Область кода одна — `PreviewController`, `PreviewPane`, `ImageDecoder`,
`PreviewRouter`, миниатюры. Порядок: AK (замер и меры) → AM → форматы → B6
→ Q5. Спека AK дописывается после замера; остальное — ниже.

### AK. Изображение открывается мгновенно — [P1, иссл.]

Столп 4 (CLAUDE.md): смена снимка выглядит мгновенной, без мельканий и
заглушек; ориентир — FastStone.

1. **Замер** (Фабле, релизная сборка): `PerfLog` от смены выделения до
   первого кадра по форматам (JPEG 24 Мп, CR3, PNG, webp); что видит глаз
   между снимками — пустая панель, заглушка, смена «быстрая → полная»,
   прыжок футера. Записать числа в PERFORMANCE.
2. **Прежний кадр держится до готовности нового**: `Image` не обнуляется на
   старте загрузки; заглушка только если новый не пришёл за 300 мс.
3. **JPEG в два шага**: сначала `DecodePixelWidth` под размер панели
   (масштаб в DCT 1/2 … 1/8, в разы быстрее и без сотни мегабайт), полный —
   тем же `LoadFullSizeAsync` только для лупы, в `ZoomSource`; `MaxWidth` у
   `ImgFit` — от полного размера. Уходит `HighQuality` по 24 Мп в потоке
   рендера. У CR3 вписанный кадр — `PRVW`, панель шире 1620 DIP он не
   заполняет — тот же механизм.
4. **Упреждающее чтение соседей**: следующий и предыдущий по списку
   декодируются в фоне в кэш на две картинки (ключ путь + mtime + размер),
   отмена при смене направления, сброс при уходе из папки.
5. **Debounce смены выделения** (TECHDEBT): Task на каждое изменение,
   200–300 мс при быстрой прокрутке — заменить на «показать сразу, если
   декод в кэше; иначе отложить».
6. Повторный замер; подвисания галереи (BACKLOG «Производительность») —
   отдельный заход по описанному там порядку.

### AM. Резкость и масштаб — хвосты 2026-09-21

- **Вписанная картинка при 125–150 % растянута.** `MaxWidth` / `MaxHeight`
  у `ImgFit`, `GifPreview` и обложки аудио из `BitmapPixelSize` — пиксели
  как DIP. Конвертеру DPI не видно — привязка с самим элементом либо код с
  `DpiChanged`. У `GifPreview` нет `HighQuality`.
- **Миниатюры галереи при 150 %+ мягкие.** `Large` — 256 px, ячейка 200
  DIP при 150 % — 300 px. Размер извлечения от DPI — вместе с ключом кэша
  `thumbs`.
- **Рамка `TileChrome` съедает 2 px и в «Плитке»** — `2 * ChromeBorder` в
  `ForTiles`.
- **«Таблица»: рамка ячейки не входит в `RowHeight`** — `DataGridCell`
  рисует рамку 1 px, строка на 2 px выше настройки.
- **Курсор-лупа 32 px при любом масштабе** — `new Cursor(ms)` без
  `scaleWithDpi`.

### Форматы и миниатюры

- **[P2] B10. webm и heic.** webm уже в списке видео `PreviewRouter:85`,
  без кодека — «не поддерживается»: стенд — что нужно `MediaPlayer` (VP9 /
  Web Media Extensions) и не проще ли WebView2 (Chromium играет сам;
  транспорт другой). heic: в миниатюрах есть (`SystemIconProvider:1106`), в
  панели нет; WIC открывает при HEIF Image Extensions + HEVC Video
  Extensions (HEVC платный либо от производителя). Решить: опираться на
  кодек системы и подсказывать, чего не хватает (столп интеграции), или
  libheif зависимостью. Фикстуры — BACKLOG.
- **[P2] AL. Миниатюры в архиве.** Сейчас выключены решением. Рядом: панель
  распаковывает запись во временную копию (одна за раз). Решить: источник
  (миниатюра shell-элемента записи либо распаковка + обычный путь);
  бюджет (размер записи, только видимое, отмена по уходу); solid 7z / rar
  — одна запись стоит блока; ключ `thumbs` — архив + запись + mtime
  архива; автогалерея внутри архива.
- **[P2] B8. SVG и TGA.** SVG: панель знает `.svg` как код; рисовать —
  WebView2 с переключателем «картинка / исходник»; миниатюра —
  растеризатор пакетом (`Svg.Skia` / `SharpVectors`: зависимость и чужой
  парсер XML) либо без неё — решить. TGA: своего декодера (несжатый и RLE,
  8 / 16 / 24 / 32 бит, ориентация из дескриптора, ~200 строк) в Platform,
  как `RawThumbnail`.
- **[P2] B7. exe, dll, sys, msi** — карточка вместо «нет просмотра»:
  `FileVersionInfo`; архитектура и подсистема из PE-заголовка; подпись
  (`X509Certificate.CreateFromSignedFile`, `WinVerifyTrust`). Факты —
  интерфейс в Core, чтение в Platform, карточка как у папки.
- **[P2] B5-хвост. `.doc` в панели** — абзацы текстом через `IContentExtractor`
  (`OffFilt.dll`) с подписью «форматирование не разобрано»; закрывает
  `.rtf`, `.docx`, `.xlsx`, `.pptx`, `.epub`, OpenDocument.
- **[P2] B4. DjVu** — решить, чем декодировать (обвязка над `djvulibre`,
  порт `DjvuNet`, свой разбор первой страницы) и цену зависимости.
- **[P1] B3.** Сверить футер для картинок и текста после переработок панели.

### B6. Поиск по тексту в панели — [P1]

`Ctrl+F`, когда клавиатура в панели, — поле сверху панели, **не** фильтр
списка. Сегодня ищет только код (AvalonEdit своя панель, `MainWindow`
уступает через `IsCodeEditorFocused`). Одно поле на все текстовые ветки
`PreviewRoute` (Text, MaybeText, Code, Book, Document, Markdown текстом):
«3 из 17», `Enter` / `Shift+Enter`, `Esc` закрывает и оставляет клавиатуру
в панели; AvalonEdit — своя панель спрятана, поиск через `TextDocument`;
Web (HTML, PDF) — свой `Ctrl+F`. **Подхват из поиска по содержимому:**
выделение результата с текстом открывает панель с заполненным полем на
первом совпадении. Решить: как попасть в панель с клавиатуры (BACKLOG
«Клавиатура в панели просмотра»); «искать дальше».

### Q5. Окно «Сравнить» и полноэкранный просмотр — [P1]

Потребители отвязанной `PreviewPane` (`DataContext` — свой
`PreviewController`; второй экземпляр живёт в сплите). **«Сравнить»**: два
текста рядом с синхронной прокруткой; две картинки рядом плюс A/B на одном
месте; остальное — две панели; отдельное немодальное окно с двумя
контроллерами; кнопка — в строке пары `ConflictWindow`. **Полноэкранный
просмотр** по `Enter` / `Space` в галерее — решить форму окна, фокус,
`Esc`. Хелперы блока 1 — тем же `ReviewHelpers` в обоих окнах.

## Блок 6. Мелочи и сайт

### X. Вставка текста и картинки из буфера файлом — [P2]

`Ctrl+V` при тексте в буфере — `.txt`, при картинке (`CF_DIB` / `PNG`) —
PNG без настройки; имя «Текст.txt» / «Изображение.png» с `(1)`
(`UniqueNames`) и сразу в переименование (`RenameAdorner`); UTF-8 без BOM;
`SystemPathGuard`, лог, undo (файл в корзину), без подтверждения; в архиве
и корзине погашено; `ISystemClipboard` — пара «есть текст / есть картинка»,
байты читать в момент вставки; `ClipboardController` сегодня читает только
`CF_HDROP` и `Preferred DropEffect`.

### AD1. WebView2: место и чистка — [P2]

`<DataRoot>\WebView2` — 42 МБ за два месяца: компоненты, которые процесс
браузера качает сам. (1) `AdditionalBrowserArguments` =
`--disable-component-update --disable-background-networking`,
`EnableTrackingPrevention = false` — стендом: свежий профиль, состав через
сутки; (2) место — за `AppSettings.UseSystemTemp` → `%TEMP%\Wander\WebView2`
(профиль одноразовый), действует после перезапуска, старая папка удаляется
на старте, как `TempFiles.Sweep`; (3) «очищать при выходе» — нет (1–2 с
холодного старта).

### AD2. Корзина: холодный заход — [P1]

Тёплая — ~0,4 с. **Не проверено:** первый заход после перезагрузки — на
`Shell.Application` стоил 6–7 с. Строка лога `Recycle bin: opened … first
row after … rows in …` скажет, где секунды: размазаны — отдавать порциями
(`ReconcileEntries`); все до первой строки — пересмотреть отказ от `$I`
(REJECTED). Быстро и холодным — закрыть.

### G6. Поле фильтра над списком — [P2; решено 2026-09-03: пробуем]

Поле из верхней панели окна (`SearchBox` в `MainWindow`) — в полосу над
списком (`GalleryStrip`), к звёздам и хелперам: оно про область файлов;
полоса тогда видна всегда. Решить: одно поле или два (фильтр у списка, окно
поиска `Ctrl+Shift+F` как есть); ширина; скрытие поля, пока открыто окно
поиска. Оценка глазами.

### Q6. Внешнее сравнение — [решить]

Шаблон команды в настройках с автоопределением (VS Code → WinMerge → `git
difftool --no-index`), «открыть оба» — когда ничего не нашлось; картинки —
кнопка ведёт в Q5. Открытое по конфликтам: «Оставить оба» подставляет `(1)`
без диалога имени (TECHDEBT); записи слитой папки внутри архива —
спрашивать ли по одной.

### F2. Сайт: лендинг и гайд — [P2]

Решения человека 2026-09-22: [сайт русский]; [`GUIDE.md` остаётся
источником — общая техническая информация о возможностях; сайт формируется
из него]. Цель: лендинг на GitHub Pages — вводная, читаемая вёрстка, быстрая
загрузка, отдельные страницы гайда с оглавлением, «дальше / назад»,
якорями для ссылок из приложения; скриншоты повторяемо (харнесс рендерит
окно в PNG).

Спека [генератор — решить]: (а, рекомендация) свой генератор `tools/site/`
— консоль на Markdig (уже зависимость App): режет `GUIDE.md` по `## ` на
страницы, оглавление и навигацию из заголовков, лендинг — `docs/site/
index.md` руками + скриншоты, шаблон HTML один, без JS; Actions по пушу в
`master` при изменении `docs/` → Pages. Ноль внешних инструментов, вёрстка
своя. (б) MkDocs Material — навигация и поиск из коробки, Python в CI,
структура из `mkdocs.yml`, GUIDE.md всё равно резать на файлы. (в) голый
`docs/` на Pages с Jekyll — дёшево, оглавление руками, вид по умолчанию
бедный. Адрес — `github.io`; ссылка «Справка» из приложения
(`CrashReporter:51-53`: master для Debug, `blob/vX.Y.Z/` для релиза) → на
сайт с якорем.

## Блок 7. Доки и релиз

- **Сжать под конец версии** (решение человека 2026-09-22, по одному
  файлу за заход, начиная с пары ARCHITECTURE ↔ DONE): ARCHITECTURE (185
  КБ), DONE (127), QA (118), GUIDE (78). Правило — факты, решения, имена;
  история — в CHANGELOG и гите.
- **Устаревшие ссылки**: QA.md → PLAN W1; ARCHITECTURE → PLAN AC, R2 / R3,
  поле `_manualViewModeFolders` (в коде `_manualViewModes`); проверить
  скриптом `PLAN [A-Z]{1,2}\d?` по докам перед релизом.
- **Вопросы, отложенные с 0.4.0** [решить]: Win32-имена в Rider (118 «Name
  does not match rule» на P/Invoke — `ReSharper disable InconsistentNaming`
  парами либо не трогать); анализаторы (`TreatWarningsAsErrors`, точечно
  `CA1310`, `CA1309`, `CA2101`, `CA1001`; `VSTHRD100` / `110`); слова из
  XMP при конвертации (вшитый XMP / IPTC через `XmpDirectory` или сайдкар;
  нужен RAW со словами в фикстурах); «Документы» и соседи в
  `SystemPathGuard` (тест `UserProfileRoot_IsProtected_ButDocumentsAreNot`
  закрепляет обратное Проводнику); рабочий набор под харнессом +10–15 %;
  `.rar` / `.tar.gz` при WinRAR — привязка обработчика по CLSID мимо
  ассоциации.
- **Корзина на границах — стенд на машине без человека** (T): (а) путь
  внутри `$Recycle.Bin` на 60–70 знаков длиннее — папки с путями 200–259
  внутри могут получить системный вопрос с «Да» по умолчанию; (б) папка
  больше лимита корзины. Страховка (в) — блок 4.
- PRERELEASE.md целиком; `check.bat qa` и полный набор харнесса по слову
  «готовимся к релизу».

## Обсуждения

Кода не получают, пока нет решения. Кандидаты для сравнения (объём: малый
— дни, средний — неделя, крупный — больше):

| Кандидат | Что даёт | Объём | Риск |
|---|---|---|---|
| **Тёмная тема** (Roadmap) | витрина релиза | средний: второй набор ключей `Palette.xaml`, `GalleryPalette`, тема AvalonEdit, CSS Markdown / HTML, `AppsUseLightTheme` + `UserPreferenceChanged` | каждый XAML; скриншоты харнесса меняются |
| **AA синхронизация, фаза 1** | архив «источник → копия» одним окном | крупный | удаления в копии; деструктивная фаза — после релиза |
| **G5 результаты поиска узлами панели** | выдача переживает навигацию; закрывает пять хвостов (BACKLOG) | крупный | средний |
| **B9 системные обработчики** | doc / xls / ppt / chm / djvu | крупный | чужой COM, `HwndHost`, фокус |
| **M переназначение хоткеев** | для живущих на клавиатуре | крупный: три места разбора | средний |

### Y3. «Сеть» и FTP

Узел «Сеть» через `IShellNamespace` (ветка корзины и архивов);
«подключить сетевой диск» — `WNetConnectionDialog`; закладка на
`\\server\share` работает сегодня. FTP: тот же namespace на `ftp://` без TLS
либо свой клиент (`FluentFTP`, второй `IFileSystem`). Решить: сценарий и
стоит ли он больше, чем закладка на UNC и WinSCP рядом.

### B9. Плагины / системные обработчики для тяжёлых форматов

Системные `IPreviewHandler` / `IThumbnailProvider` — плагины, которые пишут
другие (SumatraPDF, LibreOffice, 7-Zip); столп интеграции за, цена —
`HwndHost`, чужой COM в процессе. Свой формат плагинов — Roadmap.
Предложение: сначала системные обработчики.

### AA. Синхронизация папок

Фаза 1 — односторонний апдейт (источник → копия) окном конфликтов в
режиме «план синхронизации»: новые, изменённые, удалить в копии (в
корзину); фаза 2 — двусторонний синк со снимком прошлого синка в data-dir.
Рядом есть `MergeScanner`, `ConflictVerdict`, `FileContentComparer`,
`ConflictWindow`, `BatchExecutor`. Решить: перенос и переименование без
индекса; «новее» на FAT и при переводе часов; точка входа. Сосед — BACKLOG
«оставить обе папки».

### M. Переназначение горячих клавиш — [P2]

Жесты в трёх местах — `MainWindow.xaml` `InputBindings`, `MainWindow.xaml.cs`,
`FileListView.xaml.cs`; нужен один реестр: идентификаторы команд, жест ↔
текст с раскладко-независимостью, контекст, конфликты, в `AppSettings`
только отличия. Пока — `HotkeyCatalog` менять вместе с местом обработки.

### N. Чек-лист в приложении

Живёт в приложении, отмечает сам там, где действие видно; список — файл,
достижимый Claude и видимый приложению, не в `docs/`. Открыто: источник
событий, место файла, только отладочная сборка или любая, как не
задублировать FINALIZING / RELEASING. Сосед — меню отладки (AI).

### T. Прогон автотестов вне рабочей машины

Изоляции машины нет: харнесс за краем экрана человека, операции по живой
ФС, настоящая корзина, `attrs` правит ACL, `deep` создаёт junction.
Варианты: Windows Sandbox (чисто, нет состояния, WebView2 под вопросом),
Hyper-V со снапшотами (обслуживать), CI `windows-latest` (окно WPF и шелл
в неинтерактивной сессии), отдельная машина. Решить: что уносим, кто
запускает, как возвращаются артефакты, профили с правами. Туда же — UIA
(FlaUI) на релизном exe, стенды корзины на границах, `ConflictWindow` в
харнессе.

### Поиск и фильтры — P3

- **G3-хвост.** Фильтры по дате, размеру и regex — форма диапазонов, общая с
  J3 (ISO и выдержка; EXIF — кэш путь + mtime рядом с `thumbs`).
- **G4.** PDF без системного `IFilter` — честно «не прочитан»; вариант с
  зависимостью — BACKLOG.
- **I5.** Спутники: какие форматы ещё — BACKLOG «Файлы-спутники».

## Чистки

Code health pass между вехами, без фич; повод — фича, изменившая публичный
API Core. Правила и роли — WORKFLOW, «Техчистка»; пройденные #1–#4 — DONE.
Остатки #4: файловые операции в `MainViewModel` (O4.6; блок 4 даёт теме
своё состояние — запись операции); `FileListView.xaml.cs` ~1200 строк
(блок 2); `SystemIconProvider` 1419 (PInvoke без тестов — худший размен);
`TreeNodeViewModel` (блок 2); тесты на `NavigationController` — с
`IDispatcher`; `Wander.Core/FileSystem/` (27 файлов) — не резать.

## Направления (P3)

- Hex view в панели просмотра.
- Symlinks / junctions / hard links (сейчас только `.lnk`) — Roadmap.
- Тёмная тема — Roadmap; сравнение — таблица выше.
