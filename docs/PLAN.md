# Wander — план

Список **обязательных целей** проекта и их детализация — что делаем и
в каком порядке.

Здесь только **несделанное**. Всё, что уже работает, живёт в
[DONE.md](DONE.md) и описано там как «как оно устроено сейчас», а не как его
делали; закрытый пункт уезжает туда целиком, а не остаётся тут зачёркнутым.

В отличие от соседей:

- [`DONE.md`](DONE.md) — снапшот того, что уже работает.
- [`REJECTED.md`](REJECTED.md) — что обсудили и решили не брать, с причиной.
- [`TECHDEBT.md`](TECHDEBT.md) — мелкая шероховатость (дальние чистки).
- [`BACKLOG.md`](BACKLOG.md) — конкретные второстепенные задачи (полезное, но не сейчас).
- [`CLAUDE.md`](../CLAUDE.md) Roadmap — стратегические направления продукта.
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — как устроен код и механизмы.

---

## Столпы проекта

1. **UX** — убрать лишнее из Win11 Explorer, починить его баги, добавить удобств.
   Понятный, быстрый, отзывчивый интерфейс и управление
2. **Надёжность** — деструктивные операции всегда требуют подтверждения с Cancel
   по-умолчанию; явные ошибки вместо тихих сбоев; обратимость через `Ctrl+Z`.
3. **Интеграция с системой** — по-умолчанию выбираем вариант, при котором обмен
   с системой и другими приложениями работает (буфер обмена, drag & drop,
   ассоциации, хоткеи). Своя изолированная реализация — осознанная уступка,
   которая выносится на обсуждение и записывается в
   [BACKLOG.md](BACKLOG.md), а не принимается молча.

---

## Приоритеты

| | Что значит | Когда брать |
|---|---|---|
| **P0** | Багфиксы, незавершённое из готового, блокеры | Прямо сейчас, до новых фич |
| **P1** | Обязательное для proper file-manager UX | Следующая итерация |
| **P2** | Желательное, заметное улучшение | После P1 |
| **P3** | Опциональное / нишевое | Когда дойдём |

---

## Пройденные спринты

Таблица спринтов 1–7 с итогами — в [DONE.md](DONE.md), «Пройденные
спринты»; что менялось от версии к версии — в [CHANGELOG.md](CHANGELOG.md).

---

## Ближайший фокус

### Спринт 8 (текущий)

Три блока, и первым берётся чистка — остальные два садятся в тот код,
который она приведёт в порядок.

| | Блок | Что это | Где детализация |
|---|---|---|---|
| 1 | **Чистка и реструктуризация логики** | Ревизия структуры без новых фич: мёртвый код, крупные сущности, недетерминированные состояния, направление зависимостей | секция **O** |
| 2 | **Умный режим разрешения конфликтов** | Сравнение файлов попарно (дата / размер / контрольная сумма / превью) и нормальное окно вместо нынешнего | секция **Q** |
| 3 | **Открытие архивов как папок** | zip / rar / 7z в панели папок: просмотр, извлечение, «открыть» через временный файл | секция **P** |

**Почему такой порядок.** Чистка трогает `MainViewModel`, `BatchExecutor` и
навигацию — то есть ровно те места, куда лягут два других блока. Сделать
наоборот значит написать новый код в старой структуре и потом переносить его
второй раз. Дальше — конфликты: блок небольшой, замкнут в `Conflict/` и в
контракте `IConflictResolver`, и закрывает окно, которое видно каждый раз
при перезаписи. Архивы последними: это самая широкая поверхность (навигация,
адресная строка, дерево, буфер обмена, панель просмотра, гейты команд), и
браться за неё стоит по прибранному.

Порядок не догма: блоки независимы, и если архивы нужнее — их можно взять
вторыми, заплатив за это переносом (см. «Что решить до начала» в O).

### Хвосты, которые стоит подобрать заодно

- **B3** — сверить футер панели просмотра для картинок и текста. Мелочь,
  скорее всего один прогон глазами.
- **I5** — какие ещё форматы спутников поддерживать; нужен разговор,
  не реализация.
- **G5** — доделки по результатам поиска. Шаг **O4.3** сделан, так что
  доделки лягут в `Controllers/SearchResultsController` — можно брать.
- **Фризы: разобраны.** Итог — в [DONE.md](DONE.md), «Навигация не ждёт
  диск», механика — в [ARCHITECTURE.md](ARCHITECTURE.md), «Отзывчивость»;
  шаг **O10** закрыт 2026-09-01. В [TECHDEBT.md](TECHDEBT.md) остались
  синхронный `Enumerate` уровня дерева при первом раскрытии (редко, но
  ~110 мс), сама стоимость демонтажа контейнеров (теперь платится ниже
  ввода) и принудительный layout при возврате фокуса (`ui.restore` до
  500 мс). Регрессионные кейсы — в [MANUAL-CHECKS.md](MANUAL-CHECKS.md),
  «Скорость и отзывчивость» — прогнать руками после этого шага.

---

## Обязательные цели

Закрыто целиком и описано в [DONE.md](DONE.md): **A** (вёрстка / UI),
**C** (выделение), **F** (настройки; новые категории заводятся под
реальные настройки, а не заранее — пустая страница «Тема» до самой тёмной
темы была бы обещанием на годы), **L** (контекстное меню в духе Win10;
его P3-хвосты — в [TECHDEBT.md](TECHDEBT.md) и [BACKLOG.md](BACKLOG.md)).

### B. Панель просмотра

- **[P1] B3. Сверить общую информацию под содержимым.** Футер собирается
  для всех типов, но для картинок и текста его не перепроверяли после
  переработок панели. Скорее всего обернётся в один прогон глазами.
- **[P2] B4. Разобрать DjVu.** Нужен предпросмотр и обложка в плитках:
  часть книг лежит именно в этом формате, и без превью полка из них
  неразличима. Это единственный формат из заказанного пака, который не
  открылся ничем из того, что уже есть, — Windows его не знает, WebView2
  тоже. Что предстоит выяснить в первую очередь:

  - **Чем декодировать.** DjVu — это JB2 для текстового слоя и IW44 для
    фонового, то есть два настоящих кодека. Варианты: живая .NET-обвязка
    над `djvulibre` (нативная DLL рядом с приложением), чистый .NET-порт
    (`DjvuNet` — MIT, но давно не трогали и покрытие форматов неполное),
    либо свой разбор только того подмножества, которого хватает на первую
    страницу.
  - **Что показывать.** Для плитки достаточно первой страницы в мелком
    разрешении; для панели — постранично, с прокруткой. Это разные объёмы
    работы, и первую половину можно закрыть отдельно.
  - **Цена по слоям.** Декодер — это внешний бинарник либо чужой код в
    процессе, который читает файл неизвестного происхождения. Решение по
    зависимости нужно принять до кода, а не после (см. столп «минимализм»
    и разбор `.doc` / CHM в [BACKLOG.md](BACKLOG.md)).

- **[P2] B5-хвост. Показать `.doc` в панели просмотра.** Половина пункта
  закрыта поиском: текст из `.doc` теперь достаётся — системным `IFilter`
  (`OffFilt.dll`, он есть в самой Windows, Office для этого не нужен), а
  абстракция под это — `IContentExtractor` в `Wander.Core/Search/`, и она
  закрывает заодно `.rtf`, `.docx`, `.xlsx`, `.pptx`, `.epub` и
  OpenDocument. Осталось довести до панели: минимум — абзацы простым
  текстом с честной подписью «форматирование не разобрано». Полноценная
  вёрстка — только через системный обработчик предпросмотра (разбор — в
  [BACKLOG.md](BACKLOG.md)).

Остальное (B1, B2) закрыто — см. [DONE.md](DONE.md).

### D. Закладки

- **[P3] D6-хвост. Перетаскивание закладок между собой.** Порядок стал
  изменяемым 2026-08-31: «Переместить вверх» / «Переместить вниз» в меню
  строки и `Ctrl` + `↑` / `Ctrl` + `↓` с клавиатуры, порядок живёт в
  `AppState.Favorites`. Осталась мышь — перетащить строку на новое место
  внутри панели. Делать при первом запросе: два-три пункта переставляются
  клавишами быстрее, чем мышью.

Остальное закрыто — см. [DONE.md](DONE.md).

### E. Async-операции

- **[P2] E2-доработка. Спрятать прогресс в статус-бар.** Сейчас
  `ProgressDialog` модальный: пока идёт копирование, окно занято. Аггрегатный
  индикатор в статус-баре уже есть — не хватает кнопки «свернуть» в диалоге и
  возврата из статус-бара обратно.

Остальное закрыто — см. [DONE.md](DONE.md).

### F2. Документация — нормальная страница гайда — [P2, следующая версия]

`docs/GUIDE.md` дорос до размера, в котором один markdown-файл перестаёт
работать: семьсот с лишним строк, навигация только через `Ctrl` + `F`, ни
одной картинки там, где половина вопросов — «как это выглядит». Пока
сойдёт, но на следующую версию нужно:

- **разобрать подходы**, а не выбрать первый попавшийся. Кандидаты, каждый
  со своей ценой: GitHub Pages + статический генератор (MkDocs Material,
  Docusaurus, Astro Starlight) — навигация и поиск из коробки, но это
  второй репозиторий-артефакт и CI; голый `docs/` на GitHub Pages без
  генератора — дёшево, но оглавление и поиск делать руками; страница
  внутри самого приложения (`WebView2` уже есть) — работает офлайн и без
  браузера, но её надо собирать и складывать в exe;
- **структуру**: разделы по задачам («первый запуск», «навигация»,
  «просмотр», «операции», «настройка»), а не по подсистемам, и отдельная
  страница-справочник хоткеев;
- **картинки**: скриншоты режимов, панели просмотра, галереи, окна поиска.
  Снимать их придётся регулярно — значит нужен способ снимать их
  повторяемо, а не руками (офлайн-харнесс из `check.bat` умеет поднимать
  окно за экраном и рендерить его в PNG — оттуда и плясать);
- **навигацию**: оглавление, ссылки «дальше/назад», якоря, на которые можно
  сослаться из приложения.

Что нужно решить до начала: одна страница на весь гайд или много; где она
живёт (репозиторий / Pages / внутри приложения); и кто её собирает — руками
или сборкой.

### G. Поиск

Сделанная часть (быстрый фильтр, окно поиска, побайтовый режим) — в
[DONE.md](DONE.md) и [ARCHITECTURE.md](ARCHITECTURE.md); область «весь
компьютер» была сделана и удалена как ненужная — [REJECTED.md](REJECTED.md).

Открыто:

- **[P2] G5. Доделки по результатам поиска** — пять задач, поставленных
  сразу после того, как поиск заработал: путь у результата во всех видах,
  кнопка «перейти», судьба фильтра при клике по дереву, знаменатель у
  прогресса и результаты как узлы отдельной панели. Разбор каждой и то, как
  они друг от друга зависят, — в [BACKLOG.md](BACKLOG.md), раздел «Поиск:
  что доделать». Последняя закрывает остальные, поэтому браться стоит с
  неё.
- **[P3] G3-хвост. Фильтры по дате, размеру и regex.** Фильтр по типу
  закрыт маской имени (`*.cs;*.md`); осталось то, что требует формы для
  диапазонов, и regex как третий язык поля имени. Место под это в окне
  поиска уже есть. Это та же задача «форма фильтра по диапазонам», которая
  стоит у хвоста **J3** (фильтр по ISO и выдержке), и решать её стоит один
  раз на обе.
- **[P3] G4. PDF там, где системного фильтра нет.** Содержимое `.pdf`
  читается через `IFilter`, а его на чистой Windows нет — он приезжает с
  Adobe Reader или Foxit. Сейчас такие файлы честно считаются
  непрочитанными и это видно в строке состояния. Разобранный вариант с
  зависимостью — в [BACKLOG.md](BACKLOG.md).

### H. Спец-возможности — [P3]

- **H1. desktop.ini.** При смене вида в папке — опционально писать в
  `desktop.ini`. Сначала решить: учитывать чужой, писать свой, или оба.

H2 (Unity `.meta`) поглощён секцией **I**.

---

### I. Companion-файлы

Общий механизм «файл + его спутник(и) = одна сущность». Как это работает
сейчас — в [DONE.md](DONE.md) и [ARCHITECTURE.md](ARCHITECTURE.md); правила
живут в `Wander.Core/Companions/`, поддержаны `.meta`, `.pp3` и `.xmp`.

Открыто:

- **I2-хвост.** Если спутник упал уже после того, как основной файл переехал,
  группа помечается Failed, а успевшие члены остаются на месте. `Ctrl+Z`
  чинит, но автоматического отката середины группы (как в `RenameMany`) тут
  нет.
- **I5. Какие ещё форматы поддержать** — [обсудить]. Механизм расширяется
  одной строкой в наборе правил, но ни один кандидат пока не выглядит
  нужным. Список кандидатов и причина откладывания — в
  [BACKLOG.md](BACKLOG.md).

---

### J. Режим галереи

Сделан, кроме фильтра по EXIF, — см. [DONE.md](DONE.md). Открыто:

- **[P3] J3-хвост. Фильтр по ISO и выдержке.** Звёзды и цветные метки
  фильтруются бесплатно: они лежат в сайдкарах, которые свёртка листинга и
  так нашла, поэтому папка без сайдкаров не стоит ни одного обращения к
  диску. С ISO и выдержкой это не так — их надо доставать из EXIF каждого
  файла, то есть открывать сотни файлов на папку. Что нужно решить до кода:

  - **Кэш.** Метаданные снимка не меняются; ключ — путь плюс время
    изменения, и хранить их логично там же, где миниатюры
    (`%LocalAppData%\Wander\thumbs` рядом, отдельным индексом), иначе
    каждый заход в папку читает всё заново.
  - **Прогресс и отмена.** Проход по сотням файлов — это уже операция, а
    не деталь: нужна отмена по уходу из папки (как у прохода по сайдкарам)
    и, скорее всего, индикатор.
  - **Форма фильтра.** Звёзды и цвет умещаются в полосу; диапазон ISO и
    выдержки — уже нет. Либо полоса раскрывается, либо это отдельная
    панель, и решать это стоит вместе с G3 (расширенный поиск), где та же
    задача «фильтр по нескольким полям» стоит целиком.

---

### K. Надёжное копирование больших файлов — [P2/P3, исследование]

Заменить/дополнить текущий `System.IO`-путь внешним движком для тяжёлых
случаев: копирование десятков гигабайт, нестабильная сеть, съёмные диски.

**Кандидаты:**

- **robocopy** — встроен в Windows, ставить нечего. Даёт retry
  (`/R` / `/W`), restartable mode (`/Z` — продолжает с места обрыва),
  многопоточность (`/MT`), длинные пути, внятные коды возврата.
- **FastCopy** (открытые исходники) — интересен не как зависимость, а как
  референс: свои буферы и обход системного кэша, чтобы копирование не
  вытесняло всё остальное из памяти. Посмотреть подход, не тащить код
  (лицензию проверить отдельно).
- **certutil** — копировать не умеет, это хешер (`certutil -hashfile`).
  Его место — **вторая половина задачи: verify-after-copy**. Скопировали →
  посчитали хеш источника и приёмника → сверили. Для «надёжно» это как раз
  недостающий кусок; для больших файлов считать хеш дорого, так что нужен
  флаг, а не всегда.

**Что придётся решить — это и есть основная работа, а не выбор утилиты:**

- **Слои.** Внешний процесс — платформенная деталь, значит реализация в
  `Wander.Platform.Windows` за существующей абстракцией. В Core не должно
  протечь ни слова про robocopy.
- **Прогресс.** `OperationTracker` ждёт per-item шаги, robocopy печатает
  свой прогресс в stdout. Либо парсить вывод, либо смириться на неопределённый
  прогресс для внешнего пути.
- **Отмена.** Сейчас `CancellationToken` проверяется между элементами. С
  внешним процессом отмена = убить процесс, и надо решить, что делать с
  недокопированным хвостом (robocopy `/Z` позволяет продолжить).
- **Undo.** Копирование само по себе откатывается удалением приёмника — но
  надо убедиться, что undo-запись формируется и для внешнего пути тоже.
  Операция без undo в UI не выпускается.
- **Когда включать.** Всегда? По порогу размера? Флагом в настройках?
  Гонять отдельный процесс ради копирования мелочи — лишние накладные
  расходы.
- **Ошибки.** Коды возврата robocopy нетривиальны: 0–7 это успех разной
  степени, 8+ — реальные сбои. Замапить в человеческие сообщения.

---

### M. Переназначение горячих клавиш — [P2]

Справочник сделан (2026-08-29): в настройках появилась категория
«Клавиатура» — те же пять таблиц, что в [GUIDE.md](GUIDE.md), читаются из
приложения. Она **читает**, не редактирует, и так и написано на странице.

Редактирование — отдельная работа, и вот из чего она состоит. Сейчас
сочетания живут в трёх местах:

1. `MainWindow.xaml`, `Window.InputBindings` — 16 `KeyBinding` на команды.
2. `MainWindow.xaml.cs` — разбор клавиш там, где нужен контекст (какая
   область активна, что под курсором).
3. `FileListView.xaml.cs` — стрелки в плиточных видах, цифры-оценки,
   `Enter` / `Esc` в переименовании.

Чтобы жест стал настройкой, все три должны спрашивать «какая команда
назначена на это сочетание» у одного реестра, а не решать сами. Значит:

- **Идентификаторы команд.** У `MenuCommandId` уже есть половина нужного
  списка — но не вся: перемещение по областям, режимы отображения и
  оценки цифрами командами не являются. Либо расширять его, либо заводить
  второй перечень и отвечать, чем они отличаются.
- **Разбор жеста в текст и обратно** — `Ctrl + Shift + 1` ↔
  (`ModifierKeys`, `Key`), с раскладко-независимостью: `Ctrl` + `Я` и
  `Ctrl` + `Z` — одно и то же нажатие.
- **Контекст.** Половина сочетаний зависит от того, где клавиатура:
  `Enter` в дереве, в адресной строке и в списке — три разных действия, и
  реестр обязан это различать, иначе переназначение одного ломает два.
- **Конфликты.** Диалог должен ловить «это сочетание уже занято» до
  сохранения, включая конфликт с системными и с нашими же контекстными.
- **Хранение и сброс.** В `AppSettings` — только отличия от значений
  по-умолчанию, иначе смена умолчания в новой версии не доедет до тех, кто
  ничего не менял.

Пока это не сделано, `HotkeyCatalog` остаётся списком-справочником, и
менять его нужно вместе с местом, где жест обрабатывается — иначе
справочник соврёт.

### N. Интерактивный чек-лист прогона релиза — [к обсуждению]

Записано 2026-08-31 по итогам первого захода на замеры из логов
(см. [RELEASING.md](RELEASING.md), «Замеры перед релизом»). **Не делаем,
пока не обсудим** — здесь идея и открытые вопросы, а не постановка.

Идея. Перед релизом собирается релизная сборка и её надо погонять руками
по одному и тому же списку — иначе проверяется то, что вспомнилось, а не
то, что важно. Пусть список живёт в приложении: пункт «Релиз: чек-лист» в
меню «Отладка» открывает отдельное окно со списком того, что надо сделать,
и **сам отмечает пункты по мере прохождения** — приложение видит, что
пользователь открыл галерею, переименовал файл, отменил операцию. Когда
все пункты отмечены — «Well done».

Что нужно обсудить до кода:

- **Откуда берутся события.** Отмечать пункт по факту действия — значит
  иметь шину событий, которой сейчас нет: команды выполняются напрямую, а
  лог пишется строками текста. Либо чек-лист подписывается на `ILogger` и
  разбирает строки (хрупко, но ничего не меняет в коде), либо появляется
  настоящий поток событий (честно, но это новая абстракция поперёк всего
  приложения — ровно то, от чего предостерегает столп «минимализм»).
- **Что считается пунктом.** «Открыть галерею» приложение увидит.
  «Убедиться, что миниатюры не мигают» — нет, а это как раз то, ради чего
  прогон и делается. Значит, пунктов два сорта: автоматические и те, что
  человек отмечает сам, — и надо решить, не превращается ли вторая
  половина в обычный текстовый список.
- **Где список.** Захардкожен, отдельный файл рядом с exe, или из
  `docs/`? От этого зависит, кто его правит и переживает ли он обновление.
- **Только для отладочной сборки или для любой.** Гонять надо **релизную**
  сборку — значит, пункт меню «Отладка» должен быть в ней доступен, а
  сейчас это меню включается настройкой `ShowDebugMenu`.
- **Не дублирует ли это [FINALIZING.md](FINALIZING.md) и
  [RELEASING.md](RELEASING.md).** Два списка про одно и то же расходятся
  быстрее, чем один устаревает.

---

### O. Чистка и реструктуризация логики — [P0 спринта 8]

Это **чистка #4** из раздела «Технические чистки» ниже, доросшая до
отдельной секции: условие для прохода наступило (галерея изменила публичный
API Core), но повестка оказалась шире обычной ревизии — не «посмотреть, не
пора ли», а конкретный список того, что надо порезать и выпрямить.

Заход **не меняет поведение**. Ни одной новой возможности, ни одного
исправленного бага «раз уж я здесь»: баг найден — запись в
[TECHDEBT.md](TECHDEBT.md) или сюда, но не в тот же коммит. Иначе разбор
регрессии превращается в чтение диффа на три тысячи строк.

**Положение на 2026-09-01:** сделаны **все шаги, O1–O11**; все диффы
Опуса (O5, O4.7, O6.3, O6.5, O6.6, O8, O9) прошли ревью Фабле. Последним
закрыт **O10** (фризы — поднят решением 2026-09-01, потому что виден на
живом прогоне; O7 и O9 выполнены раньше него). Заход закончен; ручной
прогон MANUAL-CHECKS «Скорость и отзывчивость» — за человеком.

#### Правила прохода

- **Каждый шаг — свой коммит, и каждый заканчивается зелёным
  `.\tools\check.bat`.** Не «в конце всё проверим».
- **Тесты не переписываются по существу.** Переименование namespace'а и
  правка `using` — да. Если тест пришлось переписать, потому что «теперь
  так не работает», — поведение изменилось, а это запрещено; шаг
  откатывается.
- **Мера успеха фиксируется числом до начала** (шаг O1) и сверяется после.
  «Стало чище» — не результат.
- **Найденное, но не входящее в шаг, — записывается, а не делается.** Список
  находок ведётся отдельным файлом на время захода и в конце разносится по
  TECHDEBT / BACKLOG / PLAN.
- **Разделять — не раньше, чем есть вторая причина трогать файл.** Сам по
  себе размер — повод записать; повод переписывать — размер плюс то, что в
  файл предстоит лезть на следующем же шаге. Для `MainViewModel`,
  `PreviewController` и `MainWindow.xaml.cs` вторая причина есть: в них
  садятся секции P и Q.
- **Исполнитель размечен по шагам** (2026-09-01): «Фабле» — там, где риск
  — тихая смена поведения или проектное решение; «Опус» — механические
  шаги по написанной спеке, промахи в которых ловят компилятор и тесты.
  Шаг, выполненный Опусом, после зелёного чека **не закрыт**, пока Фабле
  не посмотрел дифф: отчёт исполнителя обязан заканчиваться напоминанием
  позвать Фабле на ревью.

#### Крупные шаги

##### O1. Замер и инвентаризация — ✅ пройден

Снимок «до» снят скриптом `tools\metrics.ps1`; скрипт остался в
репозитории и описан в CLAUDE.md (см. «Решено до начала»). Список находок
жил в scratchpad захода и разнесён по шагам.

##### O2. Мёртвый код и лишние абстракции — ✅ пройден (2026-08-31)

Неиспользуемые `using`, приватные члены и мёртвые fakes сняты; публичные
члены Core без внешних вызовов ушли во `internal`. Остаток — один мёртвый
`using Microsoft.Web.WebView2.Core` в `MainWindow.xaml.cs`, см.
[TECHDEBT.md](TECHDEBT.md).

Критерий «когда интерфейс остаётся» действует и дальше (на него ссылается
секция **P**). Интерфейс остаётся, если выполняется хотя бы одно: (1) он
пересекает границу Core ↔ Platform.Windows — это правило архитектуры,
обсуждению не подлежит; (2) его подменяют тесты; (3) вторая реализация уже
запланирована и записана. Всё остальное — inline. Примеры того, что
**остаётся**: `IConflictResolver` (пункт 2), `IFileSystem` (пункт 1).

##### O3. Общий код — вынести дубли — ✅ сделано (2026-08-31)

Итог: `Util/SizeFormatter` и `Util/TimeFormat` — по одной реализации;
`DispatcherExtensions.Post` / `Ask` вместо ручного «выполни на UI-потоке»;
`ServiceLocator.TryGet<T>()` вместо трёхстрочника (осталось одно место
`IsRegistered`); обходы дерева собраны в `FolderTreesController` (O4.2);
третий фоновый проход по папке закрыт эпохой листинга (O6.2) — общий тип
не понадобился.

##### O4. Порезать `MainViewModel` — 4330 строк

Самый крупный кусок работы захода. Файл вырос вдвое с чистки #2 (та ужала
его до ~1230) и по собственному признаку из списка ревизий — самый большой
кандидат на разделение в проекте.

Режется по темам, **каждая тема — отдельный коммит**, в порядке от самой
изолированной к самой связанной:

| | Тема | Куда уехало | Состояние |
|---|---|---|---|
| O4.1 | **Закладки** | `Controllers/BookmarksController` | ✅ сделано |
| O4.2 | **Деревья** | `Controllers/FolderTreesController` | ✅ по вью-модели; жесты дерева и разметка — **O4.7**, ✅ сделано |
| O4.3 | **Результаты поиска** | `Controllers/SearchResultsController` | ✅ сделано, **до G5** |
| O4.4 | **Оценки** | `Controllers/RatingsController` | ✅ кроме фильтра, см. ниже |
| O4.5 | **Оболочечные команды** | `Controllers/ShellCommandsController` | ✅ кроме `OpenSettingsDialog`, см. ниже |
| O4.6 | **Файловые операции и отчёты** | — | ⏸ **не выносится**, см. ниже |

**Что осталось в `MainViewModel`:** навигация, текущий листинг, выделение,
режим отображения, файловые операции и связывание контроллеров между собой —
**3300 строк** на момент решения, **2949** к 2026-09-01 (попутные чистки),
**2808** после O11. Порог «меньше 1000» из первой редакции не достигнут и
достижим разрезанием по темам не был: он ставился до того, как стало видно,
сколько в файле самого листинга. Число зафиксировано как факт, критерием
готовности шага оно не является (решение 2026-08-31).

**Что не поехало и почему:**

- **O4.6 — целиком.** У темы **нет собственного состояния**: каждая операция
  читает выделение и текущую папку, зовёт сервис и просит перелистать. Проверка
  из правила ниже даёт прямой ответ — контроллер получился бы из восьми
  сервисов, трёх событий и делегата на намерение выделения, то есть ровно
  «дёргает хозяина через точку». Половинчатый вынос (только `ConfirmMove`,
  `DescribeError`, `ReportBatchResults`) развёл бы вопрос и его вызывающего
  по двум файлам, не убрав ни одной связи. **Вынесено в вопросы к обсуждению.**
- **Фильтр по оценкам** (`SetFilterRank` / `SetFilterColor` / `SyncFilterChoices`) —
  три-четыре строки делегирования в `_search.RatingFilter` плюс свойства, на
  которые XAML смотрит изнутри `Style` и `DataTemplate`. Перенос означал бы
  правку биндингов в шаблонах — она не проверяется ни компилятором, ни
  тестами, ни smoke-запуском, а выигрыш нулевой. `UpdateFilterStatus` тем
  более остаётся: это статус-строка листинга, оценки там один случай из трёх.
- **`OpenSettingsDialog`** — модальное окно WPF, а не оболочечный глагол.
  Контроллер, который `new`-ает `Window` и дёргает `MainWindow.FocusWorkArea()`,
  хуже метода на месте. По той же причине рядом остался выбор папки в
  `RelocateBookmark`. Обе — кандидаты в **O8**.

##### O4.7. Деревья как отдельный контрол — ✅ сделано (2026-09-01)

*Исполнитель: Опус. Ревью Фабле пройдено 2026-09-01 — принято без правок
(сверены `NodeAt`, эхо-гашение `NavigateFromTree`, гашение клика при
драге, приём drop'а на закладки).*

Хвост O4.2 и чистки #3. Из `MainWindow` уехала вся левая панель —
`Views/FolderTreesView` по образцу `FileListView`:

- **Разметка (488 строк XAML):** оба `TreeView` со своими шаблонами, общий
  стиль строки `WanderTreeViewItem` и шеврон `TreeExpander`, меню панели
  `PanelSettingsMenu`, заголовок закладок, полоса «+» и разделитель. В окне
  на их месте один элемент и комментарий, куда всё уехало.
- **Жесты (665 строк code-behind):** раскрытие по клику и `Alt` на шевроне,
  drag узла, правый клик как цель операции, `Shift` + колесо,
  коалесированный клавиатурный обход (`TreeNavBurstMs` / `TreeNavSettleMs`),
  меню строки закладки и её перемещение, приём drop'а на закладки, высота
  панели и её делитель.

**Граница получилась узкой:** три события наверх (`ContextMenuRequested`,
`FolderTargeted`, `FocusListRequested`) — ровно то, что требует соседа
(меню собирает окно, выделение отдаёт список) — и горсть методов вниз для
порядка областей, который по плану остаётся в окне до **O8**
(`FocusBookmarks` / `FocusDrives` / `HasBookmarks` / `PaneOf` /
`ShowFocusOutline` / `RevealAndFocus` / `TargetSelected`). Общие объекты
drag & drop передаются один раз через `Connect(drops, drag)`: панели и
список обязаны отвечать на одно перетаскивание одинаково, а для этого им
нужны те же самые `DropTargetController` и `OutgoingDrag`.

Попутно (обе правки — следствие переезда, а не «заодно»):
`ListVisuals.IsInsideControl` — у него стало два вызывающих (адресная
строка и строка дерева); `DropTargetController.Execute` — обвязка вокруг
плана (отказ, `Handled`, снятие подсветки в `finally`) в одном месте
вместо двух копий на двух поверхностях.

**Числа:** `MainWindow.xaml.cs` 1637 → **972**, `MainWindow.xaml`
1182 → **694**; новый контрол — 870 строк кода и 482 разметки.

**Чем проверено, раз тесты сюда не дотягиваются.** Кроме
`.\tools\check.bat run`: харнесс в scratchpad поднимает **настоящее окно**
за экраном (`--smoke`, `Left = -32000`, без фокуса и панели задач) с
подменённым `IAppStateStore` и слушает трассировку биндингов WPF
(`PresentationTraceSources.DataBindingSource`) — то есть видит то, чего не
видит компилятор. Проверено: обе панели набиты строками, шаблон строки
реализуется, `FocusDrives` / `FocusBookmarks` ставят клавиатуру на строку,
`PaneOf` отвечает про сфокусированную строку, рамка области ставится и
снимается, меню настроек панели дотягивается до вью-модели через
`PlacementTarget`, ни одной ошибки биндинга. Сам слушатель проверен заведомо
битым биндингом — иначе «ошибок нет» ничего не значит.

**Руками остаётся** (MANUAL-CHECKS, «Дерево (диски) и панель закладок»):
раскрытие по клику и `Alt`, drag папки из дерева наружу и внутрь, правый
клик как цель операции, `Shift` + колесо, `Ctrl+↑/↓` на закладке, «…» на
строке закладки, бросок папки на полосу «+» и в пустое место панели,
делитель между панелями и его сохранение.

##### O11. «Сессия папки» — оркестрация навигации и листинга в Core — ✅ сделано (2026-09-01)

*Исполнитель: **Фабле**, целиком.*

Решение открытого вопроса O4.6: резать не по теме, а **по состоянию**.
Итог — `Wander.Core/Listing/`: `FolderSession` (эпохи листинга, намерение
на прибытие, память выделения LRU 64, решения сторожа поверх
`FolderChanges`), `ListingDiff` (дифф листинга чистой функцией — план
правок, VM только применяет), `ArrivalIntent` (переехал из App). Машина
решений без ввода-вывода: факты на входе, решения на выходе,
`MainViewModel` — исполнитель. Механизм и границы описаны в
[ARCHITECTURE.md](ARCHITECTURE.md), «Сессия папки»; +40 тестов
(`FolderSessionTests`, `ListingDiffTests`), 803 → 843, `MainViewModel`
2949 → 2808 строк.

Инварианты, которые раньше проверялись только руками, теперь тесты:
навигация во время незавершённого листинга, опоздавший ответ не
публикуется, правка метаданных не пересобирает папку, намерение не
переживает навигацию в другую папку и не открывает переименование под
чужим листингом, тик сторожа идемпотентен и не теряет изменения под
занятостью (закрыта сторожевая половина O6.5).

**O11.5 решён по месту**, как и допускалось: признак «прибытие» — в
сессии (`BeginListing`), а правило спиннера, восстановление статуса
поверх «N элементов» и мост `FocusListAfterRestore` осознанно остались в
VM — тестируемого содержания в них нет, причина записана в
ARCHITECTURE.md.

**Дальше:** пересмотреть O4.6 (у файловых операций появился сосед с
состоянием — часть связей может стать вызовами к сессии) при следующем
заходе на `MainViewModel`; O7 разблокирован.

##### O5. Порезать остальных крупных — ✅ сделано (2026-09-01)

*Исполнитель: Опус. Ревью Фабле пройдено 2026-09-01 — принято без правок
(порядок маршрутов `PreviewRouter` сверен с прежней цепочкой `if` один в
один; `RatedListing.WithRatings` — дословный перенос с делегатом чтения).*

- **`PreviewController` — 2067 → 1525 строк**, три роли разъехались:
  - «какой файл чем открывать» — таблица `PreviewRouter` в
    `Wander.Core/Preview/`, без чтения с диска и под тестами
    (`PreviewRouterTests`, +24 теста). Цепочка из тринадцати `if` на месте
    вызова стала `switch` по маршруту. `PreviewRoute` — не то же самое, что
    `PreviewKind`: первое говорит, каким загрузчиком файл прочитан, второе —
    какой контрол его рисует, и Markdown, FB2 и PDF показывают, что это
    разные вопросы. Механизм описан в
    [ARCHITECTURE.md](ARCHITECTURE.md), «Preview pane».
  - раскодирование — `Wander.App/Preview/`: `ImageDecoder` (правила
    декодера), `ModelBuilder` + `ModelScene` (геометрия Core → WPF),
    `PreviewText` (чтение с бюджетом, обрезка, Markdown, HTML-обёртка).
  - подпись под содержимым — `SummaryText` там же.
  - Контроллеру остались конвейер, отмена и наблюдаемые свойства.
    Наблюдаемая поверхность **не трогалась**: XAML биндится к
    `Preview.Summary`, `Preview.FolderTypes`, `Preview.Volume*` как и
    раньше — перенос свойств означал бы правку биндингов внутри
    `Style` и `DataTemplate`, которую не ловит ни компилятор, ни тесты,
    ни smoke-запуск (то же решение, что по фильтру оценок в O4).
- **`MainWindow.xaml.cs` — 1806 → 1637 строк.** Уехало перетаскивание
  наружу: `DragPreview/OutgoingDrag` — плашка, курсор, формулировка,
  один конвейер для списка и для дерева. Окну остался жест «потащили» и
  одна связь обратно — полоса закладок сообщает о своём загорании,
  потому что это её собственная часть окна. Заодно два одинаковых
  «добавить брошенные папки в закладки» стали одним `AddDroppedBookmarks`.
  Остальные три темы файла принадлежат другим шагам: жесты дерева —
  **O4.7**, области окна и политика фокуса — **O8**, туда же
  захардкоженные кисти полосы закладок.
- **`CompanionMetadataService` — 428 → 392 строки.** `WithRatings` уехал в
  `Wander.Core/Listing/RatedListing`: пройти строки папки и решить, какие
  заменить, — вопрос про листинг, а не про сайдкар; как читается оценка,
  приходит делегатом. Чтение / правка / создание сайдкара **остались
  вместе** — узкий путь записи (только существующий файл, только через
  `ReplaceAtomic`, с логом и undo) это одна история, и файл после переезда
  ниже порога в 500 строк.
- **`FileListView.xaml.cs` (1216) и `SystemIconProvider` (1419) — не
  режутся в этом шаге**, решение по факту замера:
  - у `FileListView` ближайший шов — геометрия перемещения стрелками
    (`TryGridStep` / `IsAtEdge` / `CaretIndex`, ~120 строк), и она уже
    расписана в **O8** с переездом в `Wander.Core/Layout/`. Резать файл
    дважды подряд — двойная работа; решение по остатку — после O8.
    *(O8 сделан 2026-09-01: шов ушёл, файл — 1201 строка. Решение по
    остатку открыто.)*
  - у `SystemIconProvider` два настоящих шва: кэш (память + диск + LRU +
    ключи) и блок PInvoke-объявлений. Кэш — ровно то, что обязана
    пересмотреть категория 6 в **O6** (изменяемая статика), и переносить
    его до того решения значит переносить дважды. PInvoke без тестов и
    без ручной проверки трогать в чистке — худший размен из возможных.
    *(Итог O6.6: кэши оказались полями экземпляра под замком и остаются
    на месте; шероховатости ключей — отдельными записями в TECHDEBT.
    Файл в этом заходе так и не режется.)*

##### O6. Недетерминированные состояния — ✅ сделано (2026-09-01)

> **Недетерминированное состояние** — то, где результат зависит не от того,
> что попросил пользователь, а от того, **кто успел первым**: какой поток,
> какое событие, какой таймер, что зарегистрировалось при старте. Это не
> синоним «багов параллельности»: однопоточный код с четырьмя отложенными
> намерениями и одним стоком недетерминирован ровно так же.

Семь категорий, по каждой — правка либо запись «почему оставили». Все
долговременные правила и вердикты живут в
[ARCHITECTURE.md](ARCHITECTURE.md); здесь — итог одной строкой на
категорию:

1. **Отложенные намерения с общим стоком** → один `ArrivalIntent`:
   установка заменяет, применение в одном месте, под тестами (O11.3);
   мостом к вью остался `FocusListAfterRestore`.
2. **Гонка писателей в `Entries`** → эпоха листинга с одной точкой
   отбраковки (`PublishRows`, O11.3); новые писатели (EXIF из J3, архивы
   из P) входят в тот же механизм, а не приносят свою проверку.
3. **Опциональные сервисы** (Опус, ревью пройдено) → 41 место `TryGet`
   стало 18 по правилу «сервис, который где-то читается `Get<T>()`,
   обязателен везде»; null-object в бутстраппер осознанно не поехал;
   таблица «чего не будет без каждого необязательного» — в ARCHITECTURE.
   Единственная тестируемая деградация — `ITextSource`
   (`TextFallbackTests`).
4. **Скрытый порядок инициализации** (Фабле) → конструктор `MainViewModel`
   в две фазы «построить → включить», флаг `_restoring` удалён
   (ARCHITECTURE, «Конструирование в две фазы»); попутно закрыт живой баг
   того же класса — null-`Settings` в `RatingsController`, NRE на
   постановке оценки.
5. **Таймеры и троттлинг** (решение Фабле, правки Опус, ревью пройдено) →
   правило «таймер — троттл решения, а не место решения» с инвентарём —
   ARCHITECTURE, «Таймеры»; абстракция часов осознанно не заводится, пока
   таймеры живут в App. Оба найденных расхождения закрыты на ревью:
   таймер видео гасится вместе с медиа, две секции про порог автозапуска
   сведены.
6. **Изменяемая статика** (решение Фабле, правки Опус, ревью пройдено с
   одной поправкой формулировки по `CrashReporter`) → единственная
   правка — `lock` в `ServiceLocator`; правило «локатор — не канал
   доставки фейков», таблица вердиктов по оставленной статике и
   конвенция потребления сервисов — ARCHITECTURE, «Изменяемая статика» и
   «Как потребляются сервисы».
7. **Маршалинг вручную** → `DispatcherExtensions.Post` / `Ask` (закрыт
   ещё в O3).

##### O7. Выпрямление зависимостей — ✅ сделано (2026-09-01)

*Исполнитель: Фабле. Выполнен раньше O10 по прямому решению 2026-09-01.*

Что сделано, по пунктам исходного списка:

- **Граф «от → к» — артефактом.** `tools\deps.ps1`: свод `using`-ов по
  папкам, уровни, конденсация циклов, сверка namespace ↔ папка;
  `-UpdateDoc` перезаписывает сгенерированный блок в
  [ARCHITECTURE.md](ARCHITECTURE.md) («Граф зависимостей между папками») —
  там же записано правило «нет циклов, у каждой папки уровень».
- **Обратные вызовы вверх — развёрнуты.** `PreviewController` вместо двух
  делегатов поднимает события `RatingRequested` (ответ хоста — в
  `RatingRequestedEventArgs.Rating`, синхронно, чтобы звёзды не отставали
  на кадр) и `RevealRequested`; `NavigationController` вместо трёх
  коллбэков получил `IFileSystem` + `IShellNamespace?` + `ILogger` в
  конструктор (проверка пути и имя shell-сентинела — его собственные
  вопросы к сервисам, не решения хоста) и событие `StatusReported` для
  «путь не найден» — по образцу остальных контроллеров.
- **Правило `IFileSystem` / `System.IO`** записано в ARCHITECTURE.md
  («Когда `IFileSystem`, а когда `System.IO`»); факт сверен: напрямую в
  Core читает только `Preview/`.
- **`SettingsViewModel` ↔ `MainViewModel` — подозреваемый закрыт без
  правок:** после O4.7/O6.4 связь односторонняя. Settings никого не знает
  и только поднимает `PropertyChanged`; MainVM пишет в него как владелец
  (команды сортировки, колесо, сбросы) и реагирует в одном месте
  (`OnSettingsChanged`), записей в Settings из обработчиков нет.
- **Циклы между папками.** Шесть переездов, каждый ради конкретного ребра:
  в Core — `UndoableActions` → `FileSystem/` (в `Undo/` остался чистый
  механизм, уровень 0), `SidecarRating`+`ColorLabels` → `FileSystem/` (тип,
  который несёт `FileSystemEntry.Rating`, обязан жить не выше записи),
  `SearchController` и `ImageFolderProbe` → `Listing/` (живой фильтр и
  автогалерея — решения листинга); в App — `DropTargetController` →
  `DragPreview/`, `RubberBandController` → `Controls/` (плюс
  `ColorLabelViewModel` перестал тащить `using Controllers` ради
  doc-ссылки). **Core и Platform.Windows — без циклов, у каждой папки
  уровень.** В App остался ровно один клубок `[Controllers+ViewModels]`
  (`MainViewModel` хостит контроллеры, контроллеры используют базовые
  типы из `ViewModels/`) — он разрубается только решением O9 «отделять ли
  ViewModels от Controllers», запись есть там. **Разрублен в O9
  (2026-09-01):** `MainViewModel` переехал в корень `Wander.App`, App стал
  ацикличным.

Попутная находка (в [TECHDEBT.md](TECHDEBT.md), не в этот дифф): лимиты
кэша миниатюр применяются только на старте, вопреки док-комменту
`ApplyThumbnailCacheSettings`.

##### O8. Отделение логики от представления — ✅ сделано (2026-09-01)

*Исполнитель: Опус. Ревью Фабле пройдено 2026-09-01 — принято без правок:
все 159 пар «был оттенок → стал ключ» сверены скриптом, дрейфа нет;
семантика `Ring` (возврат в исходную зону последней), лестницы
`FolderPane`, констант геометрии (320×240, полосы 100/60) и якоря
Shift-выделения сверена с прежним кодом построчно; блок графа в
ARCHITECTURE совпадает с живым прогоном `deps.ps1`.*

**Логика из code-behind уехала в `Wander.Core/Layout/`**, где её достаёт
тест (+35 тестов, 870 → **905**):

- `GridNavigation.IsAtEdge` и `GridNavigation.Anchor` — края сетки и
  правило «якорь — тот конец прогона, на котором не стоит каретка».
  В `FileListView` остались три строки обвязки: каретка берётся из
  `Keyboard.FocusedElement`, выделенность приходит делегатом.
- `WindowZones` (`WindowZone`, `Order`, `Ring`, `FolderPane`) — порядок
  областей и политика `Ctrl` + `1`. Кольцевая арифметика и лестница
  из пяти фактов — ровно те два места, где прячется ошибка на единицу и
  забытый случай «открыли ни из одной панели». Окну осталось
  исполнение: `ZoneOf` по визуальному дереву и `FocusZone`.
- `WindowPlacement` — восстановление геометрии окна (порог размера,
  прижатие к виртуальному экрану). В списке шага его не было — взят по
  критерию «после шага в code-behind не остаётся того, что хочется
  покрыть тестом»: два его случая молчаливы и видны только на чужой
  конфигурации мониторов.

**Цвета собраны в одно место — `Resources/Palette.xaml`** (97 ключей,
влит в `App.xaml` первым) плюс `Resources/Palette.cs` для code-behind.
Убраны **все** 145 захардкоженных цветов из 12 XAML-файлов и все кисти
из восьми файлов C# (`_activeZoneBrush`, `_dropZone*`, адорнеры лассо и
цели drop’а, плашка драга, ошибка в `PromptDialog`, пять цветных меток,
три цвета полосы заполнения диска, которые нашлись строками в
`PreviewController`). Семь кистей меню уехали туда же из `MenuStyles.xaml`.
Имена говорят, **что красится**, а не какой это оттенок; что осталось
снаружи и почему — списком в шапке файла (четыре места:
`GalleryPalette`, `.xshd`-подсветка, `DefaultBackgroundColor` у WebView2 и
сборка обложки в платформенном слое).

Найденное по дороге — три записи в [TECHDEBT.md](TECHDEBT.md), не в этот дифф:
восемь градаций серого текста, дубль проводниковых цветов в
`GalleryPalette` и то, что ключи палитры никто не сверяет — в отличие от
ключей `Strings.resx`.

##### O9. Именование, навигация по файлу и раскладка по папкам — ✅ сделано (2026-09-01)

*Исполнитель: Опус. Ревью Фабле пройдено 2026-09-01 — принято без правок:
40 из 47 переложенных файлов — чистая перестановка (сверено сравнением
мультимножеств строк «до/после»), все 7 содержательных отличий — ровно
заявленные (namespace, два снятых `using`, воссоединённые доки, баннеры,
два уточнённых комментария); опасных зависимостей порядка инициализации
статических полей нет (проверено сканом); блок графа совпадает с живым
прогоном, App ацикличен. Одна правка по ревью — дерево в ARCHITECTURE
не показывало переезд `MainViewModel`, поправлено там же.*

Идёт **после** разрезания: переименовывать до переезда — двойная работа.

**Мера:** сканер порядка членов (в scratchpad захода, см. TECHDEBT) насчитал
**138 членов не на своём месте в 67 файлах** до прохода и **39 в 19** после.
Все 39 оставшихся — либо блок P/Invoke, который не режется (в
`ShellContextMenuInterop` порядок членов интерфейса равен порядку vtable),
либо пара `DependencyProperty` + свойство, либо предел самого сканера
(кортеж в типе поля, многострочная сигнатура). Тестов **905**, формат чистый,
строк **426** — как до прохода: поведение не менялось.

- **Один порядок внутри файла** везде: константы → поля → конструктор →
  свойства → публичные методы → приватные помощники → вложенные типы.
  `#region` не заводим: лекарство от «в файле не найти» — файл поменьше, а
  не свёрнутая простыня.
  **Сделано, с двумя записанными оговорками** (обе в CLAUDE.md, «Порядок
  членов в файле»): порядок держится **внутри раздела** там, где файл разбит
  баннерами `// --- Заголовок ---` (иначе в `MainViewModel` и `FileListView`
  фича разъезжается на три экрана), а приватное поле прямо над своим
  свойством и блок P/Invoke остаются целыми. Заодно приведён к одной форме
  сам баннер: коробки `// ====` в `PreviewPane.xaml.cs` стали
  `// --- Заголовок ---`, как во всех остальных 36 файлах.
  По дороге нашлись и починены **три осиротевших док-комментария** — блоки
  `/// <summary>`, оставшиеся без члена после прошлых разрезаний: доки
  `WatchIntervalMs` и `SearchResultsController.FlushIntervalMs` (жила в
  `MainViewModel`) и доки `AsyncIcon.ApplyPriority`.
- **Папка — это домен, а не сорт.** `Companions/`, `Search/`, `Undo/` —
  правильно. O7 уже увёл из `Util/` приём drop'а (в `DragPreview/`) и рамку
  выделения (в `Controls/`); остались форматтеры, контроллер выделения,
  `ListVisuals` и `DispatcherExtensions`.
  **Решено (2026-09-01): `Util/` остаётся как есть, без искусственного
  потолка.** Это утилитарные классы, по смыслу отдельные от контроллеров;
  критерий здоровья папки — не число файлов, а уровень 0 в графе
  зависимостей: появившееся ребро из `Util/` вверх ловит `deps.ps1`, и вот
  оно — повод для переезда, а не счётчик.
- **`ViewModels/` (16 файлов) и `Controllers/` (8)** — раскол уже случился
  в O4/O5, но не доведён: граф O7 показывает ровно один оставшийся цикл
  проекта — `[Controllers+ViewModels]`: `MainViewModel` хостит контроллеры,
  контроллеры берут базовые типы из `ViewModels/`.
  **Решено (2026-09-01): `MainViewModel.cs` выносится из `ViewModels/` в
  корень `Wander.App` — к `MainWindow.xaml.cs`, окну, которое он
  наполняет.** `ViewModels/` становится слоем биндабельных типов ниже
  контроллеров, цикл исчезает одним переездом. Цена: namespace →
  `Wander.App`, одна живая строка XAML (`<vm:MainViewModel/>` в
  `MainWindow.xaml` — сменить префикс) плюс `using`-и и упоминания в
  доках; и одна оговорка читателю — «главная VM живёт при окне, а не в
  `ViewModels/`» — записать в ARCHITECTURE, «Окно и его контролы».
  После переезда перегенерить блок графа (`deps.ps1 -UpdateDoc`) — App
  обязан стать ацикличным.
  **Сделано.** Префикс в `MainWindow.xaml` — `local` (`xmlns:local=
  "clr-namespace:Wander.App"`), а не `vm`: указывать `vm` на корневой
  namespace вводило бы в заблуждение. `using Wander.App.ViewModels` снят там,
  где после переезда стал лишним (`MainWindow.xaml.cs`,
  `Views/SearchWindow.xaml.cs`) — остальным он по-прежнему нужен ради
  `ObservableObject` и прочих базовых типов. Граф перегенерён: **у App шесть
  уровней и ни одного цикла**, `ViewModels/` на уровне 1 под `Controllers/`
  на уровне 2. Оговорка записана в ARCHITECTURE, «Окно и его контролы».
- **`Wander.Core/FileSystem/` — 27 файлов**: интерфейсы, сервисы, записи,
  компараторы, guard'ы. **Решено (2026-09-01): в O9 не резать.** Циклов
  нет, уровень есть, читаемость достаточна; вопрос откладывается до
  следующего цикла рефакторинга — надёжность файловой системы дороже
  перекладки ради перекладки.

Найденное по дороге и **не** сделанное — две записи в
[TECHDEBT.md](TECHDEBT.md): 1649 типографских знаков в комментариях кода
(правило записано в CLAUDE.md в тот же день, код ему пока не соответствует)
и то, что порядок членов, в отличие от ключей `Strings.resx`, никем не
сверяется — сканер прохода жил в scratchpad и в репозиторий не попал.

##### O10. Оптимизации без усложнения — ✅ сделано (2026-09-01)

*Исполнитель: **Фабле** — фиксы отзывчивости трижды ломали друг друга
(см. MANUAL-CHECKS), это худшее место для «почти правильно».*

**Поднят в очереди решением 2026-09-01**: фризы видны на живом прогоне
после O11, и разобраны сразу после детерминированности (остатка O6); O7 и
O9 к этому моменту уже были сделаны. К списку ниже добавилась стоимость
приземления листинга — двух Reset'ов на навигацию (немедленная очистка
при входе — осознанное решение по UX, см. DONE.md и TECHDEBT.md).

Правило шага: только то, что делает код короче или таким же; всё, что
требует нового кэша, нового потока или нового флага, — не сюда, а в
[BACKLOG.md](BACKLOG.md). `Task.Run` внутри уже асинхронного метода за
«новый поток» не считался: это тот же пул, на котором давно живут листинг
и пробы шевронов.

**Мера — `PERF`-строки логов сессий за 2026-09-01, все прогоны дня до
правки.** `nav.refresh` p50 **78** / p90 **119 мс** на каждую навигацию —
и ровно столько же в первом `list.apply` той же секунды: это синхронный
демонтаж контейнеров покидаемой папки внутри обработчика клика, а второе
приземление стоит ~1 мс. `nav.trees` p50 111 мс, но 8 строк на ~100
навигаций (первый вход в незагруженную ветку). `ui.restore` p90 504 мс.
`ui.stall` p50 301 / p90 723 мс, максимум 9,3 с. Сравнение «после» — по
живому прогону MANUAL-CHECKS «Скорость и отзывчивость»: из сессии клик не
воспроизвести, ожидание — `nav.refresh` перестаёт попадать в лог (порог
33 мс). Структурно (`metrics.ps1`): тестов **905**, строк ресурсов
**426**, формат чистый — как до шага; кода без комментариев **+50 / −31
строк** в шести файлах, то есть на 19 строк длиннее — каждая из них гард
«ответ вернулся в изменившийся мир» (токен, `_loaded`, «пользователь уже
ушёл»), цена ухода с UI-потока; нового кэша, флага или потока нет.

Сделано, один и тот же класс проблемы — диск на UI-потоке:

- **Очистка при входе — ниже ввода и не всегда** (`RefreshFolderAsync`).
  Строки покидаемой папки убираются на `Background`, после того как клик
  обработан и переход нарисован; если листинг к тому моменту уже пришёл —
  очистка пропускается, локальная папка встаёт одним свопом. UX-решение
  «контекст переключается сразу» сохранено: стоять старым строкам дольше
  одного кадра некуда, а кликнуть за кадр нельзя.
- **`F5` и тумблеры Hidden/System перечитывают дерево на пуле**
  (`TreeNodeViewModel.RefreshChildrenAsync`): уровень читается фоном,
  сверка — на диспетчере; ветка, схлопнутая, пока диск отвечал, ответ
  отбрасывает. Первое раскрытие (`EnsureLoaded`) осталось синхронным —
  `TryExpandToPath` требует детей сразу, честная починка — асинхронный
  спуск, это отдельная работа (TECHDEBT).
- **Старт не ждёт диск** (`OpenStartFolderAsync`): «есть ли ещё последняя
  папка» спрашивается на пуле, фолбэк на первый диск — как раньше, ответ
  отбрасывается, если пользователь уже ушёл сам.
- **Чистка результатов поиска** (`PruneMissingAsync`): те же `stat`'ы по
  снимку строк, но на пуле. Пять тысяч `stat`'ов остались — запись в
  TECHDEBT про «знать, какие пути затронула операция» жива.
- **Панель просмотра**: открытие файла для текста (синхронное при любом
  `useAsync`), `stat` для книги и проба «похоже на текст» — на пуле.
  `TooBigForText` как такового уже не было.
- **Размер кэша миниатюр** в настройках считается на пуле.
- **Корзина** — оказалось, уже починена (`RefreshShellAsync` через
  `Task.Run`); запись в TECHDEBT закрыта.

**Живой прогон после первого диффа** показал, что при быстром проходе по
папкам с фотографиями открытая папка подтормаживает и некоторое время
видна предыдущая. Причина была не в очистке, а в конвейере миниатюр
(`AsyncIcon`): (1) их приземления стояли на том же `Background`, что
очистка и листинг, и по FIFO шли раньше — сотни доставок старой папки,
потом новая; (2) контейнеры, снесённые Reset'ом, не сбрасывали поколение,
и их загрузки продолжали занимать шлюз на четыре слота и shell-вызовы по
30–75 мс впереди миниатюр новой папки. Починено там же: миниатюры
приземляются на `ContextIdle`, ярусом ниже листинга; `Unloaded` поднимает
поколение, и загрузка ушедшего контейнера отступает у шлюза и у декодера;
контейнер, вернувшийся на тот же файл, переспрашивает по `Loaded`.

Не сделано, осознанно: **`SyncEntries` квадратичный** — словарь позиций не
спасает (индексы плана сдвигаются на каждом Insert/Move), честная O(n log
n) длиннее нынешних десяти строк, а случай по логам не встречался.
Найденное по дороге — запись в TECHDEBT: `ui.restore` до 500 мс — это
принудительный `UpdateLayout` в `FileListView.FocusRow`, не диск.

#### Карта: пункт запроса → шаг

| Из запроса | Шаг |
|---|---|
| Удаление мёртвого кода и лишних абстракций | O2 |
| Выделить общий код | O3 |
| Порезать крупные сущности | O4, O5 |
| Упрощение алгоритмов, связей, абстракций | O2 (абстракции), O7 (связи), O10 (алгоритмы) |
| Разбивка крупных методов на под-функции | внутри O4 / O5, отдельным шагом не идёт |
| Сокращение логических путей — `A(+B+C)-D(+E)-F` | O4 / O5: это и есть критерий «правильно ли порезали». Цепочка **на месте вызова** должна стать короче, а не просто переехать в другой файл |
| Правка недетерминированных состояний | O6 |
| Оркестрация навигации и листинга — тестируемой сущностью | O11 |
| Выпрямление зависимостей | O7 |
| Отделение логики от представления | O8 ✅ |
| Именование, понятность, лаконичность | O9 ✅ |
| Оптимизации без добавления сложности | O10 |
| Навигация по файлу и раскладка по папкам | O9 ✅ |

#### Решено до начала

1. **Порог `MainViewModel`** — пожелание, не критерий. Режем по связности
   тем, итоговый размер фиксируем числом. См. итог в O4.
2. **`ViewModels/` разделена на `ViewModels/` (то, с чем связывается XAML) и
   `Controllers/` (то, что владеет поведением)**; `Util/` оставлена как
   осознанная свалка мелочей с потолком по числу файлов — потолок уже
   выбран (8 файлов после `TimeFormat` и `DispatcherExtensions`).
3. **`tools\metrics.ps1` живёт в репозитории**, описан в CLAUDE.md.
4. **Правило «когда `IFileSystem`, когда `System.IO`»** принято как есть: в
   Core напрямую читает только `Preview/`, больше ни одного `File.` /
   `Directory.`. ✅ Записано в ARCHITECTURE.md на шаге **O7** (2026-09-01).
5. **Порядок блоков спринта** — чистка первой, как и планировалось.
6. **Судьба O4.6 решена (2026-09-01): резать по листингу** — см. ниже.
   Принцип зафиксирован в [CLAUDE.md](../CLAUDE.md): «сложность выносится
   туда, где её достаёт тест».

#### Судьба O4.6 — решено: резать по листингу

Из трёх вариантов выбран второй (2026-09-01): шаг **O11** вынул из
вью-модели сам листинг — эпоху, намерения, память выделения, решения
сторожа — сущностью Core (✅ сделан в тот же день). Теперь у файловых
операций есть сосед с состоянием, и O4.6 подлежит пересмотру: часть
связей может стать вызовами к сессии, а не к хозяину. «Смириться с
обратными ссылками» отвергнут; «оставить и записать причину» — решить
при следующем заходе на `MainViewModel`.

#### Чего этот заход не делает

- Не вводит DI-контейнер и MVVM-фреймворк — правила проекта не меняются.
- Не трогает Core-логику, покрытую тестами, иначе как переносом.
- Не «улучшает» соседний код: правило хирургических правок действует и
  внутри чистки.
- Не начинает тёмную тему — только собирает в кучу то, что ей мешает (O8).

---

### P. Открытие архивов как папок — [P2, спринт 8]

Архив (`.zip`, `.rar`, `.7z`) открывается в панели папок как папка: видно
содержимое, можно вытащить выделенное, можно «открыть» файл внутри — он
распакуется во временный и запустится ассоциированной программой.

Сюда же переезжает пункт «расширение панели просмотра: zip-архивы» из
«Активных направлений»: показать содержимое архива в панели просмотра и
открыть его как папку — это одна механика, а не две.

#### Минимальный функционал

1. Архив разворачивается как папка: вход по `Enter` / двойному клику, выход
   назад, адресная строка показывает `…\pack.zip\subdir`.
2. Содержимое видно в списке: имя, размер, дата. Папки внутри архива —
   папками.
3. Выделенное извлекается наружу: перетаскиванием, `Ctrl` + `C` → `Ctrl` + `V`
   в обычную папку, пунктом меню «Извлечь…».
4. «Открыть» файл внутри — распаковка во временный файл и запуск.
5. Всё остальное внутри архива **запрещено** и погашено: удаление,
   переименование, вставка внутрь, создание папки. Ровно так же, как сейчас
   ведёт себя корзина.

#### Что уже есть и почему это дёшево

Прецедент в проекте: **корзина**. Она — не файловая система, а
shell-namespace, и вся обвязка под это уже написана:

- `IShellNamespace` (Core) — `IsShellPath` / `Enumerate` / `GetDisplayName`;
- `WindowsShellNamespace` (Platform) — обход через `IShellItem`;
- `MainViewModel.RefreshShellAsync` — отдельная ветка листинга;
- `PathIsNavigable` / `IsCurrentShellNamespace` — навигация и гейты команд;
- `IShellContextMenu` — чужое меню уже строится по пути.

Архив в Windows устроен так же: `.zip` — это namespace-extension
(`zipfldr.dll`), и Проводник показывает его папкой через тот же
`IShellFolder`. То есть **основной кандидат реализации — не новый механизм, а
обобщение существующего**: снять с `IShellNamespace` предположение «путь
начинается с `shell:`» и научить его понимать «настоящий путь до файла +
хвост внутри него».

После **O11** shell-ветка листинга — ветка «сессии папки», а не ещё один
метод вью-модели: архивное перечисление садится третьей веткой той же
сессии, с той же эпохой и теми же тестируемыми правилами прибытия. Ещё
одна причина брать P по прибранному.

#### Разобрать поведение Проводника: что можно взять дёшево

Это первый шаг секции, до кода. Что проверить на текущей машине
(Windows 11 Pro 26200) и записать результатом:

- **Какие форматы Проводник открывает сам.** `.zip` — точно. `.rar`, `.7z`,
  `.tar`, `.gz` Windows 11 научилась читать (libarchive, начиная с 23H2) —
  **проверить, работает ли это как namespace, то есть видит ли их
  `IShellFolder`**, или только как «Извлечь всё» в контекстном меню. Это
  главная развилка секции: если видит — rar и 7z приезжают бесплатно вместе
  с zip; если нет — под них нужен свой читатель, и они уходят во вторую
  очередь.
- **Извлечение перетаскиванием** Проводник отдаёт потоком содержимого
  (`CFSTR_FILEDESCRIPTOR` / `CFSTR_FILECONTENTS`), а не путём. У нас этот
  формат **уже распознаётся** — и честно отвечает «вставить не умею»
  (см. DONE.md, буфер обмена). Научиться его принимать — это одновременно:
  извлечение из архива, вставка вложений писем и вставка снимков с телефона.
  **Самый выгодный шаг секции: одна работа закрывает три сценария**, и первые
  два — ровно то, что здесь нужно.
- **«Извлечь всё…»** — это shell-глагол на самом файле архива. Звать его
  через `IShellLauncher` мы уже умеем; кода — строчка.
- **Чего Проводник не делает и нам тоже не надо в минимуме:** записи внутрь
  архива, редактирования на месте, обратной записи изменённого временного
  файла (Проводник её теряет молча — известная ловушка, и повторять её мы
  как раз не обязаны, см. вопросы ниже).

#### Развилка реализации

| | Через shell (namespace-extension) | Свой читатель |
|---|---|---|
| Зависимости | нет | `System.IO.Compression` для zip (в составе .NET), для rar/7z — сторонняя библиотека |
| Форматы | что умеет система: zip всегда, rar/7z — по результату проверки выше | что подключим |
| Реюз | `IShellNamespace`, `RefreshShellAsync`, гейты команд, чужое контекстное меню — всё уже написано | всё своё |
| Тестируемость | никакая: COM, живая система | полная, в Core, на подготовленных архивах |
| Столп «интеграция» | попадает точно | своя песочница — уступка, которую надо обосновывать |
| Риски | RCW и STA (по корзине уже наступали), непредсказуемая производительность на больших архивах, пути-сентинелы | zip-бомбы и `../` в именах записей — защита на нас; solid-архивы rar/7z дают квадратичное время при случайном доступе |

**Предлагаемый порядок:** сначала shell — он дешевле, покрывает zip
гарантированно и попадает в столп «интеграция с системой». Абстракцию в Core
при этом завести сразу такой, чтобы второй путь мог встать рядом, а не вместо
(и это ровно тот случай, когда абстракция оправдана по критериям из **O2**:
вторая реализация запланирована).

#### Что придётся решить до кода

- **Что такое «путь внутри архива».** `C:\x\pack.zip\a\b.txt` выглядит
  путём, но `Directory.Exists` на нём вернёт false, `Path.GetDirectoryName` —
  сработает, а `IFileSystem` — сломается. Нужен разбор в Core: найти первый
  сегмент, который является файлом с архивным расширением, и всё после него
  считать хвостом. **Кто это делает и где** — решить до кода, потому что
  спрашивать будут все: навигация, дерево, адресная строка, гейты команд,
  панель просмотра, буфер обмена.
- **Сколько кода узнает про архивы.** Опасность — что проверка «а не архив
  ли это» расползётся по тридцати местам, как чуть не расползлась проверка
  на корзину. Один предикат, одно место.
- **Дерево.** Разворачивается ли архив в дереве папок или только в списке?
  Проводник разворачивает. Стоит ли нам — вопрос: дерево у нас грузит детей
  синхронно (TECHDEBT), а открытие большого архива стоит дорого.
- **Сторож папки.** Внутри архива событий файловой системы нет; сторож надо
  либо отключать, либо вешать на сам файл архива (изменился — перечитать).
- **Панель просмотра.** Чтобы показать картинку из архива, нужны байты, а
  значит распаковка во временный файл — то есть тот же механизм, что у пункта
  «открыть». Миниатюры в плиточных видах — тот же вопрос, только умноженный
  на число файлов в папке. **Решить: показываем ли миниатюры внутри архива
  вообще** (вариант «только значок по расширению» честен и стоит ноль).
- **Временные файлы: политика.** Где живут (`%LOCALAPPDATA%\Wander\tmp`),
  когда убираются (при закрытии? при следующем запуске? по возрасту?), и
  главное — **что делать, если пользователь отредактировал открытый файл**.
  Проводник теряет правку молча. Варианты: (а) повторить поведение, но
  сказать об этом заранее; (б) заметить изменение и предложить записать
  обратно — это уже запись в архив, то есть отдельная фича. Умолчание —
  (а) с честной строкой в статус-баре, но это надо назвать вслух.
- **Извлечение = файловая операция**, значит по правилам проекта оно обязано
  пройти `SystemPathGuard`, залогироваться, положить `IUndoableAction` в
  `UndoService` (откат = удалить извлечённое) и показать прогресс с отменой,
  если файлов много. Отступление — только осознанное и записанное.
- **Архивы с паролем.** Минимум — внятно сказать «нужен пароль» и не
  притворяться, что папка пустая. Спрашивать пароль — уже не минимум.
- **Поиск, спутники, оценки внутри архива** — выключены. Записать это
  решением, а не оставить как «случайно не работает».
- **Битый архив** — сообщение вместо пустой папки.

#### Как расширять дальше (не в этот заход)

- Запись в архив: удаление и добавление файлов внутри.
- Создание архива из выделения (сейчас это делает пункт 7-Zip в чужом меню —
  и, возможно, этого достаточно; см. BACKLOG, «поднимать отдельные пункты
  расширения на верхний уровень»).
- Вложенный архив внутри архива.
- Просмотр содержимого архива прямо в панели просмотра, без входа внутрь.

---

### Q. Умный режим разрешения конфликтов — [P1, спринт 8]

При переносе или копировании файла на место существующего показывать не
«файл уже есть, заменить?», а **сравнение двух файлов**: чем именно они
отличаются, и — для картинок — как они выглядят.

#### Что есть сейчас

Два окна в `Wander.App/Conflict/`:

- **`BatchConflictDialog`** — задаётся один раз в начале, если конфликтов
  больше одного: «Заменить все / Пропустить все / Разбирать по одному /
  Отмена». Вопрос задаётся **вслепую**: пользователь ещё не видел ни одного
  файла, а уже решает за все.
- **`ConflictDialog`** — по одному файлу: имя, размер, дата у обоих, четыре
  кнопки. `StackPanel` в 48 строк XAML, фиксированная ширина 520,
  `ResizeMode="NoResize"`, цвета `#CCC` / `#666` / `#444` прямо в разметке,
  свой форматтер размера (обрывается на GB, для папки печатает слово вместо
  прочерка), даты — без пометки, какая новее.

`BatchExecutor` спрашивает через `IConflictResolver` — **pull-интерфейс**:
`StartBatch(int)` один раз и `Resolve(FileConflictInfo)` по мере
обнаружения. `DispatcherConflictResolver` затаскивает вызов на UI-поток,
воркер ждёт.

#### Что делаем

**Q1. Сравнение — что показать.** `FileConflictInfo` сегодня несёт две
`FileSystemEntry`. Добавляется рядом с ним расчёт различий:

- **Размер** — есть всегда, бесплатно. Различие подсвечивается, совпадение —
  тоже говорится вслух («размер тот же»).
- **Дата** — есть всегда. Показывается не просто дата, а **вердикт**:
  «источник новее на 3 дня». Это то, ради чего люди смотрят в это окно.
- **Контрольная сумма** — не бесплатна, поэтому: считается сама, если оба
  файла меньше порога (кандидат — 64 МБ), и по кнопке «Сравнить» — если
  больше. Считается в фоне, с отменой; окно живёт и без неё.
- **Превью** — для картинок обе миниатюры рядом. Берутся у
  `IIconProvider` (`GetIcon` с большим размером) — он уже кэширует и уже
  умеет RAW; отдельного декодера не нужно.
- **Совпадают полностью** — это отдельное состояние окна, а не «все три
  строки серые»: «файлы идентичны» и подсказка, что заменять нечего.

**Q2. Окно.** Переписать целиком: две колонки одинаковой ширины, превью
сверху, различия таблицей, кнопки внизу. Resizable, положение и размер
запоминаются, `Cancel` — кнопка по-умолчанию (столп «надёжность»), полная
клавиатурная навигация. Ни одного захардкоженного цвета — палитра берётся
оттуда же, откуда её берёт галерея (см. **O8**, это и есть его прикладной
результат). Все подписи — в `Strings.resx`.

**Q3. «Применить ко всем».** Вместо слепого вопроса в начале — галка
**в самом окне сравнения**, которую ставят, уже посмотрев на файл. И, если
пойдём дальше, вердикт-зависимые варианты: «пропускать все идентичные»,
«заменять, если источник новее». `BatchConflictDialog` в этом случае
исчезает, а не остаётся вторым способом сделать то же самое.

**Q4. Пропуск идентичных.** Настройка «не спрашивать про полностью
совпадающие файлы» — то, что убирает большинство вопросов вообще. **Ловушка,
которую надо назвать вслух:** для копирования пропуск идентичного файла —
безвредный no-op, а для **перемещения** — нет: файл должен исчезнуть из
источника. «Пропустить» при перемещении обязано означать «удалить источник»,
а не «ничего не делать», иначе настройка тихо превращает перемещение в
копирование.

#### Что придётся решить до кода

- **Чем считать сумму.** BCL (`SHA256` — аппаратно ускорен, но это хеш, а
  не CRC) против пакета `System.IO.Hashing` (`Crc32`, `XxHash3` — быстрее,
  но это зависимость). Пользователю в окне в любом случае показывается
  «контрольная сумма», а не имя алгоритма. Умолчание — BCL, потому что
  операция всё равно упирается в диск, а зависимость надо обосновывать.
- **Где считать.** В Core, обычным `File.OpenRead` — так уже читает весь
  `Preview/`. Но это ровно тот случай, из-за которого в **O7** заведено
  правило «когда `IFileSystem`, когда `System.IO`»: решить там, применить
  здесь.
- **Форма контракта.** Оставить pull-интерфейс (`Resolve` по одному, окно
  просто становится богаче) или перейти на «покажи весь список конфликтов
  сразу, верни решения». Второе даёт лучший UX для батча, но ломает случай
  «конфликт возник в процессе» (файл появился, пока мы копировали) и
  переписывает `BatchExecutor`. **Умолчание — оставить pull**, а список
  сделать позже, если он понадобится.
- **Папка против папки.** Сейчас `Replace` для папки делает delete + copy и
  **теряет непересекающееся содержимое цели** — записано в
  [TECHDEBT.md](TECHDEBT.md). Проводник делает рекурсивный merge. Богатое
  окно сравнения делает эту разницу заметной: у папки нет ни размера, ни
  суммы, а кнопка «Заменить» означает совсем другое, чем у файла. Минимум —
  назвать вслух, что произойдёт. Максимум — сделать merge, но это отдельная
  работа, не в этот заход.
- **«Оставить оба» без диалога имени.** Кнопка подставляет `(1)` —
  записано в TECHDEBT. Чинить здесь или оставить.
- **Медленный источник.** Сумма и превью по сети или со съёмного диска —
  это ожидание. Всё, что считается, обязано быть отменяемым, а окно —
  живым до того, как посчиталось.
- **Спутники.** Конфликт считается по группе, вопрос задаётся про основной
  файл. В окне сравнения это надо показать: «вместе с ним поедут .meta,
  .pp3» — иначе пользователь решает про один файл, а меняются три.

#### Порядок работ

Q2 (окно) можно делать первым и отдельно — он не трогает Core. Q1 добавляет
расчёт, Q3 и Q4 меняют поведение батча и требуют решения по контракту.
Разумный минимум на спринт: **Q1 + Q2**, остальное — по результату.

---

## Технические чистки

Между крупными вехами проекта останавливаемся и спрашиваем: «а структуру кода
надо ли освежить?». Это не TECHDEBT (там мелочёвка по ходу) и не BACKLOG (там
конкретные фичи). Это **code health pass** — ревизия структуры, тестов и
документации без новых пользовательских фич.

**Что смотрим на каждом проходе:**

- Не пора ли **разделить класс**? Признаки: длинный файл (>500 строк), несколько
  смешанных ответственностей, тесты на разные сценарии живут в одном файле.
- Не пора ли **слить два класса**? Признаки: один всегда таскает второй, второй
  без первого не используется, проектные зависимости циклические.
- **Public API**: какие методы / типы перестали быть нужны снаружи и могут уйти
  во `internal`.
- **Тесты**: что покрыто, что нет, нет ли мёртвых fakes.
- **Doc-комментарии**: появились ли публичные типы без XML-doc.
- **`TECHDEBT.md`**: пора ли выбрать пакет пунктов на разовое закрытие.

**Когда проводить:**

Не по календарю, а по большим вехам. Грубый ориентир: после фичи, которая
**меняет публичный API Core** (новые async-overload-ы, новый интерфейс,
изменение сигнатур service-классов).

### Пройденные проходы

Чистки #1–#3 — что в них вошло и что они дали — в [DONE.md](DONE.md),
«Технические чистки: пройденные проходы».

### Чистка #3 — что осталось

- ~~**Жесты дерева всё ещё в окне**~~ — закрыто шагом **O4.7**: и жесты, и
  разметка обеих панелей уехали в `Views/FolderTreesView`.
- **Пересмотреть `TreeNodeViewModel`** — он накопил bookmark-флаги,
  drives-флаги, lazy-load semantics. Возможно, `BookmarkNodeViewModel`
  отдельно.
- **Тесты на `NavigationController`** — сейчас покрытие идёт через
  `NavigationService` напрямую; обёрточный контроллер с командами и
  `AddressText` стоит покрыть, когда добавим `IDispatcher`-абстракцию.

### Чистка #4 — идёт сейчас

Условие было: следующий заход, который меняет публичный API Core. Галерея
его изменила — `FileSystemEntry.Rating`, `SortKey.Rating`,
`EntryComparers.Sort`, `ImageFolderProbe`, `RatingFilter`, `SidecarFormat`,
`ImageFormats` и три новых метода на `CompanionMetadataService`.

Повестка прохода выросла из обычной ревизии в отдельную секцию плана —
**см. [O. Чистка и реструктуризация логики](#o-чистка-и-реструктуризация-логики--p0-спринта-8)**.
Там же — правила захода, крупные шаги и то, что надо решить до начала.

Пункты, поставленные в повестку #4 и разложенные по шагам O:

- `CompanionMetadataService` вырос до трёх ролей и заодно обогащает целый
  листинг (`WithRatings`) → **O5**, ✅ сделано: `WithRatings` уехал в
  `Listing/RatedListing`, три роли записи остались вместе (причина — там же).
- Два фоновых прохода с одинаковой механикой отмены (`StartRatingPass` и
  листинг), к которым добавится третий → закрыто эпохой листинга в **O6.2**:
  общий тип оказался не нужен, достаточно одного числа и одной точки записи
  (`PublishRows`).
- Пересортировка после прохода по оценкам зовёт `EntryComparers.Sort` без
  платформенного компаратора имён, то есть тай-брейк ординальный, а не «как
  в Проводнике» → это **правка поведения**, а значит не в чистку: остаётся
  записью в [TECHDEBT.md](TECHDEBT.md) и делается отдельно.
- `MainViewModel` — на момент постановки было 2700 строк, к началу захода
  **4330** → **O4**, разложен на шесть тем; после пяти из них **3300**,
  к 2026-09-01 — **2949**, после **O11** — **2808**.

---

## Активные направления (P3 / обсуждается)

- Hex view в панели просмотра.
- Symlinks / junctions / hard links — отдельная фича (сейчас только `.lnk`).
- Темы (светлая / тёмная) — см. Roadmap в [CLAUDE.md](../CLAUDE.md).
  Подготовка сделана шагом **O8** (2026-09-01): все цвета — в
  `Resources/Palette.xaml`, тема — второй набор тех же ключей.

Просмотр zip-архивов переехал отсюда в секцию **P**: открыть архив как папку
и показать его в панели просмотра — одна механика.

---

## Связанные файлы

- [CLAUDE.md](../CLAUDE.md) — точка входа, навигация по всем докам, правила.
- [ARCHITECTURE.md](ARCHITECTURE.md) — как устроен код и механизмы.
- [DONE.md](DONE.md) — что уже реализовано.
- [REJECTED.md](REJECTED.md) — что решили не делать и почему.
- [TECHDEBT.md](TECHDEBT.md) — мелкие чистки.
- [BACKLOG.md](BACKLOG.md) — отложенные задачи.
- [README.md](../README.md) — описание для пользователя, сборка, лицензия.
- [.editorconfig](../.editorconfig) — codestyle.
