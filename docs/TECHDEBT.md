# Wander — технический долг

Мелочь, замеченная по пути и сознательно отложенная: одна-две строки, место
и суть. Чистится заходом, не вперемешку с фичами. Перед релизом список
разбирается целиком (PRERELEASE.md, шаг 2): критичное — в PLAN, серьёзное —
на обсуждение, остальное остаётся. Закрытое удаляется (история — CHANGELOG
и гит). Последний разбор — 2026-09-18 для 0.4.0.

## QA-харнесс

- **Окно на кадр при прогонах 2026-09-16** — одно за шесть сценариев, какое
  — неизвестно. Сторож `App.WatchWindowsWhenHeadless` пишет окна, меню и
  тултипы; прогон 2026-09-17 (весь набор + soak 30 мин) строк `WARN HEADLESS
  … ON SCREEN` не дал, но глазами его не смотрели — сторож не опровергнут и
  не подтверждён. Кандидаты: первая инициализация WebView2 и `broken.lnk` в
  `preview-formats`. Не найдётся строкой — окно не WPF (процесс WebView2,
  шелл): ловить `EnumWindows` по потокам процесса на каждом шаге.
- **`navigate` на файл даёт `ERROR Enumerate failed`** (2026-09-17) — то же
  с путём файла в адресной строке: вместо «это файл» — ошибка в логе.
  Всплыло на `nested.tar.gz` при WinRAR; сам сценарий `archives` с
  2026-09-24 пропускает такие шаги (`ifOpensAsFolder`, QA.md).
- **`tree-bookmarks` красный с блока 2** (прогон 2026-09-24): после `undo`
  переноса `renamed-in-list` из `raw` обратно опустевшая ветка `raw`
  закрыта, и `assert-tree … exists: false` падает в `FindTreeRow` («is
  closed»). Те же шаги проходили на 0.4.1 (22.09): старое дерево держало
  ветку без детей раскрытой. Решить при финализации блока 2: задумано ли
  (как Проводник — без подпапок нет и раскрытия) — тогда закрытая строка
  без шеврона для `exists: false` значит «нет»; нет — баг модели.
- **`preview-formats` не знает веток 0.5** (прогон 2026-09-24): TGA,
  карточка программы (`.exe` / `.dll` / `.msi`), документы текстом из zip
  (`.docx`, `.odt`, `.xlsx`, `.pptx`, `.epub` — в песочнице `docs` лежат,
  шагов нет). TGA — генератором (формат простой), exe — копия системного в
  песочницу.
- **Клавиатура за ушедшей строкой харнессом не проверяется** (2026-09-21):
  окно харнесса никогда не `IsActive`, `FileListView.OnCurrentRowLeft` ждёт
  `Activated`. Рамка и стрелки — только руками (QA.md, «Фокус»).
- **`ui.stall` 4–8 с на первом экране под харнессом** (Debug; 4,1 с утром и
  6,4 с вечером 2026-09-02, первый экран `C:\` 4,8 с). Похоже на холодный
  JIT и первые shell-вызовы дерева. Смотреть на релизной сборке (PRERELEASE,
  шаг 5); если и там секунды — разбирать по `PERF`-строкам первой секунды.
  По Debug-цифрам не чинить. Прогон 2026-09-24: худший 1,1–2,2 с против
  6,3–8,7 с на 0.4.1 — ветки панелей читаются в фоне (блок 2); кандидат
  закрыть после релизной сборки.
- **`focus-keys`: шаги 2026-09-03** (`Enter` в таблице, многовыделение между
  видами) ни разу не прогонялись — при финализации, задевающей фокус.
- **Сценария на сплит панели просмотра нет** (2026-09-15): шаг «выделить два
  снимка, скриншот» в `preview-formats`.
- **`SYS` и `PERF` пишут числа в локали процесса** (`cpu=8,0`) — парсить с
  оглядкой на запятую.
- **Отчёт печатает «SYS (5)»** при двухстах строках: `Section` считает
  переданный хвост. Числа целиком — в `metrics.json`.
- **`SandboxBuilder.Remove` падает на junction-петле** (2026-09-02).
  `EnumerateDirectories(AllDirectories)` уходит сквозь `deep\l01\l02\loop` до
  `PathTooLongException`: `selfcheck --dir` и `run --rebuild` на песочнице с
  `deep` — exit 70. Не всплывало: профиль `deep` не использует ни один
  сценарий (предел 64, `FolderStatistics`, обход поиска никто не гоняет).
  Чинить `FileSystemEnumerable` с `ShouldRecursePredicate` = «не reparse
  point» и сценарием `deep-names`.
- **`HarnessApp` вливает словари ресурсов руками** (Palette, MenuStyles) —
  новый словарь в `App.xaml` харнесс не увидит.

## Список файлов и навигация

- **`List_SelectionChanged` пересобирает список выделенного на каждое
  изменение** (2026-09-03). `ApplyDelta` добавляет по одному, каждое — новый
  `List<FileSystemEntry>` из `SelectedItems`: рамка на пять тысяч файлов —
  квадрат. Копить до `Dispatcher` или снимать подписку на время массовой
  установки. Рядом — PLAN AB.
- **Какой флаг превращал эхо выделения в дереве в навигацию** (2026-09-02).
  Прыжок из архива обратно при выключенном `TreeKeyboardNavigates` возможен
  только через `_treeClickNavigates` в `FolderTreesView` (сбрасывается на
  `PreviewMouseLeftButtonUp`); где теряется отпускание — не найдено.
  Страховка `IsSyncingSelection` закрывает симптом.
- **`IsCutRow` — `MultiBinding` из двух привязок в каждом контейнере** (R3,
  намеренно). По-настоящему — attached-свойство плюс хук подготовки
  контейнера в обеих семьях панелей; ~80 строк, в замеры не попало.
- **`ui.restore` до 500 мс** — `FileListView.FocusRow`: `ScrollIntoView` +
  `UpdateLayout()` синхронно реализует контейнеры с декодом иконок (p50 97 /
  p90 504 мс). Честно — фокус по `ItemContainerGenerator.StatusChanged`.
- **`SelectExternalPath` делает `GetEntry` на UI-потоке** — строка уже есть
  в `Entries`.
- **Бросок из обработчика `CollectionChanged` глотается** (2026-09-22).
  `SyncEntries` идёт из `SearchController.ApplyAsync`, вызываемого как
  `_ = ApplyAsync()`: исключение панели посреди уведомления (краш
  2026-09-22) оставляет план полуприменённым, остальные панели-слушатели
  уведомления не получают, а в лог попадает лишь «Unobserved task
  exception» из финализатора — минуты спустя. Ловить в `ApplyAsync`,
  писать сразу, после сбоя — перечитать папку.
- **`ListingDiff` — квадратичный худший случай** («половина строк уехала
  далеко»; полный разворот ловит `IsWholesaleChange`). Честная O(n log n) —
  дерево Фенвика; по логам не встречалось. Уведомления (ревью 2026-09-21):
  `MaxReconciledChanges` считает только ушедшие и пришедшие — `Move` и
  `Replace` (`git checkout` переписал даты тысяч файлов) не ограничены; одна
  строка, уехавшая далеко по дате, — «смена сортировки», сброс прокрутки.
  Дёшево: `edits.Count > MaxReconciledChanges` → перестройка.
- **Правка на ходу и тик сторожа** (ревью 2026-09-21): `CommitRename` снимает
  `RenamingPath` до `await`, `DecideWatchTick` окна не видит — перечитывание
  посреди переименования на миг выделяет соседа. Считать идущие
  переименования занятостью в тике.
- **Ушла строка с рамкой, остальное выделенное осталось** (ревью 2026-09-21,
  не проверено): `ReconcileEntries` идёт в `AdoptSelection`, `CurrentRowLeft`
  не поднимается — клавиатура может остаться нигде. `CaretPath` = первая
  уцелевшая и `CurrentRowLeft` в той же ветке; сначала посмотреть глазами.
- **`keyboardLeftBehind` и обычное перечитывание** (ревью 2026-09-21):
  `Ctrl`+клик снял выделение со строки с клавиатурой; перечитывание
  возвращает выделение, клавиатура прыгает на первую выделенную. Нужен
  признак «приземление сдвинуло выделение» из `ApplyArrival`.
- **`IconImageCache` растёт по путям у `Small` / `Normal`** (2026-09-21):
  ключ — `(размер, путь)`, каждая пройденная строка оставляет `BitmapImage`
  (~4 КБ), `ThumbnailBudget` считает только `Medium` / `Large`. Ключ по
  `byte[]` (общий на тип) снял бы рост и повторный декод; `Forget` не чистит
  `_thumbOrder`.
- **`Ctrl` + стрелки на границах сетки** — двигают курсор без выделения, а
  отдельного курсора нет (`Shift` обошёлся якорем). Внутри сетки делает WPF,
  края отданы ему осознанно (комментарий в `TryGridStep`). Нужен свой
  курсор, синхронизированный с фокусом контейнера.
- **`Shift`+клик считает диапазон от нашей каретки**, а не от якоря WPF:
  после клика по пустому месту расходятся.
- **Адресная строка берёт фокус отложенно** — `BeginAddressEdit` через
  `Dispatcher.BeginInvoke`; проверка сразу после `Tab` / `Alt+D` видит
  старый фокус.
- **Две ошибки привязки при фильтрации** — `Error: 4` про
  `Horizontal/VerticalContentAlignment` на переиспользуемом `ListBoxItem`;
  задать оба на самом `ListBox`.
- **`AsyncIcon` стартует лишнюю загрузку на дефолтном размере** — `IconPath`
  применяется раньше `IconSize`. Coalescing через `Dispatcher`, если заметно.
- **Рамка выделения не тянется за экран** — автоскролл по краю +
  прямоугольник ячейки по индексу. Рядом — PLAN U2.
- **`TileLayout` предполагает одинаковые ячейки** — подпись `Height`, высота
  строки `шрифт × 1,35`, не `FormattedText`; экзотический шрифт обрежет.
- **Пересортировка по оценке — ординальный компаратор имён**
  (`StrCmpLogicalW` приватен в `SystemIOFileSystem`): `IMG_10` перед `IMG_9`.
  Компаратор в `IFileSystem` или сортировка листинга наверх.
- **`Alt` над полосой фильтра гасит клик молча** — обычный клик или слово в
  статусе.
- **Вторая строка плитки — `EntryKind.ToString()`** (замечено 2026-09-23,
  блок 2, шаг 10): у неоценённого «File» / «Directory» по-английски, мимо
  `Strings.resx` (`TileSecondLineConverter`; было и до звёзд B28).
- **Рамка в Таблице — угол по средней высоте строки** (блок 2, 2026-09-23):
  `RubberBandController.PixelOffset` = `VerticalOffset × ActualHeight /
  ViewportHeight`; `DataGrid` прокручивает строками, а в `ActualHeight` ещё
  заголовок и неполная строка — на длинной протяжке угол может съехать на
  долю строки. Честно — прямоугольник строки по индексу. Глазами — QA,
  «Рамка выделения за край».

## Дерево и закладки

- **Раскрытие ветки перечисляет уровень синхронно** — `EnsureLoaded` /
  `ReadChildFolders` на диспетчере (`TryExpandToPath` требует детей сразу).
  `nav.trees` p50 ~110 мс; на спящем HDD — пауза. Честно — асинхронный спуск.
- **`LoadRoots` ходит на диск до окна** — `HasSubdirectories` по каждому
  диску синхронно; спящий HDD или отвалившаяся шара задержат первый кадр.
- **Архив не появился узлом дерева, пока в него не зашли** (2026-09-04,
  `D:\Books\…\Библия C#, Флеменов CD.rar`). По коду `ReadChildFolders` берёт
  архивы из того же `Enumerate` по расширению, фильтр один —
  `Visibility.Allows`. Не воспроизведено; проверить: шеврон или навигация,
  атрибут «скрытый», появляется ли по `F5`.
- **Сторож смотрит только на текущую папку** — подпапка, созданная снаружи в
  другой развёрнутой ветке, доезжает по `F5`. Второй сторож или строка в
  GUIDE.
- **Закладка на пропавшую папку сереет только с пересборки панели** —
  `TryBuildFolderNode` решает один раз в `BuildBookmarks`; после «удалить
  папку в корзину» строка живая, клик ведёт в «папка удалена» (2026-09-17).
  Дёшево — пересобрать панель, если удалённое — закладка; честно — сторож на
  папки закладок.
- **`TreeNodeViewModel._placeholder` подписывают заново на каждый узел** —
  обработчики на общей статической заглушке копятся; с 2026-09-22 ещё и на
  каждом возврате шеврона (`ReprobeChevronAsync`).
- **`F2`, возврат клавиатуры и меню панели не покрыты харнессом**
  (2026-09-22): клавиша `F2` и редактор над строкой, маршрут
  `MainWindow.StartRename`, возврат рамки после ухода строки
  (`Tree_IsKeyboardFocusWithinChanged`), меню по правому клику и
  перетаскивание мышью — WPF-ввод, харнесс его не эмулирует; всё, что под
  ними, идёт шагами `tree-rename` / `tree-target` / `assert-tree` / `drop`.
  Покрыть можно: `key F2` на сфокусированной строке панели (синтетическая
  клавиша уже проходит `OnPreviewKeyDown` окна) плюс проверка
  `FolderTreesView.IsRenaming`; `key Delete` после `tree-target` с
  `assert-tree selected` на родителе. Нельзя без настоящего ввода: сам
  адорнер и его размер, клик мимо, drag.
- **Ширина строки закладок не растёт по содержимому** — вынести «…» в шаблон
  контейнера.
- **Drop на закладку = «добавить в закладки»**, не «скопировать в папку».
- **Левый клик по узлу дерева не делает его целью клавиатурных операций** —
  правый делает (`SelectExternalPath`).
- **Строка панели без имени для диктора** (замечено 2026-09-23, блок 2): у
  `TreeNodeViewModel` нет `ToString`, в шаблоне `FolderPanelList` нет
  `AutomationProperties.Name` — диктор, вероятно, читает имя класса (так же
  было у `TreeView`). Проверить Inspect'ом; лечится
  `AutomationProperties.Name` по имени строки на контейнере.

## Поиск

- **Спутники всплывают отдельными строками в результатах фильтра** — проход
  поиска не сворачивает (`CompanionResolver`). Вместе с «путь у результата»
  (BACKLOG).
- **Галочка «в подпапках» описывает только окно** — фильтр всегда достаёт
  подпапки.
- **Окно поиска не помнит геометрию** — второй блок как `WindowGeometry`.
- **Папку поиска нельзя выбрать, только увидеть** — кнопка обзора.
- **Побайтовый поиск читает файл дважды** — `PlainTextExtractor`, потом
  `MatchesRawBytes`: 748 мс против 60 на 523 файлах.
- **`PruneMissingAsync` — stat на каждую строку**; затронутые пути знает
  `BatchExecutor`.
- **`SearchController`: `FilteredChanged` может выстрелить после `Reset()`**
  — check-then-act между токеном и `Invoke`. Поколение запроса вместо токена.

## Панель просмотра и миниатюры

- **Аудио без обложки занимает место в кэше как с обложкой** —
  `BuildCacheKey` по пути; после неудачи — ключ по расширению.
- **Миниатюра ярлыка не сбрасывается при замене оригинала** — подмешать
  mtime цели.
- **Оценку предлагают только картинкам** (`OfferRating`, `ImageFormats`) —
  осознанно; видео / PDF — одна строка.
- **Перепись папки обходит дерево целиком, кэша нет** — шара или корень
  диска считаются секунды. Кэш по пути + mtime или пересчёт по кнопке.
- **WebView2 init 1–2 с при первом обращении** — прогрев на старте, если
  панель включена.
- **AvalonEdit без XAML-грамматики** — xaml как plain XML.
- **HTML: JS включён** — сеть и попапы отрезаны, скрипты локального `.html`
  исполняются (`IsScriptEnabled` глобальный, PDF-viewer без него не живёт).
- **Иконки грузятся с MTA** — apartment-threaded провайдеры могут вернуть
  пусто. Замер 2026-09-21: `GetOverlayIndex` (`SHGFI_OVERLAYINDEX`) с пула
  ждёт общий host-STA — долгий вызов там держит все четыре слота `AsyncIcon`
  (`slow shell load` с одинаковым временем). Корзину оттуда убрали; без неё
  то же на `D:\Dev\lekta\Wander` (~1,9 с), виновник — вероятно,
  overlay-обработчик. `GetImage` не блокируется.
- **Слот значка не отпускается при уходе из папки** (2026-09-18) — начатый
  shell-вызов не отменить, зависшие значки держат следующую папку.
- **Окно совпадений: значки мигают при прокрутке** (ревью 2026-09-21).
  `AsyncIcon` на `ConflictRowViewModel` не знает, папка ли строка;
  `TryGetCachedIcon(isFolder: null)` отвечает только путевыми ключами. Брать
  `Kind` стороны по `IconPath`, но заглушки `Unknown()` из `BatchExecutor` /
  `ExtractionService` всегда `File` — для папки нужен null.
- **Несколько массивов на один общий ключ значка** (ревью 2026-09-21):
  одновременные промахи по `ext|.txt|Normal` кладут разные `byte[]`,
  «проигравшие» декодируются заново на UI-потоке. `Store`, который оставляет
  первый.
- **`AsyncIcon.RowIsFolder` зовёт `Archives.Of` на UI-потоке** для узла
  дерева — `File.Exists` на перерисовку. Флаг корня архива хранить на узле.
- **Папки «Таблицы» и миниатюры делят бюджет 512** — в папке на 600 подпапок
  значки вытесняются и мигают при прокрутке назад.
- **`TryGetCachedIcon` для `Medium` ходит на диск под замком провайдера**
  (`IsThumbnailable` → `Directory.Exists`, у записи архива — `File.Exists`)
  на UI-потоке.
- **Пиксели в футере — как лежат в файле**: при ориентации 5–8 панель рисует
  портрет, строка пишет `1620 × 1080` (и `ShotSummary`). Решить, чей счёт.
- **Кадр под зажатой лупой, если загрузка упала не отменой** (ревью
  2026-09-21, пути сейчас нет): `finally` в `UpdatePreviewAsync` считает
  «`Kind` всё ещё Image» успехом. Сравнивать оставленную картинку по ссылке.
- **Балл резкости RAW зависит от размера вшитого превью** (блок 1,
  2026-09-22). Меряется по самому большому JPEG внутри файла: у CR3 это
  полный кадр, у некоторых NEF / ARW — превью в 1024 px, а на уменьшенном
  всё читается резким. Сравнивать числа между камерами нельзя, внутри одной
  серии можно. Лечится чтением с матрицы (BACKLOG) или отметкой «мерено по
  превью N px» в подсказке.
- **`ImageDecoder.ApplyOrientation`: 5 и 7, похоже, перепутаны** (блок 1,
  2026-09-22). `Mirror(90)` — отражение, затем поворот по часовой: это
  transverse, EXIF 7; для 5 нужен transpose (`RawThumbnail` через WIC
  даёт его). Камеры пишут 1, 3, 6, 8 — не проявлялось. `AfGeometry.Orient`
  сделан по EXIF; проверить стендом на файле с 5 и 7.
- **TGA, который не разобрался, уходит в шелл** (блок 5, 2026-09-22):
  `.tga` в `_thumbnailableExtensions`, и при провале `TgaThumbnail` —
  `LoadShellBitmap`, то есть запись значка в `thumbcache_*.db`, от которой
  `LoadJumboImage` бережёт. Провал TGA — сразу значок по расширению.
- **`Ctrl+Shift+F` не работает в просмотре кода** (`MainWindow`,
  `IsCodeEditorFocused`): защита ради «своей панели поиска AvalonEdit»,
  которую никто не ставит (`SearchPanel.Install` нет).
- **Поиск в панели: совпадение правее края не видно** (ревью 2026-09-24) —
  в тексте без переноса `ShowMatch` прокручивает только по вертикали
  (`ScrollToLine`); код (AvalonEdit `ScrollTo`) и документы с переносом
  этим не страдают.

## Контекстное меню (shell)

- **Первый запрос чужого меню синхронный** — `QueryContextMenu` на
  UI-потоке: 0,4–1,1 с первый раз, дальше 80–260 мс. Асинхронно — риск
  прыжка меню.
- **Глаголы запрашиваются для всех детей подменю** — обрыв на первом
  пробовали, откатили.
- **Иконки чужих пунктов конвертируются все сразу** — 30+ `ToPng` на клик.
- **Owner-drawn пункты пропускаются** — полная починка — нативный `HMENU`
  через `TrackPopupMenu`.
- **Раскладка пунктов расширений — эвристика** («подменю без канонического
  глагола» = системное); `IContextMenu` не сообщает CLSID.
- **Глагол пункта Defender не проверен** — остаётся на верхнем уровне.
- **Чёрный список по подписи** — одинаковые подписи выключатся вместе, смена
  языка системы отвяжет галочку.

## Файловые операции и корзина

К блоку «Надёжное копирование» (PLAN K) относятся первые пять.

- **`MoveEntry` cross-volume — нет уборки при частичном сбое**: частичная
  копия или дубликат при неудавшемся delete. Отмена — не этот случай
  (`CopyFileEx` убирает недописанное, частичная папка кладётся в undo).
  Ожидание занятого для папки — только в пределах тома (2026-09-21).
- **Безвозвратное удаление не ждёт занятый** — `DeleteForGood` проверяет
  только заявки операций.
- **Откат не отпускает панель просмотра** — `ReleasePreview` стоит перед
  удалением, переносом, переименованием, не перед `Ctrl+Z`. Шире — PLAN,
  «`Ctrl+Z` не ждёт занятых».
- **Папка с занятым файлом внутри — «занято» ли это для Windows** (не
  проверено): `Directory.Move`, вероятно, отвечает `ERROR_ACCESS_DENIED`, и
  `BusyGate.Retry` не ждёт. Стенд.
- **Строки отката: английское `Description`** — `StatusUndone` показывает
  `IUndoableAction.Description` («delete of 3 items»). К PLAN AD5.
- **Извлечение: отмена — между элементами**. Бар внутри элемента едет
  (`IFileOperationProgressSink.UpdateProgress`, процентом), отмена — только
  `PreCopyItem`.
- **Конфликты при извлечении — по верхнему уровню**: папка поверх папки
  спрашивает один раз, дальше решает шелл. Как у копирования папки на папку.
- **Извлечение уплощает пути** — цель = `targetFolder + имя листа`; две
  записи с одним именем из разных подпапок метят в один файл. Руками так не
  выделить.
- **Conflict: «Оставить оба» = auto `(1)`**, без диалога имени.
- **«Извлечь рядом» распаковывает пары для сравнения зря**
  (2026-09-23): `ExtractionService.UnpackForComparison` идёт до
  `ResolveAll` при любом резолвере, а `FixedConflictResolver` байты не
  смотрит — до 32 распаковок впустую. Спрашивать резолвер, нужно ли ему.
- **Окно совпадений строит вьюмодель на каждую пару сразу** — мерить на
  живой большой папке.
- **Проверка длинного пути обходит папку на каждом шаге ожидания** (ревью
  2026-09-21) — `ShellRecycleBin.TooLongForBin` из `SendMany`, `RetryMany`
  шлёт каждые 200 мс; сотни тысяч файлов — десятки секунд. Запоминать на
  операцию.
- **Исключение посреди корзины теряет откат уже отправленного** (редко):
  `DeleteManyCore` уходит до `PushComposite`. Ловить по прогону, шаги отката
  собирать из готового.
- **Откат переименования с обменом имён — цепочка зависимых шагов**:
  `UndoService.Unwind` считает их независимыми; сбой в середине оставляет
  `имя.<hex>.wander-tmp` на виду.
- **«Восстановить» в корзине — на UI-потоке** (`RestoreFromRecycleBin`): до
  ~270 мс на элемент, без прогресса и отмены.
- **Возврат из корзины — прогон на элемент** — ~15 мс на файл, откат 5000 —
  больше минуты. Пачкой — 3 мс (стенд). Нужен `IRecycleBin.RestoreMany` и
  размотка подряд идущих `DeleteAction` одним вызовом. COM — заход Фабле.
- **Порог длинного пути для корзины не измерен** — 259 по документации,
  стендом проверены 263 и 349. Мерить — только на машине без человека
  (PLAN T).
- **Три копии помощников `IShellItem`** (`CreateItem`, `DisplayName`,
  `Release`, `CreateFileOperation`) в `ShellArchiveFolder`,
  `ShellRecycleBinFolder`, `ShellRecycleBin` — место им в `ShellItemInterop`.
- **`SystemPathGuard` фаза 2** — warn вместо block для Program Files и чужих
  профилей, guard на запись внутрь, переопределение в настройках.
- **Корзина: статичная иконка** — empty / full раз за сессию; инвалидировать
  при Restore / Empty или `SHChangeNotifyRegister`.
- **Одиночные `Copy` / `Move` / `Delete` синхронные** — зовут только тесты.
- **Undo не переживает рестарт** — стек в памяти.
- **`state.json` без миграций** — старый файл → `new AppState()`. Pre-1.0 ОК.

## Свои действия

- **Папка pandoc не сверена** — `WindowsToolLocator` смотрит
  `%LOCALAPPDATA%\Programs\Pandoc`; куда ставит per-user MSI, не проверялось.
- Слова из XMP / IPTC при конвертации — PLAN, блок 7.

## Код и инфраструктура

- **Типографские знаки в комментариях кода — 1649 в 237 файлах**
  (`MainViewModel` 129, `SystemIconProvider` 69, `PreviewController` 44).
  Одним скриптом, не задевая литералы и `Strings.resx`.
- **`Core/Actions/` нет в дереве папок ARCHITECTURE** (замечено 2026-09-22,
  блок 3) — каталог действий описан текстом, а в списке папок Core его
  строки нет; дописать строку рядом с `Companions/`.
- **`deps.ps1` видит только `using`** (блок 5, 2026-09-22): полное имя
  (`Views.CompareWindow` из `Conflict`) и обращение к корневому
  `Wander.App` (`MainViewModel` из `Views`) в граф не попадают — цикл
  `Conflict → Views → Dialogs → Conflict` сканер пропустил, развёрнут
  руками через `IPairViewer`. Искать и квалифицированные имена.
- **`BitmapPixelSize` в ресурсах `MainWindow.xaml` не используется** —
  объявлен, привязок нет (с блока 5 это `IMultiValueConverter`).
- **`ReviewThumb.Drop()` прячет событие `UIElement.Drop`** (CS0108,
  замечено 2026-09-22) — единственное предупреждение сборки; переименовать
  метод или объявить `new`.
- **Порядок членов никто не сверяет** — сканер (138 → 39 нарушений) жил в
  scratchpad; шаг в `check.bat` или режим `metrics.ps1`.
- **Восемь градаций серого текста в `Palette.xaml`** (`#333` … `#9A9A9A`) и
  **копия шести цветов выделения в `GalleryPalette`** (нарочно: VM не лезет
  в `FindResource`) — сводить вместе с тёмной темой.
- **Ретрай буфера обмена спит на UI-потоке** — `WithClipboard`, до 5 × 20 мс.
- **Crash-сервисы** — GitHub issue + локальный zip; осознанно «ничего не
  уходит без действия».
- **COM-интерфейсы объявляют `out`-параметры ненулевыми**
  (`ShellItemInterop`, `SystemIconProvider`, `ShellDataObject`,
  `FilterTextExtractor`) — Rider считает `item is null` всегда ложным.
  `out IShellItem? item`: аннотация, маршалинг не трогает.
- **Рабочий набор под харнессом +10–15 % против 0.3.1** (2026-09-17, Debug):
  пик WS +40…80 МБ по всем сценариям (smoke-walk 617 против 555–572,
  preview-formats 630 против 569–585), soak-30 786 против 690, прирост за 29
  минут +85 против +76; хэндлы и gen2 те же. Порог QA (+20 %) не пройден.
  Кандидаты: вторая панель просмотра, каталог действий, Restart Manager.
  Сверить `SYS ws=` живого релизного прогона с PERFORMANCE.
