# Wander — QA

Как проверяется продукт: уровни проверки, песочница данных, матрица
«трогал → проверь», чек-листы, предрелизный прогон, метрики, план
автоматизации и роль Claude в нём. Тесты живут только на Core; всё выше
(вьюмодели, XAML, фокус, DnD, COM) покрыто компилятором, smoke и этим
документом.

## Уровни проверки

| # | Уровень | Ловит | Не ловит | Есть |
|---|---|---|---|---|
| 1 | Тесты Core (xUnit, фейки) | логику, машины решений, гонки в контроллерах | UI, COM, диск | да, `check.bat` |
| 2 | Сборка, `format`, `check-strings`, `deps.ps1` | компилируемость, ключи ресурсов, циклы папок | всё поведение | да |
| 3 | Smoke (`--smoke`) | XAML, ресурсы, регистрация, первый кадр и листинг | жесты, содержимое | да, `check.bat run` |
| 4 | Консольный харнесс на класс | конвертеры, `TreeNodeViewModel`, контроллеры, замер одной операции | окно | частично: `Wander.Harness selfcheck` (генераторы и фикстуры против читателей: RAW, EXIF, zip-документы, кодировки, теги WAV / mp3 / flac / m4a, меши); разовые — в scratchpad |
| 5 | Оконный харнесс за экраном | фокус, выделение, rename, раскладка, приземления, скриншоты, PerfLog / GC / память — на песочнице | обмен с системой, настоящий ввод ОС | **есть**: `tests/Wander.Harness`, ниже |
| 6 | UIA (FlaUI) на релизном exe | настоящий ввод, поставленный бинарник, DnD с Проводником | ничего сверх 5 по коду; хрупкие селекторы | нет; только Sandbox / VM |
| 7 | Человек по чек-листу | «прыгает или нет», обмен с системой, внешний вид | масштаб | чек-листы ниже |
| 8 | Claude через 5 / 6 | сценарии, оценка скриншотов и логов, разведочные прогоны | то же, что 5 / 6 | план ниже |

Правило: проверка, которую можно сделать тестом Core или харнессом, уходит
из чек-листов в код.

## Где это крутится

Всё локально, на этой машине. Никакого «сервера с тестами» нет и у Claude
тоже: он запускает те же команды, что человек, — `dotnet build`,
`check.bat`, `Wander.Harness.exe` — через оболочку на этом компьютере и
видит их вывод. Отсюда всё остальное: прогон занимает столько же времени,
сколько занял бы руками; окно харнесса — настоящее окно этой Windows,
просто за краем экрана; всё, что прогон пишет, ложится на этот диск.

| | Откуда | Куда |
|---|---|---|
| Данные, по которым ходят | генерируются в `%TEMP%\wander-sandbox\<имя>` (фото, RAW, документы, 5000 файлов) | там и живут между прогонами; `--rebuild` пересобирает |
| Чего генератор не умеет | `tests/Fixtures/` в репозитории | копируется в песочницу по расширению |
| Состояние чужой сборки | `tests/Fixtures/state/<версия>.json` | data-dir прогона, до старта приложения |
| Результат прогона | — | `artifacts\<сценарий>-<время>\`: `report.md`, `screenshots\`, `metrics.json`, `data\` с логом и `state.json` этого прогона |

`artifacts\` в `.gitignore` — это мусор прогонов, а не история; нужное из
него переезжает в доки словами и числами. В `%LOCALAPPDATA%\Wander` не
пишется ничего: data-dir прогона подменён переменной, и настоящее состояние
машины прогоны не трогают. Единственное касание системы — корзина: удалённое
уезжает в настоящую, сценарий возвращает его `undo`.

## Песочница данных

Живые прогоны идут по сгенерированному дереву, не по файлам машины.

**Изоляция.** Всё, что приложение пишет для себя (`state.json`, `logs`,
`thumbs`, `crashes`, `WebView2`), идёт через один корень
`AppPaths.DataRoot`: `--data-dir <путь>`, `--portable` (папка `data` рядом с
exe), переменная `WANDER_DATA_DIR`, иначе `%LOCALAPPDATA%\Wander`. Харнесс
ставит переменную на `artifacts\<run>\data` — настоящий `state.json` и
кэши машины не трогаются (в логе строка `Data root: … (env)`). Единственное
касание системы — корзина: удалённые файлы песочницы попадают в настоящую,
сценарий возвращает их `undo`.

**Генератор** — `Wander.Harness sandbox <dir> [--profiles …] [--photos N]
[--big N] [--raw N] [--raw-mb N]`; `run` строит песочницу сам в
`%TEMP%\wander-sandbox\<имя>`, если её нет (`--rebuild` пересобирает).
Позже — VHDX (`Mount-DiskImage`: съёмный том, извлечение) и SMB-loopback
(`\\localhost\share`: UNC, таймауты).

| Профиль | Что внутри | Что проверяет |
|---|---|---|
| `photos` (есть) | JPEG 1620×1080 с EXIF-ориентацией 1 / 6 / 8 / 3 по кругу; стрелка и подпись нарисованы так, что после поворота смотрят вверх — сбой ориентации виден на скриншоте; `.xmp` у каждого третьего, `.pp3` у каждого пятого; `sub\` с 10 снимками; `notes.txt` | галерея, автовыбор, фильтр, оценки, миниатюры, `ListingDiff` |
| `raw` (есть) | синтетические **CR3** (ISO-BMFF: `moov` → Canon `uuid` с `CMT1` / `CMT2` TIFF, top-level preview `uuid` с `PRVW` JPEG, `mdat` шумом до `--raw-mb`) и **DNG** (IFD0 с превью и ориентацией, Exif IFD, sub-IFD с несжатой полосой шума); `selfcheck` сверяет `RawPreviewExtractor` и `MetadataExtractor` | RAW-путь миниатюр и превью, ориентация, размер файла |
| `big` (есть) | N файлов по 1 КБ + 50 подпапок | виртуализация, `list.apply`, `ListingDiff`, поиск 5000 |
| `deep` (есть) | 70 уровней + junction `l01\l02\loop` → `l01` (`mklink /J`) | предел 64, `FolderStatistics`, `CountAndSum`, обход поиска — **сценария на него пока нет**, и `Remove` на нём падает (TECHDEBT) |
| `names` (есть) | кириллица, эмодзи, RTL, диакритика, точки, ведущие пробелы, имя 200 символов, путь > 260 | type-ahead, rename, guard, крошки |
| `docs` (есть) | docx / xlsx / pptx / odt / epub (zip + XML), fb2 с обложкой-`<binary>`, md с таблицей, rtf (кириллица `\'xx` под `ansicpg1251`), pdf в один текстовый объект с ручным xref, html с картинкой по http и `fetch()`, txt в UTF-8 / UTF-16 / 1251 / 866 — в прозе каждого слово `needle42` и больше нигде в песочнице; плюс `.doc` / `.pdf` / `.rtf` / `.docx` / … из `tests/Fixtures/`, что там есть | поиск по содержимому, экстракторы, панель, `EncodingProbe`, изоляция WebView2 |
| `media` (есть) | генерируются `tone.wav` (RIFF `LIST`/`INFO`, теги в 1251), один куб тремя файлами `cube.stl` / `.obj` / `.gltf`, `cover.jpg` рядом с треками; из `tests/Fixtures/` по расширению — mp3, flac, ogg, opus, m4a, m4b, aac, wma, mp4, webm, gif, webp, heic (чего нет — строка `missing` в сводке, не ошибка) | аудио-карточка, теги, обложка из папки, видео-миниатюра, 3D |
| `code` (есть) | `Program.cs`, `Window.xaml`, `pipeline.yaml`, `change.diff`, `build.bat` **в 866** с кириллицей, Unity `Settings.asset` (YAML) и `Binary.asset` (блоб) + `.meta` на оба | подсветка, `TextProbe` на одном расширении в две стороны, спутники |
| `attrs` (есть) | `+H`, `+S`, `+H+S`, read-only, `themed\` с `desktop.ini` и `+S` на самой папке, `denied.txt` под `icacls /deny *S-1-1-0:(R)` (по SID: на локализованной винде имя другое), `locked.txt` — обычный файл, хэндл берёт шаг `fs lock` | видимость, удаление, «файл занят», иконка папки из `desktop.ini` |
| `links` (есть) | `to-file.lnk`, `to-folder.lnk`, `broken.lnk` — через настоящий `ShellShortcutService` | прозрачность ярлыка, миниатюра, «Перейти к оригиналу» |
| `archives` (есть) | `nested.zip` (`ZipArchive`) и `nested.tar.gz` (системный `tar.exe`) с одинаковым содержимым — `readme.txt` и `docs\` из двух файлов; `nested.7z` и защищённый паролем `locked.zip` копируются из `tests/Fixtures/` по имени (по расширению нельзя: `.zip` там уже занят `texts.zip`); плюс настоящая папка `plain.zip` — контрольный случай | архив как папка, четыре формата, извлечение, undo, «пусто или защищено», папка с архивным именем |
| `state` — **не профиль** | `state.json` чужой сборки кладётся в data-dir прогона полем сценария `"state": "0.3.0"` (файл `tests/Fixtures/state/0.3.0.json`) — до старта `App`, раньше, чем любой профиль успел бы отработать. Файла нет — прогон падает сразу, а не проходит молча. Пути внутри обезличены и ведут в никуда: это и есть нормальный случай для сохранённого состояния | чтение чужого состояния, пропавшие папки, настройки из файла |

`.gitignore` игнорирует `*.meta` и `*.lnk` — такие файлы только генерировать.
Очистка — `SandboxBuilder.Remove`: junction удаляется как junction, дальше
снимается read-only (профиль `attrs` пишет такой файл нарочно, а
`Directory.Delete` на него ругается — `--rebuild` той песочницы падал), потом
дерево целиком.

**Песочница переживает прогон и делится между сценариями.** Сценарий
убирает за собой сам (`permanent-delete` в конце); упавший на середине — не
убирает, и следующий прогон считает чужие файлы. Отсюда `fs delete` первым
шагом там, где важен точный счёт, и `--rebuild`, когда песочница разъехалась.

## Матрица «трогал → проверь»

| Трогал | Автоматически | Руками (раздел ниже) | В логе |
|---|---|---|---|
| Навигация, `FolderSession`, приземление | `FolderSessionTests`, `ListingDiffTests`; харнесс: серия навигаций, выделение после подъёма | Скорость; Фокус | `nav.*`, `list.apply`, `ui.stall`, `First screen painted` |
| Иконки, миниатюры, шлюз, приоритеты | харнесс: холодная `photos`, счётчики | Скорость (все пункты — трижды ломали друг друга); Дерево (иконки после запуска) | `bg.thumb-*`, `icon.decode-ui`, `layout.*`, `[icon-diag]`, `slow shell load` |
| Шаблон плитки, `VirtualizingWrapPanel`, `TileLayout` | `TileLayoutTests`, `TileMetricsTests`; харнесс: проходы раскладки в простое = 0, `visuals` на шаблон | Скорость (смена вида); Фокус (`F2` в четырёх видах); Просмотр (миниатюры не прыгают) | `LAYOUT … visuals`, `COUNT layout.*`, `layout.realise` |
| Выделение, фокус, области, rename | `GridNavigationTests`, `WindowZonesTests`; харнесс: клавиатурные сценарии, `Keyboard.FocusedElement` | Фокус, выделение и клавиатура — целиком | — |
| Файловые операции, undo, конфликты, спутники | `BatchExecutorTests`, `FileOperationServiceTests`, `CompanionResolverTests`, `ConflictVerdictTests`, `ConflictBatchTests`, `MergeScannerTests`, `FileContentComparerTests`, `PathSafetyTests`; харнесс на песочнице (нужен шов диалогов) | Файловые операции; **Окно совпадений имён** (харнесс его не поднимает: `ScriptedDialogs` отвечает по политике); Обмен с системой | `ERROR` / `WARN`, строки операций |
| Буфер обмена, DnD | `ClipboardControllerTests`; харнесс: системный буфер читается из процесса | Обмен с системой — только вживую | — |
| Поиск, экстракторы | `ContentSearchServiceTests`, `NameFilterTests`, `ContentExtractorTests`; харнесс на `docs` / `big` | Просмотр (результаты в видах) | счётчик «не прочитано», время поиска |
| Панель просмотра, форматы | `AudioTagsTests`, `MeshFileTests`, `Fb2DocumentTests`, `EncodingProbeTests`, `PreviewRouterTests`; харнесс: скриншот каждой ветки | Просмотр и галерея | `nav.preview`, ошибки биндингов (трассировка) |
| Галерея, оценки, сайдкары | `RatingFilterTests`, `ImageFolderProbeTests`, `CompanionMetadataServiceTests`; харнесс: клик по звезде не пересобирает | Фокус (клик по звезде); Просмотр | `bg.ratings`, тик сторожа |
| Контекстное меню, shell | `ContextMenuBuilderTests`, `ShellExtensionCatalogTests`; харнесс: меню строится без исключений | Обмен с системой (сторонние пункты запускаются) | время `QueryContextMenu`, owner-drawn в логе |
| Настройки, `state.json`, палитра | харнесс: `state` из фикстур, все страницы настроек открываются | После обновления | `WARN` при загрузке состояния |
| Дерево, закладки, сторож | `FolderChangesTests`; харнесс: внешнее создание файла в песочнице → строка появилась | Дерево и закладки | `nav.trees`, `nav.watch` |
| Архивы как папки, извлечение | `ArchivePathTests`, `ExtractionServiceTests`; харнесс: `selfcheck` (ассоциации машины) + сценарий `archives` | Файловые операции; Обмен с системой (наружу пока нельзя) | `Extract:`, `Archive enumerate`, «Archives open as folders» в первых строках лога |
| Сборка, поставка, версия | `check.bat run`, `size-report.ps1` | После обновления; чистая машина | `Startup: first frame` |

## Чек-листы ручного прогона

Брать разделы, которых коснулся диф; «Всегда» — обязательный. Что не удалось
проверить — в отчёт (FINALIZING, п. 3).

### Всегда

- [ ] `.\tools\check.bat run` — зелёная целиком.
- [ ] Приложение открывается на той же папке, тем же размером, на том же
      мониторе.
- [ ] В логах сессии нет `ERROR` и лишних `WARN`.
- [ ] Ни одна подпись не выглядит как ключ ресурса.

### Скорость и отзывчивость

Три починки фризов 2026-09-01 ломали друг друга — прогонять после **любой**
правки навигации, иконок, приоритетов.

- [ ] **Навигация мгновенная**: вход, выход, первый вход, съёмный / спящий
      диск — адресная строка и дерево отвечают сразу, содержимое следом.
- [ ] **Пока сыплются миниатюры — окно живое**: холодная папка с сотнями
      RAW, сразу клики по строкам, дереву, колесо.
- [ ] **Первый экран в журнале**: `First screen painted in N ms: K icons,
      M awaited` на каждый вход, `abandoned` при уходе раньше. С «Быстрым
      чтением первых файлов» в Таблице на холодном кэше `N` не хуже, чем
      без. Зажатая стрелка в дереве не оставляет `abandoned` с остатком на
      **каждой** папке.
- [ ] **Зажатая стрелка в дереве** («стрелки открывают папку»): курсор без
      лага, листится папка остановки, промежуточные не листаются и не
      засоряют историю; одиночное нажатие открывает сразу.
- [ ] **Клавиатура по списку** не тормозит, пока генерируются миниатюры.
- [ ] **Клик по папке из папки с миниатюрами**: адрес и подсветка дерева в
      тот же кадр, список следом; `nav.refresh` в логе не появляется (порог
      33 мс). `Backspace` выделяет папку, из которой вышли, — и в быстрой
      локальной (один своп), и в медленной (очистка, спиннер).
- [ ] **Проход подряд по папкам с фотографиями** (стрелки, `Backspace` /
      `Enter`, закладки): показана выбранная сейчас папка, не предыдущая;
      миниатюры новой начинаются сразу.
- [ ] **`F5` с развёрнутыми ветками на медленном диске**: окно отвечает,
      ветки обновляются по мере ответа, развёрнутое и выделенное на месте.
- [ ] **Запуск после сессии на отключённом диске**: окно сразу, первый диск;
      с диском на месте — запомненная папка.
- [ ] **Удаление из результатов поиска** убирает строку (после операции и по
      тику сторожа), остальные не прыгают.
- [ ] **Смена вида** Плитки → Значки → Галерея → Таблица и обратно: одна
      заминка в момент переключения — норма; на навигации заминки нет.
      Строки на месте, выделение целиком, фокус в списке.
- [ ] Нет многосекундных `ui.stall` (сотни мс на холодной папке — норма).

### Фокус, выделение и клавиатура

- [ ] После **удаления** выделение на соседней строке, стрелка идёт от неё.
- [ ] После **переименования** строка выделена, список не прокручен,
      клавиатура на строке.
- [ ] **`F2` во всех четырёх видах**: редактор на месте подписи, имя без
      расширения выделено, `Enter` / `Esc` / клик мимо; прокрутка во время
      правки — редактор едет со строкой и режется краем; строка ушла из
      виртуализации — коммит по потере фокуса. В галерее цифры при открытом
      редакторе набираются в имя.
- [ ] После **`Ctrl+Z`** выделение на том, что вернулось.
- [ ] **`Alt+←` и `Backspace`** выделяют папку, из которой вышли; стрелка
      идёт от неё. И с «стрелки в дереве открывают папку».
- [ ] **Повторный вход в папку** — на строке прошлого раза.
- [ ] **Создание папки** (`Ctrl+Shift+N`, меню пустого места): создана,
      выделена, редактор открыт, имя выделено, `Enter` / `Esc`.
- [ ] **Клик по звезде** не пересобирает список: выделение, прокрутка,
      фокус, строка под курсором на месте.
- [ ] `Tab` / `Ctrl+1..4` по областям в обе стороны; свёрнутые закладки и
      выключенные кнопки пропускаются.
- [ ] **`Ctrl+1` переключает панели**, `Ctrl+Shift+E` ведёт в ту, из
      которой открыли; из адресной строки — где клавиатура была последней;
      без закладок — дерево.
- [ ] **Стрелки на краю сетки**: `→` в конце ряда — начало следующего, `↑`
      на верхнем — первый, `↓` в короткий последний — его последний; на
      первом и последнем каретка стоит. С `Shift` выделение растёт от якоря.
- [ ] Type-ahead прыгает на строку, не проваливается в другие обработчики.
- [ ] **Клавиша меню** (Apps) и `Shift+F10` открывают меню, и оно остаётся
      открытым.

### Дерево (диски) и панель закладок

- [ ] **Иконки строк на месте сразу после запуска** в обеих панелях,
      включая кастомные (desktop.ini) и спецпапки; повторить после сброса
      кэша. В логе нет `[icon-diag]`.
- [ ] Шеврон у пустой папки пропадает мгновением позже; у папки с детьми
      остаётся.
- [ ] Развёрнутая руками ветка **не сворачивается сама** — после `F5`,
      операции, переключения «скрытые».
- [ ] Созданная / удалённая / переименованная в списке подпапка отражается
      в развёрнутой ветке обеих панелей.
- [ ] `F5` подтягивает подпапку, созданную снаружи, не сворачивая ветку;
      свёрнутая сохраняет шеврон и показывает актуальное при раскрытии.
- [ ] Закладка на несуществующую папку остаётся строкой и ведёт на панель
      «папки нет».
- [ ] `Ctrl+2` / `Ctrl+Shift+E` подсвечивают текущую папку в нужной панели.
- [ ] Клик по строке открывает, по шеврону — раскрывает; `Alt` + шеврон —
      ветка целиком.
- [ ] Папку из дерева можно утащить наружу и внутрь; правый клик — цель
      операции без перехода.
- [ ] `Shift` + колесо — вбок; правый клик по пустому месту — меню панели,
      галочки работают.
- [ ] У закладки работают «…», `Ctrl+↑/↓`, бросок папки на полосу «+» и в
      пустое место; делитель двигается и переживает перезапуск.

### Файловые операции

- [ ] Деструктивная операция спрашивает, **Cancel по умолчанию** (`Enter`
      ничего не сносит).
- [ ] `Ctrl+Z` откатывает всё, кроме безвозвратного удаления.
- [ ] Прогресс и отмена на долгой операции.
- [ ] «Файл занят» называет процесс.
- [ ] Системный путь упирается в `SystemPathGuard`.
- [ ] Прерванная операция (закрыть окно / убить процесс на середине копии):
      частичная копия видна и объяснима, состояние не побито.

### Окно совпадений имён

Автотестами закрыта только логика (`ConflictBatch`, `PathSafety`,
`BatchExecutor` в Core); само окно харнесс не поднимает — `ScriptedDialogs`
подменяет `IDialogs` и отвечает по политике. Всё ниже — глазами, на
копировании между папками и на перемещении между дисками.

- [ ] Окно открывается **до** того, как что-то тронуто: `Esc` — и в целевой
      папке ничего не изменилось.
- [ ] Заголовок окна называет операцию и число совпадений; «Из:» и «В:»
      стоят над своими колонками и не разъезжаются при появлении полосы
      прокрутки.
- [ ] На плашках рисуются обе миниатюры (картинка — содержимым, не значком).
- [ ] Галки: левая = замена, правая = оставить как есть, обе = оба файла,
      снятые обе = пара снова не решена; после имени появляется слово
      «заменить» / «оставить» / «оба». Отвергнутая сторона гаснет; когда
      отмечены обе или ни одной — не гаснет ничего.
- [ ] Файл со спутником (`.png` + `.meta`, оба совпали): две строки, у
      каждой свой ответ; «оставить» на png и «заменить» на meta приезжают
      порознь.
- [ ] Больший размер и более свежая дата жирные; когда размер и дата
      совпадают — жирного нет.
- [ ] Одинаковые файлы: у имени стоит «Сравниваем…», потом доезжает «Файлы
      идентичны» — припиской в строке имени; при нескольких крупных (больше
      64 МБ) сначала отмечаются мелкие, окно не виснет.
- [ ] Галка «не спрашивать про одинаковые файлы» стоит с открытия
      (настройка по умолчанию включена): идентичные пары отметились сами
      «оставить»; сняли — вернулись в нерешённые, а то, что выбрано руками,
      осталось.
- [ ] Булеты «применить к нерешённым» не перетирают уже выбранное;
      «заменить, если новее» трогает только те пары, где новее исходник.
- [ ] **ОК** серый, пока решены не все; `Enter` ничего не запускает; `Esc`
      отменяет всю операцию. Нижняя полоса отделена и читается как «конец
      разговора».
- [ ] Размер окна переживает перезапуск; окно на отключённом мониторе
      возвращается на существующий.
- [ ] **Перемещение**: заголовок окна говорит «Перемещение»; после правой
      галки исходник остался на месте — и у идентичной пары тоже.
- [ ] **Папка на папку, обе галки**: под строкой сразу появились
      совпадения внутри с отступом и относительным путём, строка папки
      говорит «слияние: совпадений N, прочих файлов M»; вложенная папка
      пришла в слиянии и раскрыта; переключили папку на «заменить» —
      вложенные строки исчезли, вернули — появились с прежними ответами.
      После ОК: несовпавшие файлы скопировались, совпавшие — по ответам,
      при перемещении опустевшая папка исчезла, `Ctrl` + `Z` вернул всё.
- [ ] Папка на папку, левая галка: «Папка заменится целиком, без слияния»
      видно на самой паре и только при этом выборе.
- [ ] **Извлечение из архива** с совпадением: строки без «Сравниваем…»,
      обе галки на папке дают новое имя, не слияние.
- [ ] **Копия в свою же папку**: `Ctrl` + `V` и `Ctrl` + перетаскивание
      дают «имя (1).ext» без окна; спутник получил такое же имя.
      Перетаскивание без `Ctrl` туда же по-прежнему ничего не делает, папку
      в саму себя и в свою подпапку не бросить.
- [ ] **Вырезать и вставить в ту же папку**: ничего не спрашивается и не
      меняется, вырезание снято, в статусе «Уже в этой папке, вырезание
      снято».

### Архивы как папки

Сценарий `archives` закрывает вход, листинг, извлечение и гейты; глазами —
то, что он не поднимает.

- [ ] **«Открыть» файл из архива**: временная копия открывается нужной
      программой, строка состояния предупреждает, что правки не вернутся;
      `%LocalAppData%\Wander\tmp` не растёт бесконечно (папка старше суток
      уходит на следующем старте).
- [ ] **«Извлечь…»** из меню: диалог выбора папки, прогресс, отмена на
      середине, `Ctrl` + `Z`; на архиве, выделенном в обычной папке, —
      извлекает всё содержимое.
- [ ] **Перетаскивание из архива** не делает вида, что работает: строка
      состояния отсылает к «Извлечь…».
- [ ] **Меню внутри архива** — четыре строки, ни одной серой; сторонних
      пунктов нет. На архиве в обычной папке рядом с нашим «Извлечь…»
      живёт шелловское «Извлечь все…» и работает по-своему.
- [ ] **Ассоциации**: отдать `.7z` 7-Zip'у — архив перестаёт открываться
      папкой (как в Проводнике); вернуть — снова открывается. Перезапуск
      приложения обязателен, набор расширений читается один раз.
- [ ] **`.rar`** на машине с WinRAR: фикстуры нет, проверить живым файлом.
- [ ] Панель просмотра внутри архива: карточка с именем, размером, датой и
      «В архиве», без миниатюр и без переписи папки.
- [ ] **Архив в дереве**: стоит среди папок, раскрывается в свои папки,
      клик по папке внутри — листинг; пустой архив теряет шеврон при
      раскрытии; drop на узел архива не принимается; закладка на архив
      раскрывается. В панели закладок и в дереве дисков одинаково.

### Обмен с системой

Только вживую — второе приложение из сессии не поднять.

- [ ] Копирование Wander → Explorer и обратно.
- [ ] DnD наружу (Explorer, браузер, редактор) и внутрь; `Ctrl` копирует,
      `Shift` перемещает.
- [ ] Двойной клик открывает ассоциацией.
- [ ] Пункты сторонних приложений в меню запускают их.

### Просмотр и галерея

- [ ] Панель просмотра: картинка, RAW, текст, код, PDF, 3D, аудио, веб,
      папка, диск — по разу.
- [ ] Галерея сама включается в папке со снимками, выключается в обычной.
- [ ] Оценки и метки пишутся в сайдкар и переживают переход туда-обратно.
- [ ] Миниатюры не прыгают размером при прокрутке.

### Окружения

- [ ] DPI 100 / 150 / 200 %, два монитора с разным DPI, геометрия окна при
      отключённом мониторе (`WindowPlacement`).
- [ ] Тёмная тема Windows и высокая контрастность — всё читаемо.
- [ ] Не-администратор и elevated; профиль с кириллицей в пути.
- [ ] OneDrive «файлы по требованию»: листинг и миниатюры не тянут файлы
      из облака.
- [ ] Съёмный диск (VHDX: смонтировать, открыть, извлечь), сетевая шара,
      привод без диска, BitLocker-том.
- [ ] Английская локаль системы: «Восстановить» из корзины работает.

### После обновления или смены зависимостей

- [ ] Кэш миниатюр сброшен, папка не растёт бесконечно.
- [ ] `state.json` прошлой версии загружается (или честно `new AppState()`).
- [ ] `About` — верные версия, коммит, дата.
- [ ] Single-file сборка стартует на чистой машине (Windows Sandbox: без
      .NET; наличие WebView2 в Sandbox проверить).

## Предрелизный прогон

Дополняет RELEASING.md; порядок — от дешёвого к дорогому, регрессия на
любом шаге останавливает релиз.

1. **База**: `check.bat run`, `deps.ps1` без циклов, `metrics.ps1` снимок,
   доки сверены (FINALIZING, п. 5).
2. **Релизная сборка**: `publish.ps1`, размер и SHA, `size-report.ps1`
   против PERFORMANCE (категории не выросли без причины).
3. **Первый запуск и обновление**: чистая машина (Sandbox) — SmartScreen,
   без `state.json`, умолчания, первая папка; поверх прошлой версии —
   `state.json` и кэш.
4. **Функциональный прогон** на песочнице релизным exe: «Всегда» + все
   разделы чек-листов; автоматизированная часть — харнессом (когда будет),
   остальное руками.
5. **Окружения** — раздел выше, хотя бы DPI и один «странный» том.
6. **Замеры** — таблица ниже, все строки; плюс 30-минутный soak: случайная
   навигация по песочнице — память выходит на плато, handles стабильны, лог
   без `ERROR`, CPU в простое ~0 %.
7. **Итог**: числа в PERFORMANCE (дата, версия), находки разложены по
   спискам, exit-критерии: 0 открытых P0 / P1, `WARN` / `ERROR` в сессиях
   релизного прогона — ноль, метрики в порогах.

## Метрики и пороги

Порог = базлайн из PERFORMANCE + 20 %; хуже — регрессия. Первые базлайны для
новых строк снимаются первым прогоном.

| Метрика | Как снять | Порог / базлайн |
|---|---|---|
| Первый кадр | `Startup: first frame N ms`, три запуска релизного exe | ~1650–2050 мс (PERFORMANCE) |
| Неответ UI | `PERF ui.stall` | одиночные < 400 мс, частые < 100; на холодной папке — сотни, не секунды |
| Первый экран | `First screen painted in N ms` по профилю `photos` холодно / тепло | базлайн снять |
| Шаблон плитки | `LAYOUT <вид> container: N visuals` | Tiles 8, Icons 6, Gallery 9 (без оценки) |
| Реализации на навигацию | `COUNT layout.new: N in K passes` | K = 1 |
| Раскладка в простое | харнесс: проходы за 5 с без ввода | 0 |
| Миниатюры RAW | `bg.thumb-shell` / `RawThumbnail`, мс на файл | ~3 мс WinRT, 75 через оболочку — регресс |
| Память | `SYS` (предложение ниже) или харнесс: `WorkingSet64`, `PrivateMemorySize64` — пик и плато после 200 навигаций | smoke 344 / 225 МБ; плато — снять |
| GC | `GC.CollectionCount(0/1/2)`, `GetTotalAllocatedBytes` за окно, `GetGCMemoryInfo` (LOH, % в GC) | gen2 не растёт линейно с навигациями; аллокации на навигацию — снять |
| Handles, потоки | `Process.HandleCount`, `Threads.Count` в soak | стабильны после разогрева |
| CPU | `TotalProcessorTime` дельта: в простое 10 с | ~0 %; таймеры останавливают себя |
| Диск | размер `thumbs` до / после, `bg.thumb-disk-write` | в пределах лимита, подрезка срабатывает |
| Закрытие | `OnClosing` → смерть процесса | окно исчезает сразу; процесс < 2 с |
| Лог | размер файла сессии, повторяющиеся строки | нет заливки одинаковыми исключениями |
| Поиск | время по `big` (5000 файлов) и `docs` с кэшем / без | 0,2 с обход репозитория; кэш ×5 |

**Предложение — строка `SYS` в логе**: тот же heartbeat `UiStallWatch`
раз в 5 с (и на каждый `ui.stall`) пишет `SYS ws=… private=… gen=0/1/2
alloc=+N MB loh=… handles=… threads=… cpu=…%`. Цена — несколько вызовов
`Process` / `GC`, ничего не аллоцирует заметно. Даёт замеры и в живых
сессиях, не только в харнессе; Core не трогается. Глубже — `dotnet-counters`,
`dotnet-trace` / PerfView, dotTrace из Rider: по требованию, не в прогон.

## Автоматизация — харнесс

Решение 2026-09-02: автоматизировать всё, что даёт адекватный результат;
оконный харнесс в репо — первым (те же кодовые пути, что у пользователя,
~70 % чек-листов, Claude не трогает экран). Отвергнуто: фейковая
`IFileSystem` на всё приложение (App и `Preview/` читают `System.IO`,
миниатюры — шелл; живая проверка работы с файлами теряется). Позже: UIA
(FlaUI) на релизном exe в Windows Sandbox — настоящий ввод и DnD с
Проводником; `--scenario` внутри продукта — только если понадобится
релизный бинарник без UIA; VHDX / SMB-loopback — дёшево, как дополнение.
Куда всё это уносить с рабочей машины — PLAN, секция T (к обсуждению после
0.3.1).

`tests/Wander.Harness` — консольный STA-проект со ссылкой на `Wander.App`:
настоящие `App`, `MainWindow`, `MainViewModel`, окно за экраном
(`App.Headless`), сценарий из JSON, артефакты в `artifacts\<имя>-<время>\`
(`report.md`, `screenshots\NN-имя.png`, `metrics.json`, `data\` с логом и
`state.json` прогона). Ввод синтезируется на UI-поток; экран и мышь не
трогаются.

```pwsh
.\tools\check.bat qa                              # сборка + selfcheck + smoke-walk, минуты
$h = "tests\Wander.Harness\bin\Debug\net10.0-windows10.0.19041.0\Wander.Harness.exe"
& $h selfcheck                                    # генераторы против читателей Core
& $h sandbox D:\tmp\sb --profiles photos,raw      # песочница руками
& $h run tests\Wander.Harness\Scenarios\smoke-walk.json [--sandbox dir] [--out dir] [--rebuild]
```

Коды: 0 — прошло, 2 — шаг упал (скриншот `fail-N` и причина в отчёте),
70 — упал сам харнесс. `check.bat qa` — отдельный режим, в обычный
`check.bat` не входит: минуты против секунд.

`selfcheck` проверяет генераторы теми классами, которыми их читает
приложение: превью и ориентация из CR3 / DNG / JPEG
(`RawPreviewExtractor`, `MetadataExtractor`), `needle42` из пяти
zip-документов (`ZipDocumentExtractor`), четыре кодировки
(`EncodingProbe`), fb2 с обложкой, теги `tone.wav` (`AudioTags`) и «в кубе
12 треугольников» для всех трёх мешей (`MeshFile`). Плюс архивы профиля
`archives`: открывает ли шелл **этой** машины каждый из них как папку и
отдаёт ли размер записи — это состояние ассоциаций, не код, и красный шаг
в сценарии `archives` иначе выглядел бы виной приложения. Формат, чью
ассоциацию забрал сторонний архиватор, — строка в отчёте, не падение.
Смысл — отличать сломанный сценарий от кривого тестового файла.

Фикстуры проверяются там же и по тому же правилу: `SandboxBuilder` помнит,
что откуда скопировал (по имени их не найти — имя у фикстуры произвольное, а
расширение может совпасть с генерируемым), и `selfcheck` читает их теми же
классами — теги и обложку у аудио, текст у настоящих docx / epub. Формат, для
которого читателя у нас нет (pdf, doc, webp, zip), только перечисляется:
подменённая фикстура ломается тихо — трек без тегов выглядит как трек без
тегов, и скриншот в `preview-formats` правдоподобен в обоих случаях.

**Что подменяется.** `ILogger` → `CapturingLogger`; `IDialogs` →
`ScriptedDialogs` (ответ по виду диалога, конфликты по политике, каждый
вопрос попадает в отчёт); `IAppStateStore` не подменяется — пишет в
изолированный корень.

`CapturingLogger` слушает событие `FileLogger.Written`, а не свои
собственные вызовы: сервисы из `PlatformBootstrapper` получают логгер в
конструкторе и больше не ищут его в локаторе, поэтому логгер,
зарегистрированный поверх, их строк не видит. До 2026-09-02 `assert-log
noErrors` их и не видел — `ERROR Delete failed` лежал в файле, а прогон
рапортовал «ошибок нет».

**Окно не должно оказаться на экране.** `App.Headless` в `App.OnStartup`
только включается (`|=`); присваивание затирало флаг харнесса, и окно
выходило на рабочий стол с фокусом (2026-09-02). `HarnessApp` перепроверяет
флаг и отказывается работать при выключенном, а позицию окна пишет в лог:
`HARNESS window at (-26214, -26214), taskbar=False, active=False`.

**Диалоги тоже.** Окно прогресса (`ProgressDialog`: `CenterOwner`, владелец
за экраном) WPF ставило в (0, 0) с фокусом — на каждой вставке в `file-ops`
и `archives` оно мелькало на рабочем столе человека (2026-09-02; зонд на
WPF .NET 10: `CenterOwner` при владельце в (−32000, −32000) даёт ровно
(0, 0), `ShowActivated=false` с `ShowDialog` работает). Теперь каждое окно
приложения зовёт `App.ParkIfHeadless(this)` в конструкторе; новое окно
обязано тоже, иначе харнесс снова поднимет его на экран. Подтверждения
человеком, что мелькание ушло, пока нет — отсюда правило «сценарии только по
просьбе» ниже.

**Поля сценария**: `name`, `sandbox` (папка под `%TEMP%\wander-sandbox`),
`profiles[]`, `state` (см. таблицу профилей), `stopOnFailure`,
`stepTimeoutMs`, `steps[]`. Комментарии `//` разрешены.

**Шаги сценария** (`{"do": …}`; пути с `{sandbox}` и `{out}`):
`navigate path` (ждёт первый экран и тишину), `wait-idle [quietMs,
idleTimeoutMs]`, `screenshot name`, `view Details|Tiles|LargeIcons|Gallery`,
`select names[]` (через контрол активного вида — путь клика), `command
name` (delete, permanent-delete, copy, cut, paste, new-folder, refresh,
undo, up, back, forward, clear-search; деструктивные — только когда текущая
папка внутри песочницы), `rename name to`, `key Key [repeat]` (без
модификаторов — `Keyboard.Modifiers` читает настоящую клавиатуру; для
`Ctrl`-сочетаний есть `command`), `settings name value` (свойство
`SettingsViewModel`), `dialogs {default | kind + accept | conflict | prompt
| folder}`, `fs {op: create | mkdir | append | delete | lock | unlock,
path}` (только в песочнице; `delete` не ругается на отсутствующий путь —
это шаг сброса; `lock` держит хэндл до `unlock` или конца прогона),
`preview on`, `preview-format {ext, name}` (файл с таким расширением, если
он тут есть, — выделить и снять; нет — строка в отчёте, не падение),
`tree-expand path [panel: drives|bookmark] [expand: false]`,
`bookmark {op: add|remove, path}`, `search {name, text, subfolders,
binaries, searchTimeoutMs}`, `soak {minutes, maxWorkingSetGrowthMb,
maxHandleGrowth, seed}`, `assert-log {contains | regex | absent | noErrors
[+ allow[]], scope: step|all}`, `assert-entries {count | min | max |
contains | absent | selected, scope: model|visible}`, `assert-path {is,
crumbs[]}`, `measure name`,
`sleep ms`, `note text`.

`command` и `key` ходят через `CanExecute` и через
`Window.InputBindings`, как настоящая строка меню и настоящий хоткей:
недоступная команда не выполняется, а пишет строку в «Notes» отчёта — так
проверяется, что внутри архива или корзины она именно не срабатывает.
Синтетическое событие клавиши мимо `InputBindings` пролетает (WPF сверяет
их внутри input manager), поэтому раннер сверяет их сам — но только когда
событие никто не обработал. `Enter` в табличном виде до хоткея не доходит:
его съедает `DataGrid` (TECHDEBT), поэтому сценарии жмут `Enter` в видах на
`ListBox`.

`tree-expand` идёт по уровням, а не зовёт `Trees.ExpandTo`: ветка читает
детей с диска в момент раскрытия, и путь на шесть уровней вниз до этого
просто не находится.

`assert-entries scope: visible` считает то, что показывает список, а не
`Entries`. Разница не теоретическая: **маска имени фильтрует вид, а не
`Entries`** — после `search name` вьюмодель держит всю папку, и проверка по
ней проходит независимо от того, сработал поиск или нет (в `search` так и
было: «не меньше тысячи» в папке на 5050 файлов верно и до поиска,
замечено 2026-09-02). Поиск по содержимому — наоборот, кладёт результаты
прямо в `Entries`, там `scope` не нужен.

`preview-format` — для форматов, которые никто не генерирует: они приезжают
из `tests/Fixtures/` по расширению, и на машине без фикстуры сценарий обязан
пройти. Берёт первое совпадение по имени, поэтому годится только там, где
генератор такого расширения не пишет: `.pdf` в `docs` — это сгенерированный
`manual.pdf`, у которого свой шаг. Чего не нашлось, перечислено в отчёте
списком «эту ветку панели не открывали».

`assert-log noErrors` + `allow[]` — сценарий, который **нарочно** вызывает
ошибку (занятый файл, безвозвратное удаление), называет её. Именно
называет: ожидаемая ошибка остаётся проверяемой, а не становится уровнем,
на который перестали смотреть.

`soak` меряет плато **от конца первой минуты**, а не от старта: первая
минута — это заполнение кэша миниатюр, и на ней провалился бы любой
прогон. Отсюда минимум три минуты (одна на прогрев, две на наблюдение);
предрелизные тридцать — это `"minutes": 30` при тех же порогах.

**Сценарии** (`Scenarios\`):

| Файл | Профили | Что закрывает |
|---|---|---|
| `smoke-walk` | photos, raw, big | все виды, превью JPEG / CR3 / DNG, 5000 файлов, создание → rename → корзина → undo |
| `focus-keys` | photos | «Фокус, выделение и клавиатура»: стрелки на краях сетки, `F2` в четырёх видах, выделение после delete / undo / up |
| `tree-bookmarks` | photos, raw, big | ветка не сворачивается после `F5`, созданная папка появляется в ветке, закладка добавляется и снимается |
| `file-ops` | photos, attrs | copy / cut / paste между папками, три ответа на один конфликт, занятый файл, read-only, цепочка undo |
| `search` | docs, code, big | маска по имени, `needle42` по содержимому во всех форматах, четыре кодировки, подпапки, поиск по 5000 |
| `preview-formats` | photos, raw, docs, code, media, links | по скриншоту на ветку панели (26 при нынешнем наборе фикстур; чего не хватило — в отчёте) |
| `watcher` | photos | появилось / исчезло / изменилось снаружи — без единого `refresh` |
| `archives` | archives | zip / 7z / tar.gz как папки: вход по пути и по Enter, подпапка, `up`, крошки; архив как узел дерева (`tree-expand` до папки внутри); delete / cut / new-folder внутри не срабатывают; `copy` -> `paste` в обычную папку -> `undo`; настоящая папка `plain.zip`; защищённый паролем архив |
| `soak` | photos, raw, big, names | случайная навигация, плато WS и handles, `SYS` в логе |
| `state-upgrade` | photos, attrs | старт на `state.json` чужой сборки: настройки из файла применились (скрытые видны, а они по умолчанию выключены), пропавшие закладки — строки, а не ошибки |

Что клавиатурой и мышью не воспроизводится (`Shift`-выделение, DnD,
чужие пункты меню), помечено в сценариях шагом `note` и попадает в отчёт
списком «посмотреть глазами».

**«Тишина»** = `IsListLoading == false`, строка `First screen painted` для
папки, в которую навигировали (ждём до 5 с), и ничего в логе 400 мс.
Скриншот — `RenderTargetBitmap` визуального дерева: WebView2 (HWND) в нём
пустой.

**Полный прогон** (Debug, 2026-09-02, после правок фикстур и проверок):
восемь сценариев PASS; `state-upgrade` и `archives` появились следом и
прошли отдельно (`archives` — 65 шагов, WS пик 463 МБ). Числа — базлайн харнесса, не приложения: Debug
плюс сам харнесс в том же процессе.

| Сценарий | Шагов | WS пик | gen2 | Заметное |
|---|---|---|---|---|
| `smoke-walk` | 46 | 529 МБ | 5 | `ui.stall` 4,9 с на старте |
| `focus-keys` | 48 | 495 МБ | 4 | — |
| `tree-bookmarks` | 29 | 478 МБ | 5 | — |
| `file-ops` | 52 | 479 МБ | 5 | 9 диалогов, все в отчёте |
| `search` | 32 | 454 МБ | 5 | `needle42` — 13 строк: 12 наших плюс `letter.rtf` системным фильтром; обе pdf в «не удалось прочитать» |
| `preview-formats` | 91 | 562 МБ | 6 | 26 скриншотов; `.m4a` / `.gif` / `.mp4` в отчёте названы неоткрытыми |
| `watcher` | 24 | 439 МБ | 4 | — |
| `soak` (3 мин) | 8 | 611 МБ | 9 | WS +34 МБ, handles −21, LOH 67 МБ пилой |

Общее: `ui.stall` 4–6 с на первом экране `C:\` под Debug (холодный JIT и
первые shell-вызовы — проверить на релизной сборке, TECHDEBT). `ERROR` —
только нарочный `locked.txt` в `file-ops`; `WARN` — только «Permanent
delete», которым каждый сценарий за собой убирает.

`soak` — та самая форма, ради которой он есть: рабочий набор вышел на
плато после первой минуты (+34 МБ за две), хэндлы вернулись ниже начала
(−21), LOH ходит пилой (128 → 67 МБ), то есть собирается, а не копится.

**Кто что делает** (по правилу чистки #4: Фабле — решения и риск, Опус —
механика; задачи Опусу — PLAN, S):

| Роль | Кто |
|---|---|
| Спроектировать сценарии под правку, разобрать падение, оценить скриншоты на UX-регрессию, разведочный «ручной» прогон через харнесс шаг за шагом | Fable 5.1 (Opus 5 при экономии) |
| Прогнать набор, сверить лог и метрики с порогами, собрать отчёт, разложить находки по спискам | Sonnet 5 или Opus 5 |
| Всё с настоящим Проводником, мышью и глазом; экзотические тома; решение «релиз / не релиз» | человек |

Claude никогда не ведёт настоящий рабочий стол: харнесс — только окно за
экраном; UIA — только внутри Sandbox / VM, где чужого экрана нет.

**Когда что гоняется.** Правило простое: чем дороже прогон, тем ближе он к
концу работы, а не к каждой правке.

| Повод | Что запускается |
|---|---|
| Любая правка кода | `check.bat` — всегда, это не обсуждается |
| Тронуты XAML, ресурсы, регистрация сервисов, старт | + `check.bat run` |
| Тронуты генераторы песочницы или состав `tests/Fixtures/` | `selfcheck` (секунды) |
| Правка в области, у которой есть сценарий (матрица «трогал → проверь») | этот сценарий, `--rebuild` если менялся состав песочницы — **по прямой просьбе человека** |
| Не воспроизводится по коду: «прыгает фокус», «пропала миниатюра», «что там в панели» | сценарий-однодневка рядом с нужной папкой, находки — `note` в отчёт — по просьбе |
| Закрытие блока PLAN, крупный коммит, финализация куска работ | `check.bat qa` + сценарии задетых областей — по просьбе |
| **Подготовка к релизу** (отдельная команда, WORKFLOW.md) | весь набор + `soak` с `"minutes": 30` + замеры версии |

**Сценарии харнесса Claude сам не запускает** (правило 2026-09-02, причина
— абзац «Окно не должно оказаться на экране»): называет, какие сценарии
задеты, и ждёт «прогони». Запустив, кладёт в отчёт путь к `report.md`,
упавшие шаги, скриншоты, которые стоит посмотреть глазами, и строки `SYS` /
`ui.stall`, если они сдвинулись. `check.bat` без `run` и тесты Core окон не
открывают и запускаются на каждую правку, как раньше.

**Чек-лист для человека** — в приложении, автоматический (PLAN N, не
решено): после фичи Claude добавляет пункты «что посмотреть глазами»,
приложение отмечает само то, что видит, остальное — человек; можно
дописывать и пропускать. Пока его нет — пункты в отчёте Claude в чате.

## Что ещё берут из практики QA

- **Находка = шаги, ожидание, факт, строка версии (`v… R, коммит, дата`),
  лог сессии, скриншот.** Severity: P0 потеря данных / падение, P1 не
  работает фича, P2 неудобно, P3 косметика.
- **Теги набора**: smoke (минуты) / regression (час) / release (день);
  флаки считаются и чинятся, не «перезапустил».
- **Базлайны по релизам** — таблица в PERFORMANCE «версия → числа»; тренд
  важнее одного числа.
- **Сценарии потери данных**: kill на середине копии, диск заполнен (VHDX
  50 МБ), `state.json` при внезапном закрытии, два экземпляра Wander на
  одном кэше.
- **Первый запуск / обновление / удаление** (портативный: удалить папку
  данных) — отдельный проход, не «и так работает».
- **Доступность-минимум**: клавиатурой всё, DPI, контраст; Narrator и
  `AutomationProperties` — когда дойдут руки.
- **Безопасность превью**: html с JS и внешними ресурсами, имена с `..`,
  циклы junction, будущие архивы — zip-бомба и `../` в записях.
